# メモ
- 専用装置時代は実装コストが高く、物理的な制約を伴うRANシステムの装置は特に仕様ファーストで進むのは合理的であった
- コアも4G初期では専用装置だったが、その後のNFVや今日の汎用ハード上でコンテナ実装された5GCなど、コアはソフトウェアが中心となっている
- ソフトウェアは物理的な制約が少ない。実装ファーストありきで標準化を進めるのは、実際に使われない仕様の発生を抑制したり、製品導入までのリードタイムが短くできたりなど、効率的な面も大きい。
- そこで、既存のRANから次の世代のコアの実装に繋ぎ、互換性を図ることができるようにするのは意義がある

- 実際に必要なのは「5Gシステム全体の再現度が高くて、且つ手軽な実験環境」であって、実機が繋げるのは再現度を担保するための一手法
    - この抽象度だと5Gコアがつながる方が当然5Gシステム全体の再現度は高くなり、実際の無線が使えることでOSSの発展やローカル5Gの初期トライアルに活かせる、という整理ができるんじゃないかと。
- 2枚目の画像のような論理構成にすると破綻なく話が進むと思っていて、「そもそも何で5Gでないといけないの？」というところに関しては、世界観を示す必要がある部分で、「ローカル5Gはもっと使う側がコアをカスタムできるべき」だったり「次世代のコアはもっとOSSの知見を取り入れるべき」みたいなデカい話が理由になってくるんじゃないかと考えてます。

- 次世代のネットワークの一例として、IoT-NTNがある。
    - 現行のIoT-NTNではEPCが必須だが、3GPPのRelease 17以降では5G-NTNとして5GCをサポートする予定

- PoCとして、IoT-NTNの要件を充足するか示す？

- 主張：次世代のコアの設計には、世代間の共通部分を活用し、RANとコアを疎結合にすることで、次世代アーキテクチャの検証や移行を柔軟に行える設計手法が必要
- 背景：5G以降ではコアはソフトなので進化が早いが、対応する無線設備のは更新コストが高く、
/新機能の実証・導入のボトルネックになるはず
- 問題提起：世代間のプロトコル共通性は仕様上存在するが、それを活用して「旧世代RANで新しいコアを動かす」といった、柔軟性を高める実装モデルや環境がない
- 目的：4G/5Gのプロトコルの可換性を実装で示し、RANを更新せずともプロトコル変換のみで、最新のコアネットワーク機能を利用可能にする手法を提案
- 提案：4G-5Gのプロトコルコンバータ
- 既存研究・関連研究：シミュレータ・SDR ・ローカル5Gとの比較
- 評価：
    - 定性：変換可能な機能の特定→4G/5G間でプロトコルの機能は大して変わらない
    - 定量：変換のオーバーヘッド→コンバータを挟んでもこれくらいしかデグレがない
    - maybe：実UEにApp別スライシングを入れてスループット取る(実機の必要性の担保)
- 貢献：既存のLTE網などを流用しながら、低コストに次世代コアの先行研究や機能検証を行える「実機ベースのプロトタイピング環境」を提供

# 参考文献
## nelioさん
- 2023 Proc5GC：NFベースではなく、プロシージャベースの5GCのアーキテクチャを提案・実装
    - 問題：NFベースのマイクロサービスは障害時の影響が大きく、スケールしにくい
        - ステートフルな接続の維持：RANとAMF間のSCTP接続が切断されると障害とみなされ、AMFの保守や更新が容易ではなくなる
        - NFベースのサービスモデル：単一のNFに多くの機能が組み込まれているため、新しい機能を実装する際には、それが既存の機能に影響を与えないことを検証するためのコストが高くなる傾向がある
        - NF間のメッセージングモデル：複数のNF間でのメッセージ交換はアトミックトランザクションとして扱われるため、メッセージごとに大きなオーバーヘッドが発生
        - UE収容モデル：単一のNFインスタンスが多数のUEにサービスを提供するため、NFインスタンスがダウンした場合の影響は大きくなる
        - CNの冗長モデル：冗長化されたCN機能では、各NFのインスタンスを複数実行するのが一般的ですが、AMFを切り替える際にUEとCNの両方にシグナリング負荷が発生
    - このような問題はそもそも5GCがNF単位で設計されてるのが問題→
    - 提案：UEのプロシージャベースに設計を変え、ステートを全て外部DBに保存するステートレスな形にした
    - RegistrationのみをPoCとして実装
    - 200～6400台のUERANSIM UEを接続し、Registration開始から完了までの所要時間・平均時間を計測・比較してスケーラビリティを評価
- 2024 Cloud5GC：Proc5GCの概念をパブリッククラウド（AWS）上で実現
    - LambdaやSQS、DynamoDBを使用したProc5GCのマイクロモバイルコア版実装をした
    - 多数UE（1024台）同時登録時におけるスケールアウト性・安定性をfree5GCと比較検証
    - Cloud5GCの方が遅いが、3GPP規定タイマー（4秒以内）を十分クリア
    - ボトルネックはDBアクセス
    - 前より関連研究が充実
        - `Investigating Inter-NF Dependencies in Cloud-Native 5G Core Networks`：5GCはSBAとCUPSを前提としたクラウドネイティブ設計だが、単一NFのリソース制限が5GC全体のボトルネックになる
        - `PP5GS`：5GCにおけるプロシージャベースかつステートレスなアーキテクチャを導入。思想はProc5GCと同じだが、メインの焦点はNF間通信のオーバーヘッド削減で、こっちはEvent-drivenではなくプロシージャ専用NFが常駐のk8s Podとしてある。
        - `ECHO`：SCTPプロキシを採用し、パブリッククラウドのロードバランサ機能を活用し、スケーラブルなCNの実現を目指している
        - `MAGMA`：AMF、SMF、UPFを統合したAGWと呼ばれる統合NFを定義。SCTPプロキシとして機能するだけでなく、NF間のC-Planeトラフィックを削減。AGW内では、モビリティ管理およびセッション管理モジュール、そして他のNFとのやり取りにおいてgRPCが活用。このアプローチにより、シグナリングオーバーヘッドが最小限に抑えられ、一時的なトランスポートネットワークの切断に対する耐性が確保。
        - `Organic 6G Core`：5GCのSBAによってもたらされるNF間調整の複雑さに焦点を当て、機能分割の概念を再検討した6Gコアネットワークの設計を提示
    - 要件がより明確に
        - スケーラビリティ：UE接続数に応じて必要な数のマイクロモバイルコアを並列に起動・実行できるように設計
            - 複数UEの登録で評価
        - 冗長性：実装は処理ユニットごとに分割され、1 つのプロセス実装が別のプロセス実装に影響を与えないことが保証
            - 設計で担保
        - 使用されるリソースの最小化：UEからの要求に対応するために必要な機能のみをアクティブ化するように設計されているため、リソースの無駄が排除
            - 設計で担保
- 2025 P-Cloud5GC：データ通信（U-Plane）の経路確立まで機能を拡張
    - CN-UP手順専用の「N4Dispatcher」と呼ぶサーバーレス関数を追加し、N4GWからのPFCPメッセージを管理
    - ハンドオーバーシーケンスやボイス通話向けのIMS等は含まれない
    - PDU +登録合計で約2800msなので3GPP規定のタイムアウト（登録4秒、PDUセッション15秒）は余裕でクリア
    - FWAや大規模IoT向けに実用的
    - タイムクリティカルな制御手順（例:ハンドオーバ）は現状ベストエフォートなクラウド回線では課題あり
    - DBアクセスが主なボトルネックであり、さらなる最適化が必要

- 2025 ADMobile：RANにCNの機能を統合し、各ノードがインターネットのように自律的に分散して協調動作する新しいアーキテクチャを提案
    - 問題
        - A スケーラビリティ：コンテキストだらけでCNが輻輳しやすい
        - B 自律分散性：RAN-CN接続が失われるとRANがUEにサービスを提供できなくなる
    - モバイルシステムの要件
            - AAA(認証・承認・アカウンティング)：通信サービスへのアクセスを制御し、その利用状況を記録・追跡するための概念
            - モビリティ管理：無線リンクのハンドオーバーを制御するネットワーク機能
            - ユーザープレーン：ユーザーと外部ネットワーク間の通信を可能にするチャネル
    - 関連研究の整理：それぞれAとBのどちらの問題に対処しているかと、整理した要件のうちどれを満たしているかを星取表で整理。大抵のものにおいて、課題B：RAN-CN接続が失われた場合にどのようにサービスを継続するかに対処していない
        - CoreKube
        - SpaceCore
        - MM-QoS
        - DC-SBA
        - RANコアコンバージェンス：CNをRAN近傍に配置、あるいはRAN内に埋め込む構成
            - https://ieeexplore.ieee.org/abstract/document/10001487?casa_token=EwbfA3tDwcUAAAAA:Mwo5yyRCk1i-lnGB8r27KKkfqWor84OrIk-P_PwkOr4zwvCgWb0Y8YYchZ9y8yStSkNejUpD
            - https://ieeexplore.ieee.org/abstract/document/10001281?casa_token=NvcNTHjn7SQAAAAA:H8z-ruC3YE7u5uWlKDV7oHhFmZQGkjfIJvg_85MdmWXBMul1CKftZf0aVfNgZN_3AS8N9uDx
    - 提案：ADMobile
        - AD‑RAN：Context CacheによってC-Planeの輻輳を防ぎ課題Aを解決、CN-CPとCN-UPをRANが持つことでTNの切断が起きてもサービス停止を防ぎ課題Bを解決。MEC Siteがインターネットへの疎通性があれば、TNが切断されてもインターネット抜け可能
            - CN-CP：既存のコアのC-Plane機能をCU-CPに統合
            - CN-UP：既存のコアのU-Plane機能をCU-UPに統合、実質的にI-UPFの役割を果たす。
            - Context Cache：CN-CPが参照するためのUEコンテキストをRANごとに単一のキャッシュとして管理。1000万台オーダーのUEに対してコンテキストを10万局オーダーのRANがそれぞれ管理することで、そもそも輻輳が起きにくい構成に
        - 要件充足性
            - AAA：TNが切断された場合でも、AD-RAN間で有効なContext Cacheがあれば、サービスを継続可能。課金処理ではCN-UPが定期的にCN-CPにレポートを送信し、CN-CPが課金情報を生成。新規接続の場合は加入者DBから取得したポリシーをAD-RANにキャッシュする or キャッシュされたデータをTN内に分散させる
            - モビリティ管理：ハンドオーバーはAD-RAN間で直接処理され、CN-CPは関与しない。Context Cacheがあれば、TNが切断されてもハンドオーバーを継続可能
            - ユーザープレーン：本質的にはU-Planeは正常なC-Plane処理の結果であるため、ADMobileにおけるU-planeの実現方法は限定されない。別の5GCをAD-RANに繋ぐ場合は、AD-RAN側のCN-UPがI-UPFとして機能する。SRv6 MUPを入れる場合は、CN-UPから出たパケットがBGPルータに処理される
        - 実装：UEはPythonで自作、gNBにOAI、CN-CPにProc5GCとFree5gc UDR、CN-UPにfree5GC UPFを使用
        - 評価
            - 自律分散性：AD-RANが相互接続されている限りUEは通信を維持できることを確認
            - スケーラビリティ：UEあたりのシグナリングメッセージ数とそれに伴うトラフィック量を測定し、アーキテクチャ間で比較。実際の通信シナリオでも試す
                - Regist, PDU Sess, HOの3つの手順で発生するメッセージ数を従来型・UPF分散型・ADMobileで比較
                    - 特にHOにおいて、ADMobileは中央へのシグナリング（Edge-Central）を完全にゼロにできるが、AD-RAN間（Edge-Edge）の通信が増加するというトレードオフがある
                - システム全体のスケーラビリティ: 都市部のマクロセル環境を想定し、歩行者（低速）と車両（高速）が混在するシナリオでシグナリング負荷を試算
                    - 10,000台のUEを想定した場合、ADMobileは従来型と比較して以下の成果を示しました。エッジの負荷増: $0.26 \times 10^3$ msg/s 増加（許容範囲内）。中央の負荷減: $0.12 \times 10^6$ msg/s 削減（劇的な削減）。


