\chapter{疎結合アーキテクチャの提案}
\label{chap:proposal}

本章では，第\ref{chap:background}章で述べた技術背景を踏まえ，4G RANを改修なしに5Gコア（5GC）へ収容可能にする疎結合アーキテクチャを提案する．まず第\ref{sec:proposal_theoretical_foundation}節で提案の理論的基盤を述べ，第\ref{sec:proposed_architecture}節で提案アーキテクチャの概要を示し，第\ref{sec:logical_design_challenges}節で論理設計上の主要課題を整理する．詳細設計，プロトタイプ実装，およびプロトコル仕様上の制約については，第\ref{chap:design}章で述べる．

\section{提案の理論的基盤}
\label{sec:proposal_theoretical_foundation}

本節では，提案手法の根拠となる2つの重要な分析フレーム---「機能的共通性」と「5GC固有機能の階層的分析」---を述べる．

\subsection{4G/5G間の機能的共通性}
\label{subsec:functional_commonality}

モバイル通信システムは世代ごとにプロトコル仕様が大きく異なるが，提供される基本的な制御機能の役割には高い共通性が存在する．Watanabeらは，AAA，Mobility Management，およびU-Planeといった本質的な機能要件は世代を超えて不変であると指摘し，C-PlaneとRANの機能を抽象化・融合した自律分散型アーキテクチャを提案している\cite{Watanabe_ADMobile2025}．しかし，具体的にどのプロトコルパラメータがこれらの共通機能に対応するかについては詳細な分析を行っていない．

本研究は，この観察を理論的基盤として採用する．ただし，Watanabeらの研究がコアネットワーク自体の自律分散型への移行を志向しているのに対し，本研究は既存の5GC標準アーキテクチャを維持したまま，RANとCNの間にプロトコル変換層を挿入するアプローチを取る．これにより，プロトコル形式の違いを乗り越え，「機能的な等価性」に基づく相互運用を実現する．

以下では，まずモバイルシステムの本質的機能要件を定義し，次に機能・ノード・プロトコル・状態・インタフェース・ライフサイクルの6つの観点から，EPSと5GSの対応関係を多角的に分析する．その上で，プロトコル変換によるインターワーキングの実現可能性を論じる．

\subsubsection{モバイルシステムの本質的機能要件}

モバイル通信は世代を超えて，本質的に以下の3つの機能を提供する必要がある．

\begin{enumerate}
\item \textbf{認証・認可・課金（AAA）}: ユーザの身元確認，サービス利用権限の検証，および通信料金の計算・請求を行う機能．通信事業者のビジネスモデルを成立させるために不可欠である．

\item \textbf{Mobility Management}: 端末が地理的に移動する際に，その現在位置を把握し，位置情報に基づいてシグナリングやデータをルーティングする機能．移動体通信の基本的価値である「場所を問わない接続性」の実現に不可欠である．

\item \textbf{U-Plane}: 実際のユーザデータ（音声，映像，パケットデータ等）をネットワークを介して転送する機能．通信サービスの本体であり，いかなる世代においても必須である．
\end{enumerate}

これら3つの機能は，個々の通信プロトコルやノード構成が異なるとしても，モバイルシステムの定義上避けられない基本要件である．4Gと5Gのプロトコル仕様は大きく異なるが，これらの機能要件そのものは変わらない．

\subsubsection{本質的機能の多角的分析}

本質的機能が普遍的である以上，その機能を実現するシステムの各側面にも対応関係が存在する．ここでは，モバイルシステムを分析するための6つの観点---\textbf{機能}，\textbf{ノード}，\textbf{プロトコル}，\textbf{状態}，\textbf{インターフェース}，\textbf{ライフサイクル}---からEPSと5GSの対応関係を整理する．

\paragraph{機能の対応}

前節で定義した3つの本質的機能（AAA・Mobility Management・U-Plane）は，EPSでも5GSでも必ず実装されている．表\ref{tab:function_terminology}に，各機能がEPSと5GSの仕様においてどのような用語で記述されているかを示す．

\begin{table}[htbp]
\centering
\caption{本質的機能に対応するEPS/5GSの仕様用語}
\label{tab:function_terminology}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証: EPS AKA\newline
認可: 加入者プロファイル参照\newline
課金: オンライン/オフライン &
認証: 5G AKA\newline
認可: 加入者プロファイル参照\newline
課金: オンライン/オフライン \\
\hline
Mobility\newline Management &
サブレイヤ: EMM\newline
初期接続: Attach\newline
切断: Detach\newline
エリア更新: TAU &
サブレイヤ: 5GMM\newline
初期接続: Registration\newline
切断: Deregistration\newline
エリア更新: Registration Update \\
\hline
U-Plane &
サブレイヤ: ESM\newline
セッション: EPS Bearer\newline
転送プロトコル: GTP-U &
サブレイヤ: 5GSM\newline
セッション: PDU Session\newline
転送プロトコル: GTP-U \\
\hline
\end{tabular}
\end{table}

以下に各機能の用語について解説する．

\begin{itemize}
  \item \textbf{AAA機能}: EPSではEPS AKA（Authentication and Key Agreement）で相互認証を行い，加入者プロファイルを参照して認可を行う．課金はオンラインまたはオフライン方式で実施される．5GSでは5G AKAが認証を担い，同様に加入者プロファイルを参照して認可を行う．課金方式も同様にオンライン/オフラインが存在する．
  \item \textbf{Mobility Management機能}: EPSではNASプロトコルの中にEMM（EPS Mobility Management）サブレイヤが定義されている．主要な手続きとして，初期接続時のAttach，切断時のDetach，エリア更新時のTAU（Tracking Area Update）がある．5GSでは5GMM（5GS Mobility Management）サブレイヤが同等の機能を提供し，AttachはRegistrationに，DetachはDeregistrationに，TAUはRegistration Updateにそれぞれ対応する．
  \item \textbf{U-Plane機能}: EPSではNASプロトコルの中にESM（EPS Session Management）サブレイヤが定義されている．セッションとしてEPS Bearerを確立し，転送プロトコルとしてGTP-Uを使用する．5GSでは5GSM（5GS Session Management）サブレイヤが同等の機能を提供し，EPS BearerはPDU Sessionに対応する．GTP-Uは両世代で共通であり，この点がU-Plane変換を比較的容易にしている．
\end{itemize}

\paragraph{ノードの対応}

3つの本質的機能がEPSと5GSにおいてどのノードによって実装されているかを表\ref{tab:essential_functions_mapping}に示す．

\begin{table}[htbp]
\centering
\caption{本質的機能要件とEPS/5GSにおける実装ノードの対応}
\label{tab:essential_functions_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証: HSS\newline
認可: HSS\newline
課金: PCRF/OCS &
認証: AUSF\newline
認可: UDM/UDR\newline
課金: CHF \\
\hline
Mobility\newline Management &
C-Plane制御: MME\newline
（セッション管理も兼務） &
C-Plane制御: AMF\newline
セッション管理: SMF \\
\hline
U-Plane &
データ転送: S-GW/P-GW\newline
（C-Plane処理も兼務） &
データ転送: UPF\newline
C-Plane制御: SMF \\
\hline
\end{tabular}
\end{table}

以下に各機能のノード対応について解説する．

\begin{itemize}
  \item \textbf{AAA機能}: EPSではHSSが認証・認可を統一的に管理し，課金はPCRF/OCSが担う．5GSではAUSFが認証を，UDM/UDRが認可（加入者プロファイル管理）を，CHFが課金をそれぞれ専任で担当する．
  \item \textbf{Mobility Management機能}: EPSではMMEがC-Plane制御とセッション管理を兼務する．5GSではAMFがC-Plane制御を専任し，セッション管理はSMFに分離されている．
  \item \textbf{U-Plane機能}: EPSではS-GW/P-GWがデータ転送とC-Plane処理を兼務する．5GSではUPFがデータ転送を専任し，C-Plane制御はSMFに分離されている．
\end{itemize}

このように，EPSでは1つのノードが複数機能を兼務していたのに対し，5GSでは各機能が独立したNFに分解されている．ノード構成は大きく異なるが，機能とノードの対応関係を明示すれば，変換時のルーティング先を決定できる．

\paragraph{プロトコルの対応}

各本質的機能を実現するプロトコル要素（パラメータ，手続き，符号化）にも対応関係が存在する．表\ref{tab:protocol_parameters}に本質的機能ごとの主要プロトコル要素の対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとの主要プロトコル要素のEPS/5GS対応}
\label{tab:protocol_parameters}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
パラメータ: IMSI, GUTI\newline
手続き: EPS AKA\newline
符号化: NAS独自形式 &
パラメータ: SUPI/SUCI, 5G-GUTI\newline
手続き: 5G AKA\newline
符号化: NAS独自形式 \\
\hline
Mobility\newline Management &
パラメータ: TAI\newline
手続き: Attach, TAU, Detach\newline
符号化: S1AP (ASN.1/PER) &
パラメータ: TAI（同一構造）\newline
手続き: Registration, Reg. Update, Dereg.\newline
符号化: NGAP (ASN.1/PER) \\
\hline
U-Plane &
パラメータ: APN, QCI, EBI\newline
手続き: Bearer確立/解放\newline
符号化: GTP-U &
パラメータ: DNN, 5QI, QFI\newline
手続き: PDU Session確立/解放\newline
符号化: GTP-U \\
\hline
\end{tabular}
\end{table}

以下に各機能のプロトコル対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: ユーザ識別子としてIMSI⇔SUPI/SUCI，一時識別子としてGUTI⇔5G-GUTIが対応する．認証手続きはEPS AKA⇔5G AKAが対応し，いずれもNAS層で独自形式により符号化される．
  \item \textbf{Mobility Management}: エリア識別子TAIは両者で同一構造である．手続きとしてAttach⇔Registration，TAU⇔Registration Update，Detach⇔Deregistrationが対応する．AS層はS1AP/NGAPともにASN.1/PERで符号化される．
  \item \textbf{U-Plane}: 接続先としてAPN⇔DNN，品質クラスとしてQCI⇔5QI，識別子としてEBI⇔QFIが対応する．セッション手続きとしてBearer確立⇔PDU Session確立が対応する．GTP-Uは両者で共通のため変換が容易である．
\end{itemize}

\paragraph{状態の対応}

モバイルシステムはステートマシンとして動作し，各本質的機能に対応する状態遷移を管理する．表\ref{tab:state_mapping}に本質的機能ごとの状態対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとの状態のEPS/5GS対応}
\label{tab:state_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証成功: EMM-REGISTEREDへ遷移\newline
認証失敗: 接続拒否 &
認証成功: 5GMM-REGISTEREDへ遷移\newline
認証失敗: 接続拒否 \\
\hline
Mobility\newline Management &
未登録: EMM-DEREGISTERED\newline
登録済: EMM-REGISTERED &
未登録: 5GMM-DEREGISTERED\newline
登録済: 5GMM-REGISTERED \\
\hline
U-Plane &
未接続: ECM-IDLE\newline
接続中: ECM-CONNECTED\newline
IDLE遷移: S1 Release\newline
CONNECTED遷移: Service Request &
未接続: CM-IDLE\newline
接続中: CM-CONNECTED\newline
IDLE遷移: N2 Release\newline
CONNECTED遷移: Service Request \\
\hline
\end{tabular}
\end{table}

以下に各機能の状態対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 認証結果に応じて状態が遷移する．認証成功時はEMM-REGISTERED/5GMM-REGISTEREDへ遷移し，失敗時は接続が拒否される．認証自体は独立したステートマシンを持たないが，Mobility Management状態の遷移条件として機能する．
  \item \textbf{Mobility Management}: EPSではEMM-DEREGISTERED/EMM-REGISTEREDで端末の登録有無を管理する．5GSでは5GMM-DEREGISTERED/5GMM-REGISTEREDが対応する．Attach完了⇔Registration完了で登録済み状態へ遷移する．
  \item \textbf{U-Plane}: EPSではECM-IDLE/ECM-CONNECTEDでセッション接続状態を管理する．5GSではCM-IDLE/CM-CONNECTEDが対応する．IDLEへの遷移はEPSではS1 Release，5GSではN2 Releaseによってトリガされ，CONNECTEDへの遷移は両者ともService Requestによってトリガされる．提案アーキテクチャは，これらの状態遷移を追跡し，4G側の状態を5G側にマッピングする必要がある．
\end{itemize}

\paragraph{インターフェース境界の対応}

各本質的機能が使用するインターフェース境界にも対応関係が存在する．表\ref{tab:interface_mapping}に本質的機能ごとのインターフェース対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとのインターフェースのEPS/5GS対応}
\label{tab:interface_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
RAN--CN: S1-MME（S1AP内NAS）\newline
CN内部: S6a（Diameter） &
RAN--CN: N2（NGAP内NAS）\newline
CN内部: SBI（HTTP/2） \\
\hline
Mobility\newline Management &
RAN--CN: S1-MME（S1AP）\newline
CN内部: S11（GTP-C） &
RAN--CN: N2（NGAP）\newline
CN内部: SBI（HTTP/2） \\
\hline
U-Plane &
RAN--CN: S1-U（GTP-U）\newline
CN--DN: SGi &
RAN--CN: N3（GTP-U）\newline
CN--DN: N6 \\
\hline
\end{tabular}
\end{table}

以下に各機能のインターフェース対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 認証情報はRAN--CN境界ではS1AP/NGAP内のNASメッセージとして搬送される．CN内部ではEPSがS6a（Diameter）でHSSと通信し，5GSはSBI（HTTP/2）でAUSF/UDMと通信する．
  \item \textbf{Mobility Management}: RAN--CN境界ではS1-MME（S1AP）⇔N2（NGAP）が対応する．両者ともASN.1/PERで符号化されるが，メッセージ構造が異なるため変換が必要である．
  \item \textbf{U-Plane}: RAN--CN境界ではS1-U⇔N3が対応し，両者ともGTP-Uを使用する．5GSでは拡張ヘッダ（PDU Session Container）が追加されている．本研究が対象とするのはRAN--CN境界（S1⇔N2/N3）の変換である．
\end{itemize}

\paragraph{ライフサイクルの対応}

端末のライフサイクルに沿って，各本質的機能が発動するタイミングにも対応関係が存在する．表\ref{tab:lifecycle_mapping}に本質的機能ごとのライフサイクル対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとのライフサイクル段階のEPS/5GS対応}
\label{tab:lifecycle_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
発動: 初期接続時\newline
手続き: EPS AKA &
発動: 初期接続時\newline
手続き: 5G AKA \\
\hline
Mobility\newline Management &
初期接続: Attach\newline
エリア移動: TAU/Handover\newline
切断: Detach &
初期接続: Registration\newline
エリア移動: Registration Update/Handover\newline
切断: Deregistration \\
\hline
U-Plane &
セッション確立: Bearer確立\newline
セッション変更: Bearer変更\newline
データ転送: GTP-U\newline
セッション解放: Bearer解放 &
セッション確立: PDU Session確立\newline
セッション変更: PDU Session変更\newline
データ転送: GTP-U\newline
セッション解放: PDU Session解放 \\
\hline
\end{tabular}
\end{table}

以下に各機能のライフサイクル対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 初期接続時に認証が発動する．EPSではEPS AKA，5GSでは5G AKAが対応する．認証成功後にMobility Managementの登録状態へ遷移する．
  \item \textbf{Mobility Management}: 初期接続（Attach⇔Registration），エリア移動（TAU/Handover⇔Registration Update/Handover），切断（Detach⇔Deregistration）の各段階で発動する．端末のライフサイクル全体を通じて状態を管理する．
  \item \textbf{U-Plane}: セッション確立（Bearer確立⇔PDU Session確立），セッション変更（Bearer変更⇔PDU Session変更），データ転送（GTP-U共通），セッション解放の各段階で発動する．GTP-Uが両者で共通であることがU-Plane変換を容易にしている．
\end{itemize}

\subsubsection{機能的共通性に基づくインターワーキングの実現可能性}

以上の多角的分析から，次の結論が得られる．

\begin{enumerate}
\item \textbf{本質的機能は普遍的である}: モバイルシステムが提供すべき「AAA」「Mobility Management」「U-Plane」という3つの機能要件は，4G（EPS）から5G（5GS）への進化を通じても変わらない．

\item \textbf{6つの観点すべてに対応関係が存在する}: 表\ref{tab:six_aspects_summary}に示す通り，機能・ノード・プロトコル・状態・インタフェース・ライフサイクルのいずれの観点においても，EPSと5GSの間に明確な対応関係が存在する．
\end{enumerate}

\begin{table}[htbp]
\centering
\caption{6つの観点におけるEPS/5GS対応関係の要約}
\label{tab:six_aspects_summary}
\begin{tabular}{|p{2.8cm}|p{4.5cm}|p{4.5cm}|p{2.5cm}|}
\hline
\textbf{観点} & \textbf{EPS (4G) 例} & \textbf{5GS (5G) 例} & \textbf{対応の性質} \\
\hline
機能 & AAA, Mobility Management, U-Plane & AAA, Mobility Management, U-Plane & 完全一致 \\
\hline
ノード & HSS, MME, S-GW/P-GW & AUSF/UDM, AMF/SMF, UPF & 1対多分解 \\
\hline
プロトコル & IMSI, Attach, GTP-U & SUPI, Registration, GTP-U & 意味的等価 \\
\hline
状態 & EMM/ECM状態 & 5GMM/CM状態 & 1対1対応 \\
\hline
インターフェース境界 & S1-MME, S1-U, SGi & N2, N3, N6 & 機能的等価 \\
\hline
ライフサイクル & Attach→Bearer→転送→Detach & Registration→PDU→転送→Dereg & 順序対応 \\
\hline
\end{tabular}
\end{table}

この包括的な対応関係の存在が，本研究で提案するプロトコル変換方式による4G RANの5GC収容を実現する理論的根拠となる．提案アーキテクチャは，これら6つの観点における対応関係を活用し，S1インターフェースの通信をN2/N3インターフェースの通信に変換する．具体的には，各本質的機能（AAA，Mobility Management，U-Plane）について，対応するノード，プロトコル，状態，インターフェース，ライフサイクル段階のマッピングルールを適用することで，4G端末からの通信を5GCが処理可能な形式に変換する．

\subsection{5GC固有機能の階層的分析}
\label{subsec:5gc_layered_design_framework}

前項では4Gと5Gで変わらない本質的機能を整理し，プロトコル変換による相互接続が原理的に可能であることを示した．しかし，5Gでは4Gにはなかった新たな機能も導入されている．具体的には，eMBB（enhanced Mobile Broadband：高速大容量），URLLC（Ultra-Reliable Low-Latency Communications：超高信頼・低遅延），mMTC（massive Machine Type Communications：多数同時接続）という3つの利用シナリオへの対応が求められており\cite{ITUR_M2083}，これらの多様な通信要件を単一のネットワーク上で同時に満たすため，5GCでは新たな制御メカニズムやサービス機能が追加された．

本項では，これらの5GC固有機能を体系的に整理するため，本研究独自の分析フレームワークとして「基盤層」「制御メカニズム層」「応用サービス層」の3層モデルを導入する．この分類は，5GCの技術的特徴を「アーキテクチャ構造」「通信制御ロジック」「提供サービス価値」という抽象度の異なる観点から捉え，提案アーキテクチャがレガシーRANと整合させるための対応方針を層ごとに異なる戦略で導出することを目的とする．この階層的分析により，すべての機能を一律に変換するのではなく，層の特性に応じて対応方法を使い分けるという分割統治のアプローチを明確にし，後述するアーキテクチャ要件「5GC固有機能の整合」の理論的根拠を与える．

図\ref{fig:5gc_layered_model}に5GC機能の階層モデルを示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.85, transform shape, every node/.style={font=\small}]
    % Layer 3: Service / Outcome
    \node[draw, rounded corners, fill=GoogleGreen!15, minimum width=14cm, minimum height=2.2cm] (layer3) at (0,5.4) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer3.north west) {3. 応用サービス層};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer3.north west) {
      \textbf{例}: Network Slicing，Network Exposure，5G LAN-type service\\
      \textbf{5GCの狙い}: 産業ごとの要求に応えるマルチテナンシーと異業種システム連携};

    % Layer 2: Mechanism
    \node[draw, rounded corners, fill=GoogleYellow!15, minimum width=14cm, minimum height=2.2cm] (layer2) at (0,2.7) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer2.north west) {2. 制御メカニズム層};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer2.north west) {
      \textbf{例}: Slice Selection，QoS Flow制御，Multi-type PDU\\
      \textbf{5GCの狙い}: 多様な通信要件が混在するトラフィックの識別と高精細な制御};

    % Layer 1: Infrastructure
    \node[draw, rounded corners, fill=GoogleBlue!15, minimum width=14cm, minimum height=2.2cm] (layer1) at (0,0) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer1.north west) {1. 基盤層};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer1.north west) {
      \textbf{例}: SBA，CUPS，NFV\\
      \textbf{5GCの狙い}: クラウドネイティブによる柔軟性と迅速化};

    % Connections
    \draw[->, thick, gray] (layer1.north) -- (layer2.south);
    \draw[->, thick, gray] (layer2.north) -- (layer3.south);

  \end{tikzpicture}
  \caption{5GC機能の3層構造モデル}
  \label{fig:5gc_layered_model}
\end{figure}

\subsubsection{基盤層：CNアーキテクチャの構造的刷新}

基盤層は，CNアーキテクチャの構造的刷新を指す．従来のハードウェア依存型から脱却した，\textbf{分散・分離型アーキテクチャ構造}への転換を特性とする．代表的な技術としてSBA，CUPS，NFVが挙げられ，5GCのアーキテクチャが刷新された主な狙いはクラウドネイティブ化による柔軟性と迅速化である．4G時代の専用ハードウェアによる設計では年単位の改修サイクルを要したが，これらの基盤技術はソフトウェア中心の設計・運用により，機能の追加・更新・スケーリングを迅速に行え，新しいサービス提供を短期間で実現できる．

\paragraph{対応方針：隠蔽}

提案アーキテクチャは，この層を完全に隠蔽する．理由は以下の通りである．

\begin{itemize}
\item \textbf{CN内部事情：} SBAやCUPSは，5GC内部のノード間インターフェース（SBI等）に関する要件であり，RAN側からは見える必要がない．
\item \textbf{透過性の維持：} 提案アーキテクチャがS1インターフェースをN2/N3インターフェースに変換する際，5GC内部のこれらの技術選択には一切影響を受けない．
\item \textbf{疎結合化の根拠：} 結果として，eNBは5GCの内部実装の詳細を知ることなく通信可能となり，「双方の改修不要」という疎結合の本質が実現される．
\end{itemize}

\paragraph{実装上の位置づけ}

提案アーキテクチャは，eNBに対してはMMEとして，5GCに対してはgNBとして振る舞い，S1インターフェースとN2/N3インターフェースを終端する．5GC内部のSBI通信は提案アーキテクチャから見えず，標準のN2/N3インターフェースのみを考慮すればよい．したがって，CN側の内部実装（SBA構成等）が変わっても，提案アーキテクチャの変換ルールに影響しない．

\subsubsection{制御メカニズム層：通信制御の高度化}

制御メカニズム層は，基盤上でトラフィックを識別・制御し，要件に応じた論理的な処理を行う\textbf{通信制御メカニズム}を指す．5Gにおける通信制御の高度化とは，多様な通信要件が混在するトラフィックを識別し，フロー単位で細粒度に制御することが可能になったことを意味する．4GはQoSクラス（QCI）による優先度管理を備えていたものの，ベアラ単位の粗粒度な制御に留まっていた．5Gでは自動運転（超低遅延），4K動画（大容量），IoT（多数同時接続）といった互いに相反する要件が同一ネットワーク上で混在するため，フロー単位での細粒度制御が不可欠となった．

\paragraph{4G/5G間の本質的ギャップ}

4Gと5Gの制御メカニズムの主要な違いを表\ref{tab:mechanism_gap}に示す．

\begin{table}[htbp]
\centering
\caption{4G/5Gにおける制御メカニズムのギャップ}
\label{tab:mechanism_gap}
\begin{tabular}{|p{3cm}|p{5cm}|p{5cm}|}
\hline
\textbf{制御項目} & \textbf{4G (EPS)} & \textbf{5G (5GS)} \\
\hline
QoS管理単位 & ベアラ単位（粗粒度） & フロー単位（細粒度） \\
\hline
QoSクラス識別子 & QCI（1--9の9段階） & 5QI（標準化/非標準化値） \\
\hline
スライス識別 & なし（単一ネットワーク） & S-NSSAI（複数スライス）で識別 \\
\hline
UL/DL制御 & 対称的に管理 & ULCL等で非対称制御が可能 \\
\hline
トラフィック分類 & TFT（Traffic Flow Template）で固定的に分類 & SDF（Service Data Flow）で動的分類 \\
\hline
\end{tabular}
\end{table}

これらのギャップは，4G eNBが5GC固有機能を理解・実行できないことを意味する．ここが本研究の主たる技術的解決領域となる．

\paragraph{対応方針：変換・整合}

提案アーキテクチャは，このメカニズム層に限定して積極的な変換・マッピングを行う．以下に，論理設計として対応が求められる主要な制御メカニズムと，提案アーキテクチャの役割を述べる．なお，実装上の対応範囲と制約については第\ref{chap:design}章で述べる．

\begin{enumerate}
\item \textbf{Network Slice Selection（スライス選択メカニズム）}

用途に応じた仮想ネットワークを動的に選択し，複数のスライスを並行運用する仕組みである．5GCではS-NSSAI（Single Network Slice Selection Assistance Information）を用いてスライスを識別し，NSSF（Network Slice Selection Function）がスライスに対応したNF群（AMF，UPF等）を選択する．

  \begin{itemize}
  \item \textbf{4G側の制約}: eNBはS-NSSAIの概念を持たず，複数スライスを区別して接続を適切に振り分ける機能に対応していない
  \item \textbf{提案アーキテクチャの役割}: 4G eNBが送信してくる接続情報（APN等）を受取り，5GCへの登録時にS-NSSAI情報を付与し，異なるスライスを指定できる構成を実現する
  \item \textbf{設計上の実現指針}: CN側では論理的に複数スライスを判別管理でき，スライスごとのポリシーを独立的に適用できることを目指す
  \item \textbf{論理設計}: APNからS-NSSAIへのマッピングは以下のルールに基づく．変換器はAPN文字列をキーとしてS-NSSAI（SST/SD）を決定するマッピングテーブルを保持する．デフォルトAPN（例：internet）はデフォルトスライス（SST=1，eMBB相当）にマッピングし，特定用途APN（例：ims）は対応するスライス（例：SST=2，URLLC相当）にマッピングする．マッピングテーブルに存在しないAPNについては，デフォルトスライスへフォールバックする
  \end{itemize}

\item \textbf{Flow-based QoS Framework（フローベースQoS制御）}

QoSフロー（QFIで識別）を単位とした細粒度QoS制御を実現する仕組みである．5GCではトラフィック分類（Traffic Classification）を基盤とし，SDF（Service Data Flow）により動的に分類される．SDFは各パケットのヘッダ情報（送受信IP，ポート，プロトコル等）を検査し，ポリシーに基づいてQFI（および5QI）を動的に割り当てる．

  \begin{itemize}
  \item \textbf{4G側の制約}: EPSはEPS Bearer単位の粗粒度QoS管理であり，TFT（Traffic Flow Template）による固定的な分類と，QCI（QoS Class Identifier，1--9段階）による制御に限定される
  \item \textbf{提案アーキテクチャの役割}: QCI（4G）$\leftrightarrow$ 5QI（5G）のマッピングを行い，eNBからのBearer確立要求をFlow情報に変換する
  \item \textbf{設計上の実現指針}: CN側では論理的なQoS優先度管理が可能となり，異なるアプリケーション群に異なる品質を提供できることを目指す
  \item \textbf{論理設計}: QCI/5QI/QFIの変換は以下のルールに基づく．標準仕様においてQCIと5QIが1対1対応する範囲（1〜9）では同一値としてマッピングし，それ以外はデフォルト値（QCI=9，5QI=9）へフォールバックする．5GCから通知されるQFIはUEコンテキストに保持し，応答メッセージに含めることでプロトコル上の整合性を維持する．Bearer単位からFlow単位への粒度変換については，1 Bearer = 1 QoS Flowの対応関係を基本とする
  \end{itemize}

\item \textbf{Traffic Steering（トラフィックステアリング）}

特定のトラフィックをエッジやローカルネットワークへ分岐させ，異なる経路を提供する経路制御の仕組みである．5GCではULCL（Uplink Classifier）等の技術により，MEC（Multi-access Edge Computing）やローカルブレイクアウトへの低遅延パスを実現する．

  \begin{itemize}
  \item \textbf{4G側の制約}: EPSでは経路制御が限定的であり，ULCLのような動的なステアリング機能に対応していない
  \item \textbf{提案アーキテクチャの役割}: 5GCが指示するステアリングルールを受取り，GTP-Uパケットの経路を柔軟に制御する
  \item \textbf{設計上の実現指針}: CN側からの経路指示に従い，特定トラフィックを異なる経路へ振り分けられることを目指す
  \item \textbf{論理設計}: 5GCからPDU Session確立応答で通知されるUPF情報（N3 TEID，UPF IPアドレス）に基づき，変換器がGTP-Uパケットの転送先を決定する．複数UPFへの分岐が指示された場合は，パケットフィルタ（送信元/宛先IP，ポート等）に基づいて転送先UPFを選択するマッピングテーブルを変換器が管理する．ただし，4G RANはULCLを認識しないため，分岐の透過性は変換器が担保する
  \end{itemize}

\item \textbf{Multi-type PDU Support（異種プロトコル対応）}

従来のIPに加え，EthernetやUnstructured（非構造化データ）タイプをサポートし，産業用プロトコルを透過させる仕組みである．5GCではPDU Session確立時にPDU Typeを指定でき，産業用機器の多様な通信要件に対応する．

  \begin{itemize}
  \item \textbf{4G側の制約}: EPSはIPベースのPDN接続のみをサポートし，Ethernetフレームや産業用プロトコルの透過的伝送ができない
  \item \textbf{提案アーキテクチャの役割}: 4G RANからのIP通信を5GCのPDU Session（Type: IPv4/IPv6）にマッピングする
  \item \textbf{設計上の実現指針}: 4G RANからの接続はIPベースに限定されるが，CN側では適切なPDU Typeを選択し，5GCの機能を最大限活用できることを目指す
  \item \textbf{論理設計}: PDU Session確立時のPDU Type選択は以下のルールに基づく．4G UEからのPDN Type（IPv4，IPv6，IPv4v6）をそのまま5GCのPDU Type（IPv4，IPv6，IPv4v6）にマッピングする．EthernetおよびUnstructuredタイプは4G RANで生成されないため，変換器では対象外とする．PDN Type未指定時はIPv4をデフォルトとして選択する
  \end{itemize}
\end{enumerate}

\subsubsection{応用サービス層：最終的なサービス価値}

応用サービス層は，基盤層と制御メカニズム層を通じて実現される最終的なサービス価値を指す．本層が求められる根本的な理由は，製造業・医療・交通・エネルギーといった多様な産業からの相反する通信要求——例えば「超高速」と「超省電力」の同時実現——を，単一のネットワークインフラで効率的に満たしたいという事業要求にある．

\paragraph{本層の事業要求}

5Gの本質的なビジネス転換は，\textbf{産業ごとの要求に応えるマルチテナンシーと異業種システム連携}である．4G時代は通信キャリアの顧客が基本的に一般消費者向け（スマートフォン利用者）であったが，5Gでは産業ユースケース（工場，病院，電力会社等）へ市場が拡大し，各産業のシステムと統合する必要が生じた．この層に求められる核心的な事業要求は以下の2点である：

\begin{itemize}
\item \textbf{マルチテナンシー}: 一つのインフラを，複数の用途・事業者・産業で効率的に共有し，それぞれに対して異なるサービス品質を提供する仕組み．
\item \textbf{異業種連携}: 従来の通信事業者の枠を越えて，他産業のシステムと統合され，新しいビジネスモデルと価値を創出できる開放性．
\end{itemize}

\paragraph{実現技術}

前述の要件を実現するため，5GCは以下に述べる重要機能群を採用する．

\subparagraph{A. Network Slicing（eMBB/URLLC/mMTCの提供）}

\textbf{Network Slicing}は，マルチテナンシー実現の核である．この機能により，本項の冒頭で述べた3つの利用シナリオ（eMBB，URLLC，mMTC）を，論理的に独立した仮想ネットワークとして提供する：

\begin{itemize}
\item \textbf{eMBB}: 映像伝送，AR/VR等の帯域重視型サービス
\item \textbf{URLLC}: 自動運転，ファクトリーオートメーション等の遅延・信頼性重視型サービス
\item \textbf{mMTC}: スマートシティ，農業センサー等の接続数・効率重視型サービス
\end{itemize}

これらはNetwork Slicingが提供するサービス属性（スライス特性）であり，複数のサービス分類を同一のインフラリソースで共存させ，設備の効率的な利用を実現する．

\subparagraph{B. Industrial Integration（産業連携：5G LAN / NPN）}

\textbf{5G LAN-type Service}および\textbf{Non-Public Network (NPN)}は，産業用イーサネット機器を5Gネットワークに直接収容するプロトコルレベルの連携機能である．工場や企業プライベートネットワークの産業用機器を，通信インフラの一部として統合する．これは前述の制御メカニズム層におけるMulti-type PDU Support（Ethernetタイプ等）と連携して実現される．

\subparagraph{C. Network Exposure（APIによる外部連携）}

\textbf{Network Exposure}は，IT企業のシステムが5GネットワークのQoSやIoTデバイスの起動をAPI経由で制御できる連携機能である（NEF: Network Exposure Functionがこの機能を担うノード）．異業種システムとの統合の窓口となり，異業種連携（Vertical Integration）を実現する．

\subparagraph{D. Edge Computing Support（性能拡張）}

\textbf{Edge Computing Support}およびMEC（Multi-access Edge Computing）は，5GCの機能をエッジ拠点に分散配置し，UPFやアプリケーション処理を最適化する技術である．特にURLLCが定義する「低遅延」やeMBBが定義する「大容量処理」の実効性能を，物理的な距離短縮により担保する．

\paragraph{5GC先行移行の動機}

事業者が5GC導入を急ぐ理由は，基本的な接続性ではなく，上記の2つの要件（マルチテナンシー，異業種連携）を実現する実現技術（Network Slicing，Network Exposure，5G LAN，MEC等）が，Vertical毎の相反する要求を単一インフラで満たし，新しいビジネスモデルと価値を創出するからである．

\paragraph{対応方針：基盤層・メカニズム層の解決による透過的提供}

4G eNBを使い続ける場合でも，基盤層の隠蔽とメカニズム層での変換・マッピングにより，応用サービス層の機能を以下のように提供できる．

\subparagraph{制御メカニズム層の整合により提供可能なサービス}

\begin{itemize}
\item \textbf{Network Slicing}: Network Slice Selectionにより，APNからS-NSSAIへのマッピングを行い，CN側で複数スライスを論理的に運用可能となる．これにより，異なるサービス分類の課金分離，優先度制御，Vertical毎の隔離が実現できる．
\item \textbf{スライス内QoS差別化}: Flow-based QoS Frameworkにより，QCI/5QIのマッピングを通じてスライス内でのQoS制御（eMBB向け大容量，URLLC向け低遅延等）を提供できる．
\item \textbf{Edge Computing / MEC}: Traffic Steeringにより，5GCが指示する経路制御ルールに従ってトラフィックをエッジUPFへ振り分け，低遅延処理を実現できる．
\end{itemize}

\subparagraph{基盤層の隠蔽により自動的に利用可能なサービス}

\begin{itemize}
\item \textbf{Network Exposure}: NEFはCN内部のAPI機能であり，RAN-CN間の変換とは独立して動作する．提案アーキテクチャが基盤層を隠蔽することで，5GC側のNetwork Exposure機能はそのまま利用可能となる．
\end{itemize}

\subparagraph{4G RANの本質的制約により限定的なサービス}

\begin{itemize}
\item \textbf{5G LAN / NPN}: 5G LAN-type Serviceは Ethernet PDUタイプを前提とするが，4G RANはIPベースのPDN接続のみをサポートする．したがって，Ethernetフレームの透過的伝送を必要とする産業用プロトコル統合は，4G RAN経由では実現できない．ただし，IP over Ethernetの形式であれば，Multi-type PDU SupportのIPv4/IPv6マッピングを通じて部分的に対応可能である．
\end{itemize}

本システムは，レガシーRAN環境下においても，提案アーキテクチャの制御メカニズム層を通じて，Network SlicingとEdge Computingを中心としたサービス価値を提供し，Verticalの要求に応える．

\subsubsection{階層的分析に基づくインターワーキングの実現可能性}

以上の3層分析から，次の結論が得られる．

\begin{enumerate}
\item \textbf{基盤層は隠蔽で対応可能}: SBAやCUPSなどのCN内部実装は，提案アーキテクチャが完全に隠蔽することで，RANへの影響をゼロにできる．また，Network Exposure等のCN内部API機能は，この隠蔽により自動的に利用可能となる．

\item \textbf{制御メカニズム層は変換・整合が必要}: Flow-based QoS等は4G RANとの間で実際の通信制御として交わされるため隠蔽できず，積極的な変換・マッピングが必要である．

\item \textbf{応用サービス層は段階的に提供可能}: Network SlicingやEdge Computingは制御メカニズム層の整合により提供可能となる．ただし，5G LAN/NPNのようにEthernet PDUタイプを前提とする機能は，4G RANの本質的制約（IPベースのみ）により限定的となる．
\end{enumerate}

表\ref{tab:3layer_strategy}に，3層に対する提案アーキテクチャの対応方針を整理する．

\begin{table}[htbp]
\centering
\caption{5GC機能3層と提案アーキテクチャの対応方針}
\label{tab:3layer_strategy}
\begin{tabular}{|p{2.2cm}|p{3.5cm}|p{3cm}|p{3.5cm}|}
\hline
\textbf{層} & \textbf{機能例} & \textbf{対応方針} & \textbf{論文での位置づけ} \\
\hline
基盤層 & SBA, CUPS, NFV, Network Exposure & 隠蔽（Abstraction） & 疎結合性の根拠 \\
\hline
制御メカニズム層 & Slice Selection, QoS Flow, Traffic Steering, Multi-type PDU & 変換・整合（Interworking） & \textbf{主たる技術的貢献} \\
\hline
応用サービス層 & Network Slicing, Edge Computing & 提供可能（Enablement） & 導入動機・価値 \\
\hline
& 5G LAN / NPN & 4G RAN制約により限定的 & 将来課題 \\
\hline
\end{tabular}
\end{table}

この階層的分析により，すべての機能を一律に変換するのではなく，層の特性に応じて対応方法を使い分けるという分割統治のアプローチが明確になった．制御メカニズム層での変換・整合が本研究の主たる技術的解決領域である．

\section{提案アーキテクチャ}
\label{sec:proposed_architecture}

本節では，前節の理論的基盤に基づき，既存の4G RAN（eNB）を無改修で5GCに収容するための疎結合アーキテクチャを提案する．

第\ref{chap:related}章で分析した通り，既存の標準技術（N26，ng-eNB，N3IWF）はいずれもRAN改修またはUE改修を前提とし，学術研究によるアプローチ（5G-Flow，Hybrid 5G Core）もRAN改修やEPC残存という制約を持つ．本研究では，これらの課題を克服するため，プロトコル変換による疎結合方式を採用する．

\subsection{アーキテクチャ要件}
\label{subsec:architecture_requirements}

第\ref{chap:background}章で設定した研究目的「既存の4G RAN（eNB）を改修せずに5GCへ収容し，CN先行移行を可能にする」を実現するため，提案アーキテクチャは以下の要件を満たす必要がある．

\begin{enumerate}
    \item[\textbf{R1}] \textbf{透過的介在}: 既存RANと5GCの間に透過的に介在し，双方のインターフェース（S1およびN2/N3）を終端すること．これにより，eNBは従来通りS1インターフェースで，5GCは標準のN2/N3インターフェースでそれぞれ通信でき，相互に改修を要求しない．
    \item[\textbf{R2}] \textbf{プロトコル相互変換}: 第\ref{subsec:functional_commonality}項で示した世代不変な本質的機能（AAA，Mobility Management，U-Plane）を維持しつつ，プロトコルを相互変換すること．これにより，第\ref{chap:background}章で明らかにしたプロトコル障壁（S1AP↔NGAP，NAS，GTP-U）を解消する．
    \item[\textbf{R3}] \textbf{5GC固有機能の整合}: 5GC固有の機能を4G RANの制約下で整合させること．これにより，ネットワークスライシング等の拡張機能への対応が，アーキテクチャの根本的な変更なしに可能となる．
\end{enumerate}

以下では，これらの要件を満たすアーキテクチャの概念，設計方針，および構成について述べる．

\subsection{疎結合アーキテクチャの概念}
\label{subsec:loosely_coupled_concept}

第\ref{chap:background}章で設定した研究目的で述べた「疎結合アーキテクチャ」とは，4G RANと5GCの間に変換ゲートウェイを配置し，双方のシステムを独立に保ちながら相互接続を実現する構成を指す．この設計思想の本質は以下の3点にある．

\begin{enumerate}
\item \textbf{既存システムの独立性保持}: 4G RAN（eNB）は従来通りS1インタフェースで通信し，5GC（AMF/UPF）は標準のN2/N3インタフェースで通信する．双方とも相手側の存在を意識する必要がない．
\item \textbf{変換責務の局所化}: プロトコル変換のすべての複雑性をゲートウェイに集約する．これにより，既存機器のソフトウェア改修が不要となり，導入・運用コストを最小化できる．
\item \textbf{段階的移行の実現}: 4G RANと5GCが疎結合であるため，ネットワーク全体を一度に更新する必要がなく，基地局単位での段階的な5G移行が可能となる．
\end{enumerate}

本アーキテクチャは，\ref{subsec:functional_commonality}項で示した本質的機能の対応関係を活用し，S1インターフェースの通信をN2/N3インターフェースの通信に変換する「S1-N2変換ゲートウェイ」として具現化される．以下では，その設計方針，全体構成，およびプロトコルスタックについて述べる．

\subsection{設計方針：トランスレータ方式の採用}
\label{subsec:design_policy}

異種ネットワーク間の接続技術には，大別して2つのアプローチが存在する．一つはパケットヘッダを書き換える「トランスレータ方式」（例：IPv6とIPv4の相互変換を行うNAT64\cite{rfc6146}）であり，もう一つはパケットを別のプロトコルで包む「カプセル化方式」（例：IPパケットを別のIPパケットでトンネリングするIP-in-IP\cite{rfc2003}）である．第\ref{chap:related}章で述べた標準技術のうち，N26やng-eNBは両システム間でシグナリングを直接交換する密結合方式であり，N3IWFはIPsecトンネルによるカプセル化を用いる．

本研究では，トランスレータ方式を採用する．その理由は以下の2点である．第一に，カプセル化方式では5GC側でデカプセル化処理が必要となり，5GCを無改修で利用するという本研究の要件に反する．第二に，トランスレータ方式は疎結合アーキテクチャの原則---双方のシステムが相手側のプロトコルを意識しない---と整合する．ゲートウェイがプロトコル変換を完結させることで，eNBは従来通りS1インタフェースで，5GCは標準のN2/N3インタフェースでそれぞれ通信できる．

具体的には，C-Plane（制御面）においてS1-APメッセージとNG-APメッセージの意味的な対応関係に基づいて相互に変換を行う．変換の対象にはAS層（S1-AP/NG-AP）だけでなくNAS層も含まれ，一貫したトランスレータ方式を採用する．変換処理の詳細は第\ref{chap:design}章で述べる．

\subsection{全体構成}
\label{subsec:overall_structure}

提案システムの全体構成を図\ref{fig:architecture}に示す．ゲートウェイは4G eNBと5Gコア（AMF/\allowbreak UPF）の間に配置され，以下の2つのプレーンで変換を行う．

\begin{itemize}
\item \textbf{C-Plane（制御面）}: S1AP（S1-MME）とNGAP（N2）の相互変換．SCTP（Stream Control Transmission Protocol）接続を終端し，メッセージ単位で変換を行う．
\item \textbf{U-Plane（ユーザ面）}: GTP-Uパケットのヘッダ書き換え（TEIDマッピング）およびQoSパラメータ（QCI/5QI）の変換を行う．GTP-Uプロトコル自体は4G/5Gで共通（GTPv1-U）だが，TEID（Tunnel Endpoint Identifier）はC-Planeのセッション確立時に各ノードが独立に割り当てるため，eNBが期待するTEIDと5GCが割り当てたTEIDの対応関係をゲートウェイが管理・変換する必要がある．
\end{itemize}

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.9, every node/.style={font=\small}]
    % Nodes
    \node[draw, rounded corners, fill=GoogleBlue!20, minimum width=2cm, minimum height=1cm] (enb) at (0,0) {4G eNB};
    \node[draw, rounded corners, fill=GoogleGreen!20, minimum width=2cm, minimum height=1cm] (core) at (10,0) {5Gコア};

    % Converter Box
    \node[draw, thick, fill=GoogleYellow!10, minimum width=4.5cm, minimum height=2.5cm, dashed] (conv) at (5,0) {};
    \node[above] at (5,1.3) {\textbf{変換ゲートウェイ}};

    % Internal Logic
    \node[draw, fill=white, minimum width=1.2cm] (s1) at (3.8,0) {S1};
    \node[draw, fill=white, minimum width=1.2cm] (ng) at (6.2,0) {NG};
    \draw[<->, thick] (s1) -- (ng) node[midway, above] {\scriptsize 変換};

    % Connections
    \draw[<->, thick] (enb) -- (s1) node[midway, above] {S1};
    \draw[<->, thick] (ng) -- (core) node[midway, above] {N2/N3};

    % Roles
    \node[below=0.2cm of enb, align=center, font=\scriptsize] {MMEとして\\接続};
    \node[below=0.2cm of core, align=center, font=\scriptsize] {gNBとして\\接続};
    \node[below=1.4cm of conv, align=center, font=\scriptsize] {eNBに対してはMME，\\5GCに対してはgNBとして動作};

    % 凡例（下部中央）
    \node[draw, fill=white, anchor=north, font=\scriptsize, inner sep=4pt] at ([yshift=-8pt]current bounding box.south) {
      \begin{tabular}{@{}l@{\hspace{1em}}l@{\hspace{1em}}l@{\hspace{1em}}l@{}}
        \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!20] (0,0) rectangle (0.18,0.18);\ 4G RAN側 &
        \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleGreen!20] (0,0) rectangle (0.18,0.18);\ 5GC側 &
        \csname tikz\endcsname[baseline=-0.6ex]\draw[dashed, thick] (0,0) rectangle (0.18,0.18);\ 変換GW領域 &
        \csname tikz\endcsname[baseline=-0.6ex]\draw[thick] (0,0) -- (0.5,0);\ インタフェース
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{提案システムの全体構成（S1-N2変換ゲートウェイ）}
  \label{fig:architecture}
\end{figure}

\subsection{プロトコルスタック}
\label{subsec:protocol_stack}

ゲートウェイは，4G側と5G側で独立したプロトコルスタックを持つ（図\ref{fig:protocol_stack}）．
\begin{itemize}
\item \textbf{C-Plane（4G側）}: S1AP / SCTP / IP (eNB向け，ポート36412)
\item \textbf{C-Plane（5G側）}: NGAP / SCTP / IP (AMF向け，ポート38412)
\item \textbf{U-Plane}: GTP-U / UDP / IP (ポート2152)．eNB-UPF間のユーザデータを中継する．
\end{itemize}
なお，各ポート番号は環境変数により設定可能であり，上記は標準仕様で定められたデフォルトポート番号である．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.75, every node/.style={font=\small}]
    % eNB側
    \node[font=\normalsize\bfseries] at (1.5,5.5) {eNB};
    \node[draw, fill=GoogleBlue!20, minimum width=2.5cm, minimum height=0.6cm] at (1.5,4.5) {S1AP};
    \node[draw, fill=GoogleBlue!20, minimum width=2.5cm, minimum height=0.6cm] at (1.5,3.8) {SCTP};
    \node[draw, fill=GoogleBlue!20, minimum width=2.5cm, minimum height=0.6cm] at (1.5,3.1) {IP};

    % GTP-U (eNB側)
    \node[draw, fill=GoogleBlue!20, minimum width=2.5cm, minimum height=0.6cm] at (1.5,1.8) {GTP-U};
    \node[draw, fill=GoogleBlue!20, minimum width=2.5cm, minimum height=0.6cm] at (1.5,1.1) {UDP/IP};

    % Converter左側（S1終端）
    \node[font=\normalsize\bfseries] at (5.5,5.5) {変換GW};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] (gw_s1ap) at (5.5,4.5) {S1AP};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (5.5,3.8) {SCTP:36412};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (5.5,3.1) {IP};

    % 変換の注釈（C-Plane）
    \node[font=\scriptsize, align=center] at (7.5,4.5) {$\Longleftrightarrow$\\[-2pt]{\tiny 変換}};

    % Converter右側（NG終端）
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] (gw_ngap) at (9.5,4.5) {NGAP};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (9.5,3.8) {SCTP:38412};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (9.5,3.1) {IP};

    % GTP Proxy（U-Plane）
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (7.5,1.8) {GTP-U};
    \node[draw, fill=GoogleYellow!20, minimum width=2.5cm, minimum height=0.6cm] at (7.5,1.1) {UDP/IP};
    \node[font=\scriptsize] at (7.5,2.3) {(TEID書換)};

    % AMF側
    \node[font=\normalsize\bfseries] at (13,5.5) {AMF};
    \node[draw, fill=GoogleGreen!20, minimum width=2.5cm, minimum height=0.6cm] at (13,4.5) {NGAP};
    \node[draw, fill=GoogleGreen!20, minimum width=2.5cm, minimum height=0.6cm] at (13,3.8) {SCTP};
    \node[draw, fill=GoogleGreen!20, minimum width=2.5cm, minimum height=0.6cm] at (13,3.1) {IP};

    % UPF側
    \node[font=\normalsize\bfseries] at (13,1.8) {UPF};
    \node[draw, fill=GoogleGreen!20, minimum width=2.5cm, minimum height=0.6cm] at (13,0.8) {GTP-U};
    \node[draw, fill=GoogleGreen!20, minimum width=2.5cm, minimum height=0.6cm] at (13,0.1) {UDP/IP};

    % 接続線 C-Plane
    \draw[<->, thick] (2.75,4.5) -- (4.25,4.5);
    \draw[<->, thick] (10.75,4.5) -- (11.75,4.5);

    % 接続線 U-Plane
    \draw[<->, thick, dashed] (2.75,1.8) -- (6.25,1.8);
    \draw[<->, thick, dashed] (8.75,1.8) -- (11.75,0.8);

    % ラベル
    \node[font=\scriptsize] at (3.5,4.8) {C-Plane};
    \node[font=\scriptsize] at (4.5,2.1) {U-Plane};

    % 凡例（下部中央）
    \node[draw, fill=white, anchor=north, font=\scriptsize, inner sep=4pt] at ([yshift=-8pt]current bounding box.south) {
      \begin{tabular}{@{}l@{\hspace{1em}}l@{\hspace{1em}}l@{\hspace{1em}}l@{}}
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!20] (0,0) rectangle (0.18,0.18);\ eNB側 &
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleGreen!20] (0,0) rectangle (0.18,0.18);\ 5GC側 &
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleYellow!20] (0,0) rectangle (0.18,0.18);\ 変換GW &
      \csname tikz\endcsname[baseline=-0.6ex]\draw[thick, dashed] (0,0) -- (0.5,0);\ U-Plane
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{ゲートウェイのプロトコルスタック}
  \label{fig:protocol_stack}
\end{figure}
ゲートウェイは，C-Planeではトランスポート層（SCTP）を終端してシグナリングプロトコル（S1AP/NGAP）間でメッセージ変換を行い，U-PlaneではGTP-Uのヘッダ情報（TEID）を書き換えて転送する．これにより，eNBとAMF/UPFは互いに相手側のプロトコルを意識することなく通信できる．

\section{論理設計上の主要課題}
\label{sec:logical_design_challenges}

第\ref{subsec:functional_commonality}項および第\ref{subsec:architecture_requirements}項の分析に基づき，本提案アーキテクチャを実現する上で解決すべき主要な課題を整理する．

\subsection{手順タイミングの不一致}
4GのAttach手順ではDefault Bearer（データ経路）の確立がAttach手順の一部として同時に行われるのに対し，5GではRegistration完了後にPDU Session確立が別手順として実行される．この手順分離により，4G eNBがInitial Context Setup Responseで返却すべきS1-U TEID（GTP-Uトンネル端点ID）が，5GC側の応答（DownlinkNASTransport等）には含まれないという問題が生じる．

\subsection{認証・鍵管理の分離}
第\ref{subsec:functional_commonality}項で述べたように，認証・認可機能は世代不変であるが，4G（EPS-AKA）と5G（5G-AKA）では認証ベクトルの形式および鍵導出階層が異なる．標準のN26インターフェースを用いない構成では，変換器が認証処理を代行するためにUEの秘密情報（Ki, OPc）へのアクセス手段が必要となる．

\subsection{QoSモデルの差異}
4GのBearer単位QoS（QCI）と5GのFlow単位QoS（5QI）は，数値範囲や付随パラメータが完全には一致しない．特に5GのGFBR/MFBRから4GのGBR/MBRへの完全なマッピングは困難であり，互換性のある範囲での簡易変換が現実的な解となる．

\section{本章のまとめ}

本章では，4G RANを改修なしに5GCへ収容するための疎結合アーキテクチャを提案した．第\ref{sec:proposal_theoretical_foundation}節で理論的基盤を，第\ref{sec:proposed_architecture}節でアーキテクチャ概要を，第\ref{sec:logical_design_challenges}節で論理設計上の主要課題を述べた．主要な貢献を以下に要約する．

\begin{itemize}
    \item \textbf{理論的基盤の確立}（第\ref{sec:proposal_theoretical_foundation}節）: 提案手法の根拠として2つの分析を行った．(1) 機能的共通性分析では，モバイルシステムの本質的機能（AAA，Mobility Management，U-Plane）が世代を超えて普遍的であることを示し，機能・ノード・プロトコル・状態・インタフェース・ライフサイクルの6つの観点からEPSと5GSの対応関係を体系的に分析した．(2) 5GC固有機能の階層的分析では，基盤層（SBA，CUPS，NFV），制御メカニズム層（Slice Selection，QoS Flow制御，Traffic Steering，Multi-type PDU），応用サービス層（Network Slicing，Edge Computing，5G LAN）の3層に分類し，各層に対する対応戦略（隠蔽・変換整合・段階的提供）を示した．
    \item \textbf{アーキテクチャ概要の提示}（第\ref{sec:proposed_architecture}節）: S1-N2変換ゲートウェイによる透過的介在（R1），トランスレータ方式によるプロトコル相互変換（R2），5GC固有機能の整合（R3）という3つの要件を満たすアーキテクチャを提案した．
    \item \textbf{論理設計上の課題の特定}（第\ref{sec:logical_design_challenges}節）: 手順タイミングの不一致（4GのAttach手順と5GのRegistration/PDU Session確立の分離），認証・鍵管理の分離（EPS-AKAと5G-AKAの鍵導出階層の差異），QoSモデルの差異（Bearer単位とFlow単位の粒度差）という3つの主要課題を整理した．
\end{itemize}

次章では，これらの課題に対する具体的な設計解と，プロトタイプ実装について述べる．
