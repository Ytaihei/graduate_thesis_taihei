\chapter{提案手法：S1-N2変換ゲートウェイ}
\label{chap:proposal}

本章では，第\ref{chap:background}章で述べた技術背景を踏まえ，4G RANを改修なしに5Gコア（5GC）へ収容可能にするS1-N2変換ゲートウェイを提案する．まず\ref{sec:proposal_theoretical_foundation}節で提案の理論的基盤を述べる．提案手法を実現するためのアーキテクチャ設計，詳細設計，プロトタイプ実装，およびプロトコル仕様上の制約については，第\ref{chap:design}章で述べる．

\section{提案の理論的基盤}
\label{sec:proposal_theoretical_foundation}

本節では，提案手法の根拠となる2つの重要な分析フレーム---「機能的共通性」と「5GC固有機能の階層的分析」---を述べる．

\subsection{4G/5G間の機能的共通性}
\label{subsec:functional_commonality}

モバイル通信システムは世代ごとにプロトコル仕様が大きく異なるが，提供される基本的な制御機能の役割には高い共通性が存在する．Watanabeらは，AAA，移動管理，およびユーザデータ転送といった本質的な機能要件は世代を超えて不変であると指摘し，C-PlaneとRANの機能を抽象化・融合した自律分散型アーキテクチャを提案している\cite{Watanabe_ADMobile2025}．しかし，具体的にどのプロトコルパラメータがこれらの共通機能に対応するかについては詳細な分析を行っていない．

本研究は，この観察を理論的基盤として採用する．ただし，Watanabeらの研究がコアネットワーク自体の自律分散型への移行を志向しているのに対し，本研究は既存の5GC標準アーキテクチャを維持したまま，RANとCNの間にプロトコル変換層を挿入するアプローチを取る．これにより，プロトコル形式の違いを乗り越え，「機能的な等価性」に基づく相互運用を実現する．

以下では，まずモバイルシステムの本質的機能要件を定義し，次に機能・ノード・プロトコル・状態・インタフェース・ライフサイクルの6つの観点から，EPSと5GSの対応関係を多角的に分析する．その上で，プロトコル変換によるインターワーキングの実現可能性を論じる．

\subsubsection{モバイルシステムの本質的機能要件}

モバイル通信は世代を超えて，本質的に以下の3つの機能を提供する必要がある．

\begin{enumerate}
\item \textbf{認証・認可・課金（AAA）}: ユーザの身元確認，サービス利用権限の検証，および通信料金の計算・請求を行う機能．通信事業者のビジネスモデルを成立させるために不可欠である．

\item \textbf{移動管理}: 端末が地理的に移動する際に，その現在位置を把握し，位置情報に基づいてシグナリングやデータをルーティングする機能．移動体通信の基本的価値である「場所を問わない接続性」の実現に不可欠である．

\item \textbf{ユーザデータ転送（U-Plane）}: 実際のユーザデータ（音声，映像，パケットデータ等）をネットワークを介して転送する機能．通信サービスの本体であり，いかなる世代においても必須である．
\end{enumerate}

これら3つの機能は，個々の通信プロトコルやノード構成が異なるとしても，モバイルシステムの定義上避けられない基本要件である．4Gと5Gのプロトコル仕様は大きく異なるが，これらの機能要件そのものは変わらない．

\subsubsection{本質的機能の多角的分析}

本質的機能が普遍的である以上，その機能を実現するシステムの各側面にも対応関係が存在する．ここでは，モバイルシステムを分析するための6つの観点---\textbf{機能}，\textbf{ノード}，\textbf{プロトコル}，\textbf{状態}，\textbf{インタフェース}，\textbf{ライフサイクル}---からEPSと5GSの対応関係を整理する．

\paragraph{機能の対応}

前節で定義した3つの本質的機能（AAA・移動管理・U-Plane転送）は，EPSでも5GSでも必ず実装されている．表\ref{tab:function_terminology}に，各機能がEPSと5GSの仕様においてどのような用語で記述されているかを示す．

\begin{table}[htbp]
\centering
\caption{本質的機能に対応するEPS/5GSの仕様用語}
\label{tab:function_terminology}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証: EPS AKA\newline
認可: 加入者プロファイル参照\newline
課金: オンライン/オフライン &
認証: 5G AKA\newline
認可: 加入者プロファイル参照\newline
課金: オンライン/オフライン \\
\hline
移動管理 &
サブレイヤ: EMM\newline
初期接続: Attach\newline
切断: Detach\newline
エリア更新: TAU &
サブレイヤ: 5GMM\newline
初期接続: Registration\newline
切断: Deregistration\newline
エリア更新: Registration Update \\
\hline
U-Plane &
サブレイヤ: ESM\newline
セッション: EPS Bearer\newline
転送プロトコル: GTP-U &
サブレイヤ: 5GSM\newline
セッション: PDU Session\newline
転送プロトコル: GTP-U \\
\hline
\end{tabular}
\end{table}

以下に各機能の用語について解説する．

\begin{itemize}
  \item \textbf{AAA機能}: EPSではEPS AKA（Authentication and Key Agreement）で相互認証を行い，加入者プロファイルを参照して認可を行う．課金はオンラインまたはオフライン方式で実施される．5GSでは5G AKAが認証を担い，同様に加入者プロファイルを参照して認可を行う．課金方式も同様にオンライン/オフラインが存在する．
  \item \textbf{移動管理機能}: EPSではNASプロトコルの中にEMM（EPS Mobility Management）サブレイヤが定義されている．主要な手続きとして，初期接続時のAttach，切断時のDetach，エリア更新時のTAU（Tracking Area Update）がある．5GSでは5GMM（5GS Mobility Management）サブレイヤが同等の機能を提供し，AttachはRegistrationに，DetachはDeregistrationに，TAUはRegistration Updateにそれぞれ対応する．
  \item \textbf{U-Plane機能}: EPSではNASプロトコルの中にESM（EPS Session Management）サブレイヤが定義されている．セッションとしてEPS Bearerを確立し，転送プロトコルとしてGTP-Uを使用する．5GSでは5GSM（5GS Session Management）サブレイヤが同等の機能を提供し，EPS BearerはPDU Sessionに対応する．GTP-Uは両世代で共通であり，この点がU-Plane変換を比較的容易にしている．
\end{itemize}

\paragraph{ノードの対応}

3つの本質的機能がEPSと5GSにおいてどのノードによって実装されているかを表\ref{tab:essential_functions_mapping}に示す．

\begin{table}[htbp]
\centering
\caption{本質的機能要件とEPS/5GSにおける実装ノードの対応}
\label{tab:essential_functions_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証: HSS\newline
認可: HSS\newline
課金: PCRF/OCS &
認証: AUSF\newline
認可: UDM/UDR\newline
課金: CHF \\
\hline
移動管理 &
C-Plane制御: MME\newline
（セッション管理も兼務） &
C-Plane制御: AMF\newline
セッション管理: SMF \\
\hline
U-Plane &
データ転送: S-GW/P-GW\newline
（C-Plane処理も兼務） &
データ転送: UPF\newline
C-Plane制御: SMF \\
\hline
\end{tabular}
\end{table}

以下に各機能のノード対応について解説する．

\begin{itemize}
  \item \textbf{AAA機能}: EPSではHSSが認証・認可を統一的に管理し，課金はPCRF/OCSが担う．5GSではAUSFが認証を，UDM/UDRが認可（加入者プロファイル管理）を，CHFが課金をそれぞれ専任で担当する．
  \item \textbf{移動管理機能}: EPSではMMEがC-Plane制御とセッション管理を兼務する．5GSではAMFがC-Plane制御を専任し，セッション管理はSMFに分離されている．
  \item \textbf{U-Plane機能}: EPSではS-GW/P-GWがデータ転送とC-Plane処理を兼務する．5GSではUPFがデータ転送を専任し，C-Plane制御はSMFに分離されている．
\end{itemize}

このように，EPSでは1つのノードが複数機能を兼務していたのに対し，5GSでは各機能が独立したNFに分解されている．ノード構成は大きく異なるが，機能とノードの対応関係を明示すれば，変換時のルーティング先を決定できる．

\paragraph{プロトコルの対応}

各本質的機能を実現するプロトコル要素（パラメータ，手続き，符号化）にも対応関係が存在する．表\ref{tab:protocol_parameters}に本質的機能ごとの主要プロトコル要素の対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとの主要プロトコル要素のEPS/5GS対応}
\label{tab:protocol_parameters}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
パラメータ: IMSI, GUTI\newline
手続き: EPS AKA\newline
符号化: NAS独自形式 &
パラメータ: SUPI/SUCI, 5G-GUTI\newline
手続き: 5G AKA\newline
符号化: NAS独自形式 \\
\hline
移動管理 &
パラメータ: TAI\newline
手続き: Attach, TAU, Detach\newline
符号化: S1AP (ASN.1/PER) &
パラメータ: TAI（同一構造）\newline
手続き: Registration, Reg. Update, Dereg.\newline
符号化: NGAP (ASN.1/PER) \\
\hline
U-Plane &
パラメータ: APN, QCI, EBI\newline
手続き: Bearer確立/解放\newline
符号化: GTP-U &
パラメータ: DNN, 5QI, QFI\newline
手続き: PDU Session確立/解放\newline
符号化: GTP-U \\
\hline
\end{tabular}
\end{table}

以下に各機能のプロトコル対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: ユーザ識別子としてIMSI⇔SUPI/SUCI，一時識別子としてGUTI⇔5G-GUTIが対応する．認証手続きはEPS AKA⇔5G AKAが対応し，いずれもNAS層で独自形式により符号化される．
  \item \textbf{移動管理}: エリア識別子TAIは両者で同一構造である．手続きとしてAttach⇔Registration，TAU⇔Registration Update，Detach⇔Deregistrationが対応する．AS層はS1AP/NGAPともにASN.1/PERで符号化される．
  \item \textbf{U-Plane}: 接続先としてAPN⇔DNN，品質クラスとしてQCI⇔5QI，識別子としてEBI⇔QFIが対応する．セッション手続きとしてBearer確立⇔PDU Session確立が対応する．GTP-Uは両者で共通のため変換が容易である．
\end{itemize}

\paragraph{状態の対応}

モバイルシステムは状態機械として動作し，各本質的機能に対応する状態遷移を管理する．表\ref{tab:state_mapping}に本質的機能ごとの状態対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとの状態のEPS/5GS対応}
\label{tab:state_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
認証成功: EMM-REGISTEREDへ遷移\newline
認証失敗: 接続拒否 &
認証成功: 5GMM-REGISTEREDへ遷移\newline
認証失敗: 接続拒否 \\
\hline
移動管理 &
未登録: EMM-DEREGISTERED\newline
登録済: EMM-REGISTERED &
未登録: 5GMM-DEREGISTERED\newline
登録済: 5GMM-REGISTERED \\
\hline
U-Plane &
未接続: ECM-IDLE\newline
接続中: ECM-CONNECTED &
未接続: CM-IDLE\newline
接続中: CM-CONNECTED \\
\hline
\end{tabular}
\end{table}

以下に各機能の状態対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 認証結果に応じて状態が遷移する．認証成功時はEMM-REGISTERED/5GMM-REGISTEREDへ遷移し，失敗時は接続が拒否される．認証自体は独立した状態機械を持たないが，移動管理状態の遷移条件として機能する．
  \item \textbf{移動管理}: EPSではEMM-DEREGISTERED/EMM-REGISTEREDで端末の登録有無を管理する．5GSでは5GMM-DEREGISTERED/5GMM-REGISTEREDが対応する．Attach完了⇔Registration完了で登録済み状態へ遷移する．
  \item \textbf{U-Plane}: EPSではECM-IDLE/ECM-CONNECTEDでセッション接続状態を管理する．5GSではCM-IDLE/CM-CONNECTEDが対応する．プロトコル変換ゲートウェイは，これらの状態遷移を追跡し，4G側の状態を5G側にマッピングする必要がある．
\end{itemize}

\paragraph{インタフェース境界の対応}

各本質的機能が使用するインタフェース境界にも対応関係が存在する．表\ref{tab:interface_mapping}に本質的機能ごとのインタフェース対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとのインタフェースのEPS/5GS対応}
\label{tab:interface_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
RAN--CN: S1-MME（S1AP内NAS）\newline
CN内部: S6a（Diameter） &
RAN--CN: N2（NGAP内NAS）\newline
CN内部: SBI（HTTP/2） \\
\hline
移動管理 &
RAN--CN: S1-MME（S1AP）\newline
CN内部: S11（GTP-C） &
RAN--CN: N2（NGAP）\newline
CN内部: SBI（HTTP/2） \\
\hline
U-Plane &
RAN--CN: S1-U（GTP-U）\newline
CN--DN: SGi &
RAN--CN: N3（GTP-U）\newline
CN--DN: N6 \\
\hline
\end{tabular}
\end{table}

以下に各機能のインタフェース対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 認証情報はRAN--CN境界ではS1AP/NGAP内のNASメッセージとして搬送される．CN内部ではEPSがS6a（Diameter）でHSSと通信し，5GSはSBI（HTTP/2）でAUSF/UDMと通信する．
  \item \textbf{移動管理}: RAN--CN境界ではS1-MME（S1AP）⇔N2（NGAP）が対応する．両者ともASN.1/PERで符号化されるが，メッセージ構造が異なるため変換が必要である．
  \item \textbf{U-Plane}: RAN--CN境界ではS1-U⇔N3が対応し，両者ともGTP-Uを使用する．5GSでは拡張ヘッダ（PDU Session Container）が追加されている．本研究が対象とするのは\textbf{RAN--CN境界（S1⇔N2/N3）}の変換である．
\end{itemize}

\paragraph{ライフサイクルの対応}

端末のライフサイクルに沿って，各本質的機能が発動するタイミングにも対応関係が存在する．表\ref{tab:lifecycle_mapping}に本質的機能ごとのライフサイクル対応を示す．

\begin{table}[htbp]
\centering
\caption{本質的機能ごとのライフサイクル段階のEPS/5GS対応}
\label{tab:lifecycle_mapping}
\begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
\hline
\textbf{本質的機能} & \textbf{EPS (4G)} & \textbf{5GS (5G)} \\
\hline
AAA &
発動: 初期接続時\newline
手続き: EPS AKA &
発動: 初期接続時\newline
手続き: 5G AKA \\
\hline
移動管理 &
初期接続: Attach\newline
エリア移動: TAU/Handover\newline
切断: Detach &
初期接続: Registration\newline
エリア移動: Registration Update/Handover\newline
切断: Deregistration \\
\hline
U-Plane &
セッション確立: Bearer確立\newline
データ転送: GTP-U\newline
セッション解放: Bearer解放 &
セッション確立: PDU Session確立\newline
データ転送: GTP-U\newline
セッション解放: PDU Session解放 \\
\hline
\end{tabular}
\end{table}

以下に各機能のライフサイクル対応について解説する．

\begin{itemize}
  \item \textbf{AAA}: 初期接続時に認証が発動する．EPSではEPS AKA，5GSでは5G AKAが対応する．認証成功後に移動管理の登録状態へ遷移する．
  \item \textbf{移動管理}: 初期接続（Attach⇔Registration），エリア移動（TAU/Handover⇔Registration Update/Handover），切断（Detach⇔Deregistration）の各段階で発動する．端末のライフサイクル全体を通じて状態を管理する．
  \item \textbf{U-Plane}: セッション確立（Bearer確立⇔PDU Session確立），データ転送（GTP-U共通），セッション解放の各段階で発動する．GTP-Uが両者で共通であることがU-Plane変換を容易にしている．
\end{itemize}

\subsubsection{機能的共通性に基づくインターワーキングの実現可能性}

以上の多角的分析から，次の結論が得られる．

\begin{enumerate}
\item \textbf{本質的機能は普遍的である}: モバイルシステムが提供すべき「AAA」「移動管理」「U-Plane転送」という3つの機能要件は，4G（EPS）から5G（5GS）への進化を通じても変わらない．

\item \textbf{6つの観点すべてに対応関係が存在する}: 表\ref{tab:six_aspects_summary}に示す通り，機能・ノード・プロトコル・状態・インタフェース・ライフサイクルのいずれの観点においても，EPSと5GSの間に明確な対応関係が存在する．
\end{enumerate}

\begin{table}[htbp]
\centering
\caption{6つの観点におけるEPS/5GS対応関係の要約}
\label{tab:six_aspects_summary}
\begin{tabular}{|p{2.8cm}|p{4.5cm}|p{4.5cm}|p{2.5cm}|}
\hline
\textbf{観点} & \textbf{EPS (4G) 例} & \textbf{5GS (5G) 例} & \textbf{対応の性質} \\
\hline
機能 & AAA, 移動管理, U-Plane & AAA, 移動管理, U-Plane & 完全一致 \\
\hline
ノード & HSS, MME, S-GW/P-GW & AUSF/UDM, AMF/SMF, UPF & 1対多分解 \\
\hline
プロトコル & IMSI, Attach, GTP-U & SUPI, Registration, GTP-U & 意味的等価 \\
\hline
状態 & EMM/ECM状態 & 5GMM/CM状態 & 1対1対応 \\
\hline
インタフェース境界 & S1-MME, S1-U, SGi & N2, N3, N6 & 機能的等価 \\
\hline
ライフサイクル & Attach→Bearer→転送→Detach & Registration→PDU→転送→Dereg & 順序対応 \\
\hline
\end{tabular}
\end{table}

この包括的な対応関係の存在が，本研究で提案するプロトコル変換方式による4G RANの5GC収容を実現する理論的根拠となる．変換ゲートウェイは，これら6つの観点における対応関係を活用し，S1インタフェースの通信をN2/N3インタフェースの通信に変換する．具体的には，各本質的機能（AAA，移動管理，U-Plane）について，対応するノード，プロトコル，状態，インタフェース，ライフサイクル段階のマッピングルールを適用することで，4G端末からの通信を5GCが処理可能な形式に変換する．

\subsection{5GC固有機能の階層的分析}
\label{subsec:5gc_layered_design_framework}

異世代間の相互運用性を実現する一方で，5Gのみが提供する新しい機能が存在する．本手法の設計にあたり，これらの5GC固有機能を「基盤層」「制御メカニズム層」「応用サービス層」の3層に分類し，設計優先度を整理する．

図\ref{fig:5gc_layered_model}に5GC機能の階層モデルを示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.85, transform shape, every node/.style={font=\small}]
    % Layer 3: Service / Outcome
    \node[draw, rounded corners, fill=GoogleGreen!15, minimum width=14cm, minimum height=2.2cm] (layer3) at (0,5.2) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer3.north west) {3. 応用サービス層 (Service / Outcome)};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer3.north west) {
      \textbf{機能例}: ネットワークスライシング，NEF (API公開)，MEC\\
      \textbf{要件}: マルチテナンシー，異業種連携 (Vertical Integration)};

    % Layer 2: Mechanism
    \node[draw, rounded corners, fill=GoogleYellow!15, minimum width=14cm, minimum height=2.2cm] (layer2) at (0,2.6) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer2.north west) {2. 制御メカニズム層 (Mechanism)};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer2.north west) {
      \textbf{機能例}: Flow-based QoS，ULCL (Uplink Classifier)\\
      \textbf{要件}: 全く異なる要求条件（超低遅延・多数同時接続等）を持つトラフィックの混在収容};

    % Layer 1: Infrastructure
    \node[draw, rounded corners, fill=GoogleBlue!15, minimum width=14cm, minimum height=2.2cm] (layer1) at (0,0) {};
    \node[anchor=north west, font=\bfseries] at ([xshift=0.3cm, yshift=-0.2cm]layer1.north west) {1. 基盤層 (Infrastructure / Enabler)};
    \node[anchor=north west, text width=13cm] at ([xshift=0.5cm, yshift=-0.8cm]layer1.north west) {
      \textbf{機能例}: SBA (Service Based Architecture)，CUPS (分離アーキテクチャ)\\
      \textbf{要件}: クラウドネイティブ環境への適応，開発サイクルの高速化};

    % Connections
    \draw[->, thick, gray] (layer1.north) -- (layer2.south);
    \draw[->, thick, gray] (layer2.north) -- (layer3.south);

  \end{tikzpicture}
  \caption{5GC機能の3層構造モデル（設計分析フレーム）}
  \label{fig:5gc_layered_model}
\end{figure}

\subsubsection{基盤層}

基盤層は，CNのアーキテクチャそのものの変革を指す．代表的な技術として，SBA (Service Based Architecture) やCUPS (Control and User Plane Separation) が挙げられる．本層における5GCの要件は，\textbf{クラウドネイティブ環境への適応}と\textbf{開発サイクルの高速化}である．物理ハードウェアに依存しないソフトウェア化により，機能の追加やスケーリングを迅速に行うことが可能となる．

\subsubsection{制御メカニズム層}

制御メカニズム層は，アーキテクチャ上で動作する通信制御の仕組みを指す．4Gではベアラ (Bearer) 単位であったQoS制御が，5Gではフロー (Flow-based QoS) 単位へと細分化されたほか，特定のトラフィックをエッジ側で折り返すULCL (Uplink Classifier) などの機能が導入された．本層における要件は，超低遅延や多数同時接続など，\textbf{全く異なる要求条件を持つトラフィックが混在する状態への対応}である．

本研究では，共通機能の確実な実現を最優先とし，このメカニズム層の一部（Flow-based QoS）は段階的な拡張の対象とする．

\subsubsection{応用サービス層}

応用サービス層は，ユーザーや事業者が享受する最終的なメリットを創出する層である．論理的に分割されたネットワークを提供するネットワークスライシングや，ネットワーク機能を外部公開するNEF (Network Exposure Function)，およびMEC (Multi-access Edge Computing) との連携が含まれる．本層における要件は，\textbf{マルチテナンシー}と\textbf{異業種連携 (Vertical Integration)}の実現である．

本提案では，共通機能の検証に集中するため，本層の機能は初版では部分的なサポートとし，将来的な拡張として位置づける．

% 旧4.2節「基本概念：世代共通機能に基づく疎結合化」は4.1.1節と内容が重複するため削除
% 疎結合化の概念は4.1.1節の「機能的共通性」で説明済み
