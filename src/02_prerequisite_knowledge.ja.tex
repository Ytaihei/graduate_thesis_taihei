\chapter{モバイルネットワークの技術背景と異世代間の差異}
\label{chap:background}

本章では，本研究の前提となるモバイルネットワークの技術的背景と，4G/5G間に存在する技術的な差異について詳述する．
第\ref{chap:introduction}章で述べた「CNとRANの進化の乖離」という問題背景を踏まえ，本章ではまずモバイルネットワークの基本構成を示した後，4Gと5Gのシステムアーキテクチャの違いを整理する．次に，既存の移行技術（NSA/SA等）がなぜレガシーRANの収容に適さないのかを説明し，最後に，本研究が解決すべき具体的な技術的障壁（プロトコルレベルの差異）を明らかにする．

\section{モバイルネットワークの基本構成}

本節では，本研究の前提となるモバイルシステムの論理的な構成要素と，それらの役割について概説する．

モバイルネットワークは，図\ref{fig:mobile_network_generic}に示すように，\textbf{UE（User Equipment）}，\textbf{RAN（Radio Access Network）}，\textbf{TN（Transport Network）}，\textbf{CN（Core Network）}，\textbf{DN（Data Network）}の5つの主要要素から構成される．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.88, every node/.style={font=\small}]
    % UE
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.8cm, minimum height=1.1cm, align=center] (ue) at (0,0) {\textbf{UE}};

    % RAN
    \node[draw, rounded corners, fill=GoogleBlue!40, minimum width=2.2cm, minimum height=1.1cm, align=center] (ran) at (3.5,0) {\textbf{RAN}};

    % TN
    \node[draw, rounded corners, fill=GoogleYellow!40, minimum width=2.2cm, minimum height=1.1cm, align=center] (tn) at (7,0) {\textbf{TN}};

    % CN
    \node[draw, rounded corners, fill=GoogleGreen!40, minimum width=2.2cm, minimum height=1.1cm, align=center] (cn) at (10.5,0) {\textbf{CN}};

    % DN
    \node[draw, cloud, cloud puffs=10, cloud ignores aspect, minimum width=2.2cm, minimum height=1.3cm, fill=GoogleLightGray!50, align=center] (dn) at (14,0) {\textbf{DN}};

    % 接続線
    \draw[-, very thick, dashed] (ue) -- (ran);
    \draw[-, very thick] (ran) -- (tn);
    \draw[-, very thick] (tn) -- (cn);
    \draw[-, very thick] (cn) -- (dn);

    % 説明枠
    \node[draw, fill=white, anchor=north west, font=\scriptsize, inner sep=3pt, text width=13.5cm] at ([yshift=-10pt]current bounding box.south west) {
      \begin{tabular}{@{}ll@{}}
        \textbf{UE:} & 端末（スマートフォン，IoTデバイス等）\\
        \textbf{RAN:} & 無線アクセス網（基地局群）\\
        \textbf{TN:} & RANとCNを接続する伝送ネットワーク（IP網，専用線等）\\
        \textbf{CN:} & コアネットワーク（認証，移動管理，データ転送制御等）\\
        \textbf{DN:} & 外部データネットワーク（インターネット，企業網等）\\
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{モバイルネットワークの普遍的な基本構成}
  \label{fig:mobile_network_generic}
\end{figure}

各構成要素の役割を以下に示す：

\begin{itemize}
  \item \textbf{UE（User Equipment）}: ユーザが直接操作する移動端末を指す．スマートフォン，タブレット，IoTデバイス等が含まれる．UEには加入者の識別情報や認証鍵等が格納される\textbf{SIM（Subscriber Identity Module）}が搭載され，RANとの無線インターフェースを介してネットワークに接続する．
  \item \textbf{RAN（Radio Access Network）}: 無線アクセスネットワークであり，UEとCNの間で無線通信を提供する基地局群を指す．RANはモバイルシステムの世代ごとに無線方式（変調，符号化，周波数帯等の技術仕様等）が異なり，これらの仕様を総称して\textbf{RAT（Radio Access Technology）}と呼ぶ．
  \item \textbf{TN（Transport Network）}: RANとCNを接続する伝送ネットワークを指し，IP網，専用線，MPLS\footnote{Multi-Protocol Label Switching: ラベルによる高速パケット転送技術．}等の技術を用いて実装され，RANとCN間のデータを伝送する．
  \item \textbf{CN（Core Network）}: 認証・課金，移動管理，セッション管理，ルーティング制御といった中核機能を提供する．
  \item \textbf{DN（Data Network）}: 外部データネットワークを指し，インターネット，企業内ネットワーク，IMS\footnote{IP Multimedia Subsystem: IPベースのマルチメディアサービス基盤．}等が含まれる．UEは最終的にDN上のサービス（Webアクセス，ビデオストリーミング，VoIP\footnote{Voice over IP: IPネットワーク上での音声通信．}等）を利用する．
\end{itemize}

\section{4Gと5Gのアーキテクチャ}

本節では，\textbf{4Gシステム（EPS: Evolved Packet System）}と\textbf{5Gシステム（5GS: 5G System）}のアーキテクチャを，RANとCNの境界インターフェースおよびUEーCN間の制御層に焦点を当てて比較する．

\subsection{4G EPS: eNBとEPCの接続}

4Gシステム（EPS）は，無線アクセスネットワーク（RAN）を担う\textbf{E-UTRAN（Evolved Universal Terrestrial Radio Access Network）}と，コアネットワークである\textbf{EPC（Evolved Packet Core）}から構成される\cite{3GPP_TS23401}．E-UTRANのRATは\textbf{LTE（Long Term Evolution）}と呼ばれ，基地局ノードとして\textbf{eNB（evolved Node B）}が配置される．EPCの主要ノードとして，移動管理を行うMME（Mobility Management Entity），ユーザデータ転送を行うS-GW（Serving Gateway）/P-GW（PDN Gateway），および加入者情報を管理するHSS（Home Subscriber Server）が存在する．これらの機能は，従来，専用ハードウェアアプライアンス上で提供されていた\cite{etsi_nfv_2012}．
また，EPCへの接続端点としては，UEと，外部ネットワーク（インターネットや企業ネットワーク）を表すDNがある．UEはeNBを経由して無線アクセスでEPCに接続し，P-GWを通じてDNの各種サービスにアクセスする．

図\ref{fig:epc_architecture}に4G EPSの基本構成を示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.8, every node/.style={font=\small}]
    % UE
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.2cm, minimum height=0.8cm] (ue) at (-1,-2) {UE};

    % eNB
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.5cm, minimum height=0.8cm] (enb) at (3,-2) {eNB};

    % E-UTRAN枠（RAN; RAT: LTE）
    \node[draw, dashed, thick, GoogleBlue, fit={(1.5,-2.9) (4.5,-1.1)}, inner sep=0pt] (eutran) {};
    \node[above, GoogleBlue] at (3,-1.1) {\textbf{E-UTRAN}};

    % MME
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (mme) at (6.5,-0.5) {MME};

    % S-GW
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (sgw) at (6.5,-2) {S-GW};

    % P-GW
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (pgw) at (10,-2) {P-GW};

    % HSS
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (hss) at (10,-0.5) {HSS};

    % Internet/DN
    \node[draw, cloud, cloud puffs=10, cloud ignores aspect, minimum width=2cm, minimum height=1cm, fill=GoogleLightGray!50] (dn) at (13,-2) {DN};

    % EPC枠
    \draw[dashed, thick, GoogleGreen] (5.2,-3) rectangle (11.3,0.5);
    \node[above, GoogleGreen] at (8.25,0.5) {\textbf{EPC}};

    % 接続線（C-Plane）
    \draw[-, thick, dashed] (ue) -- (enb) node[midway, above] {\scriptsize LTE-Uu};
    \draw[-, thick] (enb) -- (mme) node[midway, above, sloped] {\scriptsize S1-MME};
    \draw[-, thick] (mme) -- (hss) node[midway, above] {\scriptsize S6a};
    \draw[-, thick] (mme) -- (sgw) node[midway, right] {\scriptsize S11};

    % 接続線（U-Plane）
    \draw[-, thick, GoogleRed] (enb) -- (sgw) node[midway, below] {\scriptsize S1-U};
    \draw[-, thick, GoogleRed] (sgw) -- (pgw) node[midway, below] {\scriptsize S5/S8};
    \draw[-, thick, GoogleRed] (pgw) -- (dn) node[midway, below] {\scriptsize SGi};

    % 凡例（色と線を統合，縦並び）
    \node[draw, fill=white, anchor=north west, font=\scriptsize, inner sep=2pt] at (-1,2.5) {
      \begin{tabular}{@{}l@{}}
      \csname textbf\endcsname{凡例}\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleLightGray!50] (0,0) rectangle (0.18,0.18);\ UE/DN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!50] (0,0) rectangle (0.18,0.18);\ E-UTRAN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleGreen!50] (0,0) rectangle (0.18,0.18);\ EPC\\
      C-Plane: 黒線\\
      \textcolor{GoogleRed}{U-Plane: 赤線}
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{4Gシステム（EPS: Evolved Packet System）の基本構成}
  \label{fig:epc_architecture}
\end{figure}

\subsubsection{RANとCNの境界: S1インターフェース}
eNBとEPCの接続アーキテクチャは3GPP TS 23.401で規定されており，論理的な境界点として\textbf{S1インターフェース}が定義されている\cite{3GPP_TS23401}．S1は制御面（C-Plane）とユーザ面（U-Plane）に分離されており，それぞれ異なるプロトコルで実装される：
\begin{itemize}
  \item \textbf{S1-MME（C-Plane）}: eNBとMME間の制御信号を伝送．プロトコルとして\textbf{S1AP（S1 Application Protocol）}がSCTP（Stream Control Transmission Protocol）上で動作する\cite{3GPP_TS36413}．位置登録，認証，ベアラ確立などの手順を制御する．
  \item \textbf{S1-U（U-Plane）}: eNBとS-GW間のユーザデータを伝送．プロトコルとして\textbf{GTP-U（GPRS Tunnelling Protocol - User Plane）}がUDP/IP上で動作する\cite{3GPP_TS29281}．GTP-Uは，UEのIPパケットをGTPヘッダでカプセル化し，eNBとS-GW間を仮想的な経路（トンネル）で転送するトンネリングプロトコルである．各トンネルはTEID（Tunnel Endpoint Identifier）で識別される．
  4Gでは，1 EPSベアラは1つのDRB（Data Radio Bearer）と1つのGTP-Uトンネルに1対1で対応する．
\end{itemize}

このアーキテクチャにより，MME（制御）とS-GW（転送）というノード間の機能分離は実現されている．しかし，従来のEPC実装では，データ転送を担うゲートウェイノード（S-GW/P-GW）がC-Plane機能とU-Plane機能を同一ハードウェア内で実装したモノリシック構成となる場合が多かった．その結果，トラフィック増大時にU-Planeリソースのみを独立して拡張することが困難であった．

\subsubsection{UEーCN 間の制御層: RRCとNAS}
4Gでは，無線区間の制御を担う\textbf{RRC（Radio Resource Control）}（UE–eNB間）と，CNと直接やり取りする\textbf{NAS（Non-Access Stratum）}（UE–MME間）が分離される．RRCは無線リンクの確立・解放や測定・ハンドオーバ制御を担い，NASは位置登録（Attach），認証・セキュリティ，ベアラ管理などCN側の手続きを担う\cite{3GPP_TS24301}．NASメッセージはS1AP上をカプセル化されてMMEへ転送され，RANと独立した状態機械・パラメータで動作する．

\subsubsection{CUPS（Control and User Plane Separation）： CU分離によるスケーラビリティの向上}
上述のスケーラビリティの課題を解決するために，3GPPのRelease 14仕様では，EPSに\textbf{CUPS（Control and User Plane Separation）}が導入され，S-GWおよびP-GWをC-Plane（S-GW-C/ P-GW-C）とU-Plane（S-GW-U/ P-GW-U）へ論理的に分離するアーキテクチャ拡張が規定された\cite{3GPP_TS23214}．C-PlaneノードとU-Planeノード間はSxインターフェースで接続され，プロトコルとしてPFCP（Packet Forwarding Control Protocol）が用いられる\cite{3GPP_TS29244}．CUPSにより，U-Plane資源のみの独立スケーリングや地理分散配置が可能となり，CUPS以前のモノリシックなスケール制約が大幅に緩和された．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.85, every node/.style={font=\small}]
    % UE
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.2cm, minimum height=0.8cm] (ue) at (0,0.8) {UE};

    % eNB
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.6cm, minimum height=0.8cm] (enb) at (3,0.8) {eNB};

    % EPC Control Plane box（EPC枠は緑）
    \draw[dashed, thick, GoogleGreen] (5.4,3.6) rectangle (12.3,5.1);
    \node[above, GoogleGreen] at (8.85,5.0) {\textbf{EPC：Control Plane}};

    % EPC User Plane box（SGW-U/PGW-Uのみを囲む）  % 後続で定義
    % MME (Control)
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.6cm, minimum height=0.8cm] (mme) at (6.5,4.2) {MME};

    % SGW-C / PGW-C (Control)
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.4cm, minimum height=0.8cm] (sgwc) at (8.8,4.2) {SGW-C};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.4cm, minimum height=0.8cm] (pgwc) at (11.0,4.2) {PGW-C};

    % SGW-U / PGW-U (User)
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.4cm, minimum height=0.8cm] (sgwu) at (8.8,0.8) {SGW-U};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.4cm, minimum height=0.8cm] (pgwu) at (11.0,0.8) {PGW-U};

    % EPC User Plane box（SGW-U/PGW-UとS5-U/S8-Uラベルを囲む）
    \node[draw, dashed, thick, GoogleGreen, fit=(sgwu)(pgwu), inner xsep=4pt, inner ysep=12pt] (epc_u) {};
    \node[above, GoogleGreen, yshift=-2pt] at (epc_u.north) {\textbf{EPC：User Plane}};

    % DN
    \node[draw, cloud, cloud puffs=10, cloud ignores aspect, minimum width=2.0cm, minimum height=1.0cm, fill=GoogleLightGray!50] (dn) at (14,0.8) {DN};

    % S1-MME (C-Plane)
    \draw[-, thick, dashed] (ue) -- (enb) node[midway, above] {\scriptsize LTE-Uu};
    \draw[-, thick] (enb) -- (mme) node[midway, above, sloped] {\scriptsize S1-MME};

    % S11 and S5-C/S8-C (C-Plane)
    \draw[-, thick] (mme) -- (sgwc) node[pos=0.52, above=10pt] {\scriptsize S11};
    \draw[-, thick] (pgwc) -- (sgwc) node[pos=0.55, above=8pt] {\scriptsize S5-C/S8-C};

    % S1-U (U-Plane) - red
    \draw[-, thick, GoogleRed] (enb) -- (sgwu) node[pos=0.55, below=3pt] {\scriptsize S1-U};
    \draw[-, thick, GoogleRed] (sgwu) -- (pgwu) node[pos=0.5, below=10pt] {\scriptsize S5-U/S8-U};
    \draw[-, thick, GoogleRed] (pgwu) -- (dn) node[pos=0.48, below=10pt] {\scriptsize SGi};

    % Sx (PFCP) links - solid black (C-Plane)
    \draw[-, thick] (sgwc) -- (sgwu) node[midway, right=0.1cm] {\scriptsize Sxa};
    \draw[-, thick] (pgwc) -- (pgwu) node[midway, right=0.1cm] {\scriptsize Sxb};

    % 凡例（色と線を統合，縦並び）
    \node[draw, fill=white, anchor=north west, font=\scriptsize, inner sep=2pt] at ([xshift=2pt,yshift=-2pt]current bounding box.north west) {
      \begin{tabular}{@{}l@{}}
      \csname textbf\endcsname{凡例}\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleLightGray!50] (0,0) rectangle (0.18,0.18);\ UE/DN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!50] (0,0) rectangle (0.18,0.18);\ RAN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleGreen!50] (0,0) rectangle (0.18,0.18);\ CN\\
      C-Plane: 黒線\\
      \textcolor{GoogleRed}{U-Plane: 赤線}
      \end{tabular}
    };

    % E-UTRAN枠（RAN; RAT: LTE）
    \node[draw, dashed, thick, GoogleBlue, fit={(2.0,0.0) (4.0,1.6)}, inner sep=0pt] (eutran) {};
    \node[above, GoogleBlue] at (3,1.6) {\textbf{E-UTRAN}};
  \end{tikzpicture}
  \caption{Release 14で定義されたEPSにおけるCUPS構成}
  \label{fig:eps_cups_architecture}
\end{figure}

\subsection{5GS: gNBと5GCの接続}
\label{sec:5gc_architecture}
5Gシステム（5GS）は，無線アクセスネットワーク（RAN）を担う\textbf{NG-RAN（Next Generation Radio Access Network）}と，コアネットワークである\textbf{5GC（5G Core）}から構成される\cite{3GPP_TS23501_R15}．NG-RANのRATは\textbf{NR（New Radio）}と呼ばれ，基地局ノードとして\textbf{gNB（next generation Node B）}が配置される．
5GCの主要ノードとして，移動管理を行うAMF（Access and Mobility Management Function），セッション管理を行うSMF（Session Management Function），ユーザデータ転送を行うUPF（User Plane Function），加入者情報の管理ロジックを担うUDM（Unified Data Management）およびデータストアとして機能するUDR（Unified Data Repository），認証を行うAUSF（Authentication Server Function）といった\textbf{Network Function（NF）}が存在する．
5GCでは，アーキテクチャが根本的に刷新され，NFV（Network Functions Virtualisation）による仮想化基盤の採用，C-PlaneとU-Planeの分離のネイティブ化（前述のCUPSの思想を基本設計として内包），およびSBA（Service Based Architecture）によるサービス指向設計が特徴となっている．これらの詳細は後述の各小節で述べる．

図\ref{fig:5gc_architecture}に5GCの基本構成を示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.8, every node/.style={font=\small}]
    % UE
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.2cm, minimum height=0.8cm] (ue) at (0,-0.5) {UE};

    % gNB (Next Generation Node B)
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.5cm, minimum height=0.8cm] (gnb) at (3,-0.5) {gNB};

    % NG-RAN枠（RAN; RAT: NR）
    \node[draw, dashed, thick, GoogleBlue, fit=(gnb), inner sep=8pt] (ngran) {};
    \node[above, GoogleBlue] at (ngran.north) {\textbf{NG-RAN}};

    % SBA Bus (lowered)
    \draw[very thick] (5.5,2.2) -- (13,2.2);
    \node[above, font=\scriptsize] at (9.25,2.2) {SBI (Service Based Interface)};

    % NFs on SBA bus（5GCノードは緑）
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.2cm, minimum height=0.6cm] (nrf) at (6,3.3) {NRF};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.2cm, minimum height=0.6cm] (ausf) at (7.8,3.3) {AUSF};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.2cm, minimum height=0.6cm] (udm) at (9.6,3.3) {UDM};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.2cm, minimum height=0.6cm] (pcf) at (11.4,3.3) {PCF};

    % AMF/SMF (connected to bus)
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (amf) at (7,1) {AMF};
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (smf) at (10,1) {SMF};

    % UPF
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.5cm, minimum height=0.8cm] (upf) at (10,-0.5) {UPF};

    % DN
    \node[draw, cloud, cloud puffs=10, cloud ignores aspect, minimum width=2cm, minimum height=1cm, fill=GoogleLightGray!50] (dn) at (14,-0.5) {DN};

    % 5GC枠（緑）
    \draw[dashed, thick, GoogleGreen] (5,-1.2) rectangle (12.5,4.1);
    \node[below, GoogleGreen] at (8.75,-1.2) {\textbf{5GC}};

    % SBA connections (to lowered bus y=2.2)
    \draw[thick] (nrf) -- (6,2.2);
    \draw[thick] (ausf) -- (7.8,2.2);
    \draw[thick] (udm) -- (9.6,2.2);
    \draw[thick] (pcf) -- (11.4,2.2);
    \draw[thick] (amf) -- (7,2.2);
    \draw[thick] (smf) -- (10,2.2);

    % N2/N3 connections
    \draw[-, thick, dashed] (ue) -- (gnb) node[midway, above] {\scriptsize NR-Uu};
    \draw[-, thick] (gnb) -- (amf) node[midway, above, sloped] {\scriptsize N2};
    \draw[-, thick, GoogleRed] (gnb) -- (upf) node[midway, below] {\scriptsize N3};
    \draw[-, thick] (amf) -- (smf) node[midway, above] {\scriptsize N11};
    \draw[-, thick] (smf) -- (upf) node[midway, right] {\scriptsize N4};
    \draw[-, thick, GoogleRed] (upf) -- (dn) node[midway, below] {\scriptsize N6};

    % 凡例（色と線を統合，縦並び）
    \node[draw, fill=white, anchor=north west, font=\scriptsize, inner sep=2pt] at ([xshift=2pt,yshift=-2pt]current bounding box.north west) {
      \begin{tabular}{@{}l@{}}
      \csname textbf\endcsname{凡例}\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleLightGray!50] (0,0) rectangle (0.18,0.18);\ UE/DN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!50] (0,0) rectangle (0.18,0.18);\ NG-RAN\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleGreen!50] (0,0) rectangle (0.18,0.18);\ 5GC\\
      C-Plane: 黒線\\
      \textcolor{GoogleRed}{U-Plane: 赤線}
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{5Gシステム（5GS: 5G System）の基本構成}
  \label{fig:5gc_architecture}
\end{figure}

\subsubsection{RANとCNの境界: NGインターフェース}
gNBと5GCは\textbf{NGインターフェース}で接続される\cite{3GPP_TS38413}．4GのS1インターフェースと同様にC-PlaneとU-Planeに分離されているが，使用するプロトコルが異なる：
\begin{itemize}
  \item \textbf{N2（C-Plane）}: gNBとAMF間の制御信号を伝送．プロトコルとして\textbf{NGAP（NG Application Protocol）}がSCTP上で動作する\cite{3GPP_TS38413}．登録（Registration），認証，PDUセッション確立などの手順を制御する．
  \item \textbf{N3（U-Plane）}: gNBとUPF間のユーザデータを伝送．S1-Uと同様にGTP-Uによるトンネリングを用いるが，5GC特有の情報（QoSフローのQFI等）を運搬する拡張ヘッダ（PDU Session Container）が付与される点が異なる．
  5Gでは，1つのPDUセッション（1つのGTP-Uトンネル）内に複数のQoSフローを多重化でき，QoSフローと無線ベアラ（DRB）の対応は柔軟（多対一／一対一）で実装に依存する．
\end{itemize}

4GのS1インターフェース（S1AP/GTP-U）と5GのNGインターフェース（NGAP/GTP-U拡張ヘッダ）の相違点については，後述の2.3節で詳しく取り扱う．

\subsubsection{UEーCN 間の制御層: RRCとNAS}
5Gでも，UEとRAN間で動作する\textbf{RRC}（UE–gNB間）と，UEとCNが直接やり取りする\textbf{NAS}（UE–AMF間）に分離される．RRCは無線リンクの確立・解放や測定・ハンドオーバ制御を担い，NASは登録（Registration），セキュリティ，PDUセッション管理などCN側の手続きを担う\cite{3GPP_TS24501}．NASメッセージはNGAPを経由してAMFへ転送され，RANと独立したステートマシンとパラメータ体系を持つ．ただし，4G NASと5G NASはプロトコル仕様が異なり，メッセージ体系やセキュリティアーキテクチャ（鍵導出階層等）が刷新されている．この相違は第\ref{sec:protocol_barriers}項で詳しく扱う．
\subsubsection{NFV（Network Functions Virtualisation）：専用機からの解放とコスト削減}
先述の通り，従来のEPCではMMEやS-GW/P-GW等の主要ノードはベンダー独自の専用ハードウェアアプライアンスとして実装されており，ハードウェアとソフトウェアが密結合していた\cite{etsi_nfv_2012}．その結果，高額な設備投資，ベンダーロックイン，および新機能導入時の物理的制約といった課題が生じていた．

この課題を解決するため，5GCでは\textbf{NFV（Network Functions Virtualisation）}技術が全面的に採用され，ネットワーク機能をハードウェアから分離し汎用サーバ上のソフトウェアとして実装する\cite{etsi_nfv_2012}．NFVにより，設備投資・運用コストの削減，ソフトウェアデプロイによる迅速なサービス提供，トラフィック需要に応じた動的スケーリングや，マルチベンダー対応が可能となった．
\subsubsection{SBA（Service Based Architecture）：サービス指向による柔軟性の向上}
従来のEPCでは，ノード間の接続がS6a，S11といった参照点方式で定義されており，各インターフェースが1対1の固定的な関係として実装されていた\cite{3GPP_TS23401}．このため，新機能の追加や既存機能の変更には複数のノード間の仕様調整が必要となり，開発・展開の柔軟性が制約されていた．また，Diameter\footnote{AAA（認証・認可・課金）用のプロトコル．EPCではMMEとHSS間等で使用される．}等の専用プロトコルを用いており，Web標準プロトコルとの連携が難しかった．

この課題を解決するため，5GCでは\textbf{SBA（Service Based Architecture）}が採用されている\cite{3GPP_TS23501_R15}．SBAでは，各NFがHTTP/2およびJSONベースのRESTful APIを用いて\textbf{SBI（Service Based Interface）}上でサービスとして公開され，\textbf{NRF（Network Repository Function）}を介した動的なサービス探索により，疎結合な連携が実現される．このアーキテクチャにより，新NF追加時の既存ノードへの影響を最小限に抑えることができ，NFごとの独立スケーリングが設計上保証され，汎用的なWeb技術やツールが活用できるようになった．SBAにより，5GCは従来のEPCに比べて進化速度と運用柔軟性を大幅に向上させた．

\subsection{4G/5Gアーキテクチャの比較}
\label{subsec:4g5g_comparison}

ここまで述べた4G EPSと5GSの主要な特徴を表\ref{tab:4g5g_architecture_comparison}に整理する．この比較表は，両システム間のプロトコル障壁を理解するための基礎となる．

\begin{table}[htbp]
\centering
\caption{4G EPSと5GSのアーキテクチャ比較}
\label{tab:4g5g_architecture_comparison}
\small
\begin{tabular}{|l|l|l|}
\hline
\textbf{項目} & \textbf{4G EPS} & \textbf{5GS} \\
\hline \hline
\multicolumn{3}{|l|}{\textit{RAN}} \\
\hline
RAT & LTE & NR \\
\hline
基地局 & eNB & gNB \\
\hline
RAN名称 & E-UTRAN & NG-RAN \\
\hline \hline
\multicolumn{3}{|l|}{\textit{CN}} \\
\hline
CN名称 & EPC & 5GC \\
\hline
移動管理 & MME & AMF \\
\hline
セッション管理 & MME（一部） & SMF \\
\hline
U-Plane & S-GW / P-GW & UPF \\
\hline
加入者管理 & HSS & UDM / UDR \\
\hline
認証 & HSS（内包） & AUSF \\
\hline \hline
\multicolumn{3}{|l|}{\textit{RAN-CN境界インターフェース}} \\
\hline
C-Plane IF & S1-MME & N2 \\
\hline
C-Plane プロトコル & S1AP / SCTP & NGAP / SCTP \\
\hline
U-Plane IF & S1-U & N3 \\
\hline
U-Plane プロトコル & GTP-U / UDP & GTP-U + 拡張ヘッダ / UDP \\
\hline \hline
\multicolumn{3}{|l|}{\textit{NAS層}} \\
\hline
NASプロトコル & EPS NAS & 5GS NAS \\
\hline
接続手順 & Attach & Registration \\
\hline
セッション確立 & Default Bearer（Attach内） & PDU Session（別手順） \\
\hline
鍵階層 & KASME → KeNB & KAMF → KgNB \\
\hline \hline
\multicolumn{3}{|l|}{\textit{アーキテクチャ特性}} \\
\hline
CU分離 & CUPS（Rel.14で追加） & ネイティブ \\
\hline
NF間通信 & 参照点方式（Diameter等） & SBA（HTTP/2, RESTful） \\
\hline
QoS単位 & ベアラ（粗粒度） & QoS Flow（細粒度） \\
\hline
\end{tabular}
\end{table}

\section{4Gから5Gへの移行シナリオ}
5Gネットワークの展開形態は，一般に\textbf{SA（Standalone）}と\textbf{NSA（Non-Standalone）}に大別される\cite{5G_NSA_SA_Primer}．

SAは，LTE もしくは 5G NR のいずれか一つの独立したアクセスネットワークのみが，EPC もしくは 5GC のいずれかに接続される構成を指す．特に \textbf{5G SA} は 5G NR と 5GC を組み合わせた構成であり，ネットワークスライシングや高度なQoS制御といった5GC固有の機能をフルに活用できる．

一方，NSAはLTE と 5G NR の両方のRATが共存し，一方のアクセスネットワークが他方が EPC もしくは 5GC に接続するのを支援する構成を指す．NSAでは，一つのRATがMaster Node（MN）として主要なシグナリングを担当し，もう一つのRATがSecondary Node（SN）として補助的に機能する．この両RATが同時に1つのUEに接続する構成を\textbf{デュアルコネクティビティ（DC: Dual Connectivity）}と呼び，複数の周波数帯を活用することで通信容量と信頼性の向上を実現する．\textbf{5G NSA}では，既存の 4G LTE 基盤を活用しながら 5G NR を追加することにより，既存インフラへの投資を活かしつつ段階的に 5G サービスを開始できるという利点がある．

図\ref{fig:nsa_sa_comparison}に両構成の概念的な比較を示す．なお，NSAには複数の実現形態があるため，図は代表例として商用で一般的なOption 3系（特にOption 3x）を示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.75, every node/.style={font=\small}]
    \clip (-7,-3.5) rectangle (17,6);
    % NSA構成（左側）
    \node[font=\normalsize\bfseries] at (-2.5,5) {5G NSA（Option 3xの例）};

    % カバレッジエリア（楕円）
    \draw[GoogleYellow!60, thick, fill=GoogleYellow!15] (-5.5,3.5) ellipse (1.5cm and 0.6cm);
    \node[font=\scriptsize, text=GoogleYellow!60!black] at (-5.5,4.5) {LTEエリア};
    \draw[GoogleBlue!60, thick, fill=GoogleBlue!15] (-5.5,1.5) ellipse (1.5cm and 0.6cm);
    \node[font=\scriptsize, text=GoogleBlue!60!black] at (-5.5,0.5) {NRエリア};

    % UE（エリア内に配置）
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.0cm, minimum height=0.5cm] (nsa-ue-lte) at (-5.5,3.5) {UE};
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.0cm, minimum height=0.5cm] (nsa-ue-nr) at (-5.5,1.5) {UE};

    % eNB / gNB / EPC
    \node[draw, rounded corners, fill=GoogleYellow!50, minimum width=1.6cm, minimum height=0.8cm] (nsa-enb) at (-1.5,3.5) {eNB};
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.6cm, minimum height=0.8cm] (nsa-gnb) at (-1.5,1.5) {gNB};
    \node[draw, rounded corners, fill=GoogleYellow!50, minimum width=1.6cm, minimum height=0.8cm] (nsa-epc) at (2,2.5) {EPC};

    % カバレッジエリアからeNB/gNBへの線
    \draw[-, thick, dashed, GoogleYellow!60] (-4,3.5) -- (nsa-enb.west) node[midway, above] {\scriptsize LTE-Uu};
    \draw[-, thick, dashed, GoogleBlue!60] (-4,1.5) -- (nsa-gnb.west) node[midway, above] {\scriptsize NR-Uu};

    % 接続線（Option 3x の具体）
    % eNB -> EPC (S1-C と S1-U の両方)
    \draw[-, thick] ($(nsa-enb.east)+(0,0.15)$) -- ($(nsa-epc.west)+(0,0.15)$) node[midway, above, sloped] {\scriptsize S1-C};
    \draw[-, thick, GoogleRed] ($(nsa-enb.east)+(0,-0.15)$) -- ($(nsa-epc.west)+(0,-0.15)$) node[midway, below, sloped] {\scriptsize S1-U};

    % User Plane: gNB -> EPC (S1-U)
    \draw[-, thick, GoogleRed] ($(nsa-gnb.east)+(0,-0.15)$) -- ($(nsa-epc.west)+(0,-0.15)$) node[midway, below, sloped] {\scriptsize S1-U};

    % eNB-gNB間（X2/デュアルコネクティビティの概念）
    \draw[-, thick, dashed] (nsa-gnb.north) -- (nsa-enb.south) node[midway, right] {\scriptsize X2};

    % 説明
    \node[text width=10cm, align=center] at (-2,-0.5) {eNBはS1-C/S1-UでEPCに接続\\gNBはS1-UのみでEPCに接続};

    % 区切り線
    \draw[thick, dashed, gray] (3.5,5.5) -- (3.5,-1);

    % SA構成（右側）
    \node[font=\normalsize\bfseries] at (9.5,5) {5G SA};

    % カバレッジエリア（楕円）
    \draw[GoogleBlue!60, thick, fill=GoogleBlue!15] (5.5,2.5) ellipse (1.5cm and 0.6cm);
    \node[font=\scriptsize, text=GoogleBlue!60!black] at (5.5,3.5) {NRエリア};

    % UE
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.0cm, minimum height=0.5cm] (sa-ue) at (5.5,2.5) {UE};

    % gNB
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.6cm, minimum height=0.8cm] (sa-gnb) at (9.5,2.5) {gNB};

    % 5GC
    \node[draw, rounded corners, fill=GoogleBlue!50, minimum width=1.6cm, minimum height=0.8cm] (sa-5gc) at (13.5,2.5) {5GC};

    % カバレッジエリアからgNBへの線
    \draw[-, thick, dashed, GoogleBlue!60] (7,2.5) -- (sa-gnb.west) node[midway, above] {\scriptsize NR-Uu};

    % 接続線
    \draw[-, thick] (sa-gnb) -- (sa-5gc) node[midway, above] {\scriptsize N2/N3};

    % 説明
    \node[text width=5cm, align=center] at (9.5,-0.5) {5G RANと5GCの組み合わせ\\5GC機能が利用可能};

    % 凡例（図の下頃中央に配置）
    \node[draw, fill=white, anchor=north, font=\scriptsize, inner sep=4pt] at (4,-1.2) {
      \begin{tabular}{@{}l@{}}
      \csname textbf\endcsname{凡例}\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleLightGray!50] (0,0) rectangle (0.18,0.18);\ UE\\
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleYellow!50] (0,0) rectangle (0.18,0.18);\ 4G系
      \quad
      \csname tikz\endcsname[baseline=-0.6ex]\fill[GoogleBlue!50] (0,0) rectangle (0.18,0.18);\ 5G系\\
      C-Plane: 黒線 \quad \textcolor{GoogleRed}{U-Plane: 赤線}
      \end{tabular}
    };
  \end{tikzpicture}
  \caption{5G NSA（Option 3xの例）と5G SA構成の概念比較}
  \label{fig:nsa_sa_comparison}
\end{figure}

\subsection{SA/NSAと移行Optionの位置づけ}
\label{sec:sa_nsa_options}
3GPPは，4G（LTE/EPC）から5G（NR/5GC）への段階的な移行を可能にするため，TR 38.801において複数の移行構成（Option 1〜7）を規定している\cite{3GPP_TR38801}．表\ref{tab:migration_options}にOption一覧を示す．ここで，末尾の「a」や「x」は，C-Plane・U-Planeの終端点やアンカー方式等の細部が異なる派生構成を表す．

前節で述べた一般的な5G SA/NSAの分類との対応は以下の通りである：
\begin{itemize}
  \item \textbf{5G SA}に該当: Option 2（gNB+5GC）
  \item \textbf{5G NSA}に該当: Option 3系（eNB+gNB+EPC）
\end{itemize}

各Optionの特徴は以下のように整理できる．

\textbf{Option 1}は従来の4G構成（eNB+EPC）であり，5G技術を含まない純粋なLTEネットワークである．

\textbf{Option 2}は5G SA構成（gNB+5GC）であり，RAN・CN共に5G技術で構成され，5G固有機能を完全に利用可能である．

\textbf{Option 3系}はEN-DC（E-UTRA-NR DC）によって実現される．この構成では，eNBがMNとしてC-Planeの主要機能を担当し，CNはEPCに接続される．一方，gNBはSNとして追加され，NR周波数帯によるU-Planeの高速化に寄与する．

\textbf{Option 4系}はNE-DC（NR-E-UTRA DC）によって実現される．この構成では，gNBがMNとして5GCに接続し，C-Plane・U-Planeの主要機能を担当する．NGインターフェースを終端できるように改修されたLTE基地局のng-eNBがSNとして追加され，LTE周波数帯を活用した補助的なU-Planeを提供する．5GCを利用するため，gNBのカバーエリアでは5G固有の機能を利用可能である．

\textbf{Option 7系}はNGEN-DC（NG-RAN E-UTRA-NR DC）によって実現される．この構成では，ng-eNBがMNとして5GCに接続し，C-Plane・U-Planeの主要機能を担当する．gNBはSNとして追加され，NR周波数帯による高速なU-Planeを提供する．Option 4系と同様にgNBのカバーエリアでは5G固有の機能を利用可能であるが，MNがLTE基地局である点が異なる．

\textbf{Option 5}はng-eNBが単独で5GCに接続する構成であり，DCを伴わない．

\begin{table}[htbp]
  \centering
  \caption{3GPPが規定する移行Option一覧}
  \label{tab:migration_options}
  \begin{tabular}{c|c|c|c|c|l}
    \hline
    Option & CN & RAN: MN & RAN: SN & 分類 & 備考 \\
    \hline\hline
    1 & EPC & eNB & --- & --- & 従来の4G構成（DCなし） \\
    2 & 5GC & gNB & --- & SA & 5G SA構成（DCなし） \\
    3/3a/3x & EPC & eNB & gNB & NSA & EN-DC \\
    4/4a & 5GC & gNB & ng-eNB & NSA & NE-DC \\
    5 & 5GC & ng-eNB & --- & SA & LTE+5GCのSA（DCなし） \\
    7/7a/7x & 5GC & ng-eNB & gNB & NSA & NGEN-DC \\
    \hline
  \end{tabular}
\end{table}

\section{異世代相互接続の技術的障壁}

第\ref{sec:5gc_architecture}節で述べたように，5GCはNFVによる仮想化とSBAによるサービス指向アーキテクチャを採用しており，ソフトウェアベースで柔軟に機能追加・更新が可能である．一方，RANは物理的な基地局設備を伴い，設置場所の制約や設備投資の大きさから，CNと同じペースでの刷新は困難である．このRAN-CN間の進化速度の非対称性を踏まえれば，\textbf{CNを先行して5GCへ移行し，5GC固有の高度な機能を早期に活用する「CN先行アプローチ」}は，事業者にとって合理的な選択肢となり得る．

本節では，この「CN先行アプローチ」を阻む技術的障壁を整理する．まず，RANの改修を前提とするアプローチが抱える本質的な問題を明らかにし，次に，eNBを無改修で5GCへ接続する場合に克服すべきプロトコルレベルの障壁を整理する．

\subsection{RANの改修を前提とするアプローチの本質的課題}

\subsubsection{モバイルネットワークにおけるCNとRANの数的非対称性}
モバイルネットワークの構造上，RANを構成する基地局の数はCNに対して圧倒的に多い．これは，モバイル通信の物理的特性に起因する必然的な構造である．

セルラー方式のモバイル通信では，サービスエリアを多数の「セル」に分割し，各セルに基地局を配置する．無線信号の到達範囲には物理的な限界があるため，広域をカバーするには多数の基地局を地理的に分散配置する必要がある．一般的なマクロセルの場合，1つの基地局がカバーできる半径は都市部で数百メートル〜1km程度，郊外でも数km程度に限られる．屋内や地下街では，さらに小型のスモールセル（フェムトセル，ピコセル等）が高密度に配置される．

一方，CNはこれら全ての基地局からのトラフィックを集約・処理する中央集権的な機能を担う．CNの構成ノードは，地理的には数拠点〜十数拠点のデータセンターに集約配置されるのが一般的である．

この結果，1つのCNに対して数百〜数千，大規模ネットワークでは数万の基地局が接続されるという数的非対称性が生じる．図\ref{fig:cn_ran_asymmetry}にこの構造を示す．日本の大手携帯電話事業者を例にとると，4G LTEの基地局数は各社10万局以上に達する\cite{MIC_Infra_Report_R05}一方，これらを収容するCNは全国で数拠点から十数拠点程度である．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.75, every node/.style={font=\small},
    % 水平間隔=1.5, 垂直間隔=1.3, オフセット=0.75
    hexcell/.style={regular polygon, regular polygon sides=6, minimum size=1.35cm,
                    draw=GoogleBlue!40, thick, fill=GoogleYellow!10, rotate=90}]
    % === 上部：セルラー方式の概念（サービスエリア） ===
    \node[font=\bfseries] at (0,7.5) {サービスエリア（セルに分割）};

    % 六角形セルをハニカム状に配置
    % 水平間隔=1.5, 垂直間隔=1.3, オフセット=0.75

    % 中央行 (y=3.5)
    \foreach \i in {-2,-1,0,1,2} {
      \pgfmathsetmacro{\xcenter}{\i*1.5}
      \node[hexcell] at (\xcenter,3.5) {};
      \node[draw, circle, fill=GoogleBlue!50, inner sep=2pt, font=\tiny] (enb_m\i) at (\xcenter,3.5) {eNB};
    }

    % 上行（オフセット）(y=4.8)
    \foreach \i in {-2,-1,0,1} {
      \pgfmathsetmacro{\xcenter}{\i*1.5+0.75}
      \node[hexcell] at (\xcenter,4.8) {};
      \node[draw, circle, fill=GoogleBlue!50, inner sep=2pt, font=\tiny] (enb_u\i) at (\xcenter,4.8) {eNB};
    }

    % 下行（オフセット）(y=2.2)
    \foreach \i in {-2,-1,0,1} {
      \pgfmathsetmacro{\xcenter}{\i*1.5+0.75}
      \node[hexcell] at (\xcenter,2.2) {};
      \node[draw, circle, fill=GoogleBlue!50, inner sep=2pt, font=\tiny] (enb_l\i) at (\xcenter,2.2) {eNB};
    }

    % セルのカバー範囲を示す注釈
    \draw[<->, thick, GoogleRed] (-0.77,3.9) -- (0.73,3.9);
    \node[font=\tiny, text=GoogleRed, below] at (0,3.2) {数百m〜数km};

    % 省略記号
    \node[font=\large] at (4.5,3.5) {$\cdots$};
    \node[font=\large] at (-4.5,3.5) {$\cdots$};

    % サービスエリア枠
    \draw[dashed, thick, gray, rounded corners] (-4.8,1.0) rectangle (4.8,6.0);

    % === 下部：CNへの集約構造 ===
    % TN（Transport Network）を雲で表現
    \node[cloud, cloud puffs=12, cloud puff arc=70, minimum width=5.5cm, minimum height=1.8cm,
          draw=black!60, thick, fill=gray!15, align=center, font=\scriptsize] (tn) at (0,-0.5)
          {TN};

    % CN
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=3cm, minimum height=1.2cm, align=center] (cn) at (0,-2.8) {\textbf{CN}\\{\scriptsize (数拠点〜十数拠点)}};

    % eNBからTNへの接続線（下行のセルからのみ，雲の曲線に合わせて個別設定）
    \draw[-, thick, GoogleBlue!60] (-2.25,1.3) -- (-2.25,0.4);
    \draw[-, thick, GoogleBlue!60] (-0.75,1.3) -- (-0.75,0.5);
    \draw[-, thick, GoogleBlue!60] (0.75,1.3) -- (0.75,0.5);
    \draw[-, thick, GoogleBlue!60] (2.25,1.3) -- (2.25,0.4);

    % TNからCNへの接続
    \draw[-, very thick] (0,-1.7) -- (cn.north);

    % 数の対比を強調
    \node[draw, fill=white, font=\scriptsize, inner sep=4pt, text width=3.8cm, align=left] at (-6.5,-1.5) {
      \textbf{数的非対称性}\\[2pt]
      eNB: 数百〜数万局\\
      CN: 数拠点〜十数拠点\\[2pt]
      $\Rightarrow$ \textbf{1 : 数百〜数千}
    };

    % 凡例
    \node[draw, fill=white, font=\scriptsize, inner sep=3pt] at (6.5,-0.8) {
      \begin{tabular}{@{}l@{}}
      \textbf{凡例}\\
      \tikz[baseline=-0.6ex]\draw[thick, GoogleBlue!40, fill=GoogleYellow!10] (0,0) -- (0.3,0) -- (0.3,0.2) -- (0,0.2) -- cycle;\ セル（カバー範囲）\\
      \tikz[baseline=-0.6ex]\fill[GoogleBlue!50] (0,0) circle (0.1);\ eNB（基地局）\\
      \tikz[baseline=-0.6ex]\fill[GoogleGreen!50] (0,0) rectangle (0.2,0.15);\ CN（コアネットワーク）\\
      \end{tabular}
    };

  \end{tikzpicture}
  \caption{モバイルネットワークにおけるCNとRANの数的非対称性}
  \label{fig:cn_ran_asymmetry}
\end{figure}

\subsubsection{3GPP標準における世代間インターフェースの分離}
この数的非対称性のもとで問題となるのが，3GPP標準仕様におけるRANとCNの関係性である．

\paragraph{世代同期を前提とした設計思想}
3GPPの標準仕様は，\textbf{RANとCNが同一世代で同期して進化することを暗黙の前提}としている．すなわち，4GのRANは4GのCNと，5GのRANは5GのCNと組み合わせて使用することが想定されており，異なる世代のRANとCNを直接接続するための標準的なインターフェースは定義されていない．

この設計思想は，各世代のRANとCNの境界インターフェースが独立して定義されていることに現れている：
\begin{itemize}
  \item \textbf{4G}: eNBはS1インターフェース（S1AP/GTP-U）でEPCに接続
  \item \textbf{5G}: gNBはNGインターフェース（NGAP/GTP-U＋拡張ヘッダ）で5GCに接続
\end{itemize}

ここで重要な点は，各世代のRANが対応する世代のCN用インターフェースのみを実装しているということである．標準のLTE eNBはS1インターフェースしか持たず，NGインターフェースを実装していない．逆に，5G gNBはNGインターフェースを持つがS1インターフェースは持たない．

\paragraph{密結合モデルの定義}
このような\textbf{RANとCNの世代が同期していることを前提とした設計}を，本論文では\textbf{密結合モデル}と呼ぶ．密結合モデルの下では，ネットワークを次世代へ移行する際には，RANとCNを同時に（または連携して）更新することが求められる．

\subsubsection{密結合モデルがもたらすスケーラビリティの問題}
密結合モデルのもとで4G RANを5GCに接続しようとする場合，RANを改修してNGインターフェースを実装する必要がある．前節で述べたng-eNB（Option 4/5/7）はまさにこのアプローチであり，LTE eNBにNGインターフェースを追加実装したノードである．

しかし，前述のCN-RAN間の数的非対称性を考慮すると，このアプローチには本質的な問題がある．1つのCNに対して数百〜数千の基地局が接続される構成において，RANの改修を前提とするアプローチは，改修対象の絶対数が膨大となる．

すなわち，RANの改修を前提とするアプローチは，改修対象の数という観点でスケールしないという構造的な問題を抱えている．

\subsection{3GPP標準における移行手段の限界}

前項で述べたスケーラビリティの問題に対し，3GPPが定義する移行手段を検討する．第\ref{sec:sa_nsa_options}節で述べた移行Optionのうち，Option 3系はCNがEPCのままであり5GC機能を利用できない．一方，5GCを利用するOption 4/5/7はいずれもng-eNBを前提としている．

ng-eNBは，無線インターフェースはLTE（E-UTRA）のままであるが，CNとのインターフェースとしてNG-C（NGAP）およびNG-Uをサポートし，5GCに直接接続可能である\cite{3GPP_TS38300}．ng-eNBを用いた移行Optionとしては以下が規定されている：
\begin{itemize}
  \item \textbf{Option 5}: ng-eNBが単独で5GCに接続する構成
  \item \textbf{Option 4/4a}: gNBが5GCに接続し，ng-eNBを副ノードとして追加する構成
  \item \textbf{Option 7/7a/7x}: ng-eNBが5GCに接続し（Master Node），gNBを副ノード（Secondary Node）として追加してデュアルコネクティビティを提供する構成
\end{itemize}

しかし，ng-eNBへのアップグレードには，基地局側でNGインターフェース（少なくともNG-C/NGAP）を終端・処理する実装が必要となる．これは結局，\textbf{全てのeNBを改修対象とする}ことを意味し，前項で述べたスケーラビリティの問題を解決しない．

以上より，3GPPが定義する移行手段では\textbf{スケーラビリティの問題を回避できない}．したがって，\textbf{eNBには一切手を加えず，S1インターフェースのまま5GCへ収容する手段}が求められる．

\subsection{eNBを無改修で5GCに接続する場合のプロトコル障壁}
\label{sec:protocol_barriers}

前項で述べた通り，既存のeNBを無改修のまま5GCへ収容するためには，S1インターフェースとNGインターフェースの間でプロトコル変換を行う必要がある．本項では，このプロトコル変換において克服すべき技術的障壁を整理する．

図\ref{fig:protocol_stack_comparison}に4G（S1インターフェース）と5G（NGインターフェース）のC-PlaneおよびU-Planeにおけるプロトコルスタックの比較を示す．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.85, every node/.style={font=\small},
    layer/.style={draw, minimum width=2.8cm, minimum height=0.7cm, align=center},
    label/.style={font=\bfseries\small}]

    % === 4G NAS + S1 C-Plane ===
    \node[label] at (1.4, 4.9) {S1};
    \node[layer, fill=GoogleBlue!40] (nas4) at (1.4, 4.2) {4G NAS};
    \node[layer, fill=GoogleBlue!30] (s1ap) at (1.4, 3.5) {S1AP};
    \node[layer, fill=GoogleBlue!20] (sctp4) at (1.4, 2.8) {SCTP};
    \node[layer, fill=GoogleLightGray!50] (ip4c) at (1.4, 2.1) {IP};

    % === 5G NAS + N2 C-Plane ===
    \node[label] at (5.0, 4.9) {NG};
    \node[layer, fill=GoogleGreen!40] (nas5) at (5.0, 4.2) {5G NAS};
    \node[layer, fill=GoogleGreen!30] (ngap) at (5.0, 3.5) {NGAP};
    \node[layer, fill=GoogleGreen!20] (sctp5) at (5.0, 2.8) {SCTP};
    \node[layer, fill=GoogleLightGray!50] (ip5c) at (5.0, 2.1) {IP};

    % === 4G S1 U-Plane ===
    \node[label] at (9.2, 4.9) {4G U-Plane};
    \node[layer, fill=GoogleBlue!30] (gtpu4) at (9.2, 3.5) {GTP-U};
    \node[layer, fill=GoogleBlue!20] (udp4) at (9.2, 2.8) {UDP};
    \node[layer, fill=GoogleLightGray!50] (ip4u) at (9.2, 2.1) {IP};

    % === 5G N3 U-Plane ===
    \node[label] at (12.8, 4.9) {5G U-Plane};
    \node[layer, fill=GoogleGreen!30] (gtpu5) at (12.8, 3.5) {GTP-U + Ext};
    \node[layer, fill=GoogleGreen!20] (udp5) at (12.8, 2.8) {UDP};
    \node[layer, fill=GoogleLightGray!50] (ip5u) at (12.8, 2.1) {IP};

    % Difference annotations
    \draw[<->, thick, GoogleRed] (nas4.east) -- (nas5.west) node[midway, above, yshift=0pt, font=\scriptsize, text=GoogleRed] {非互換};
    \draw[<->, thick, GoogleRed] (s1ap.east) -- (ngap.west) node[midway, above, yshift=0pt, font=\scriptsize, text=GoogleRed] {非互換};
    \draw[<->, thick, GoogleRed] (gtpu4.east) -- (gtpu5.west) node[midway, above, yshift=0pt, font=\scriptsize, text=GoogleRed] {拡張ヘッダ};

    % Separator
    \draw[dashed, gray] (7.0, 1.6) -- (7.0, 5.2);

    % Labels
    \node[font=\scriptsize] at (3.2, 1.4) {\textbf{制御面 (C-Plane)}};
    \node[font=\scriptsize] at (11.0, 1.4) {\textbf{ユーザ面 (U-Plane)}};

    % Legend
    \node[draw, fill=white, anchor=south, font=\scriptsize, inner sep=3pt] at (7.0, -0.8) {
      \begin{tabular}{@{}l@{}}
        \csname textbf\endcsname{凡例}\\
        \csname tikz\endcsname[baseline=-0.5ex]\fill[GoogleBlue!30] (0,0) rectangle (0.25,0.25);\ 4G\\
        \csname tikz\endcsname[baseline=-0.5ex]\fill[GoogleGreen!30] (0,0) rectangle (0.25,0.25);\ 5G\\
        \csname tikz\endcsname[baseline=-0.5ex]\draw[<->, thick, GoogleRed] (0,0.1) -- (0.5,0.1);\ 変換が必要な箇所\\
      \end{tabular}
    };

  \end{tikzpicture}
  \caption{4Gと5Gのプロトコルスタック比較}
  \label{fig:protocol_stack_comparison}
\end{figure}

図\ref{fig:protocol_stack_comparison}に示すように，トランスポート層（SCTP/UDP over IP）は4G/5Gで共通であるが，上位層のプロトコル（NAS，S1AP/NGAP，GTP-U）に非互換性が存在する．以下，各プロトコルにおける具体的な障壁を整理する．

\subsubsection{S1AP/NGAP: 制御信号と識別子の非互換性}
4Gの制御プロトコルであるS1-APと，5GのNG-APは，共にASN.1で記述されているものの，メッセージ構造やパラメータ（IE：Information Element）が異なる．
例えば，4Gではネットワークへの登録とU-Plane確立を一括して行う「Attach」手順が存在するが，5Gではネットワークへの登録を行う「Registration」とU-Planeの確立を行う「PDU Session Establishment」が分離されている．このステートマシンの違いを吸収しなければ，接続は確立できない．

% TODO: 4G Attach手順と5G Registration/PDU Session Establishment手順の
%       標準シーケンス比較図を追加する（fig:standard_sequence_comparison）
%       - 4G: UE→eNB→MME のAttach手順（Default Bearer確立まで一括）
%       - 5G: UE→gNB→AMF のRegistration手順 + PDU Session Establishment手順（分離）
%       - 両者のタイミング差異を視覚化し、第5章のEarly S1 Setup方式との対比を明確にする

加えて，5GCの特徴である\textbf{ネットワークスライシング}の\textbf{識別子（S-NSSAI）}を運搬するフィールドがS1-APには存在しない．ネットワークスライシングとは，図\ref{fig:network_slicing_concept}に示すように，単一の物理インフラ上に用途別の論理ネットワーク（スライス）を構築する機能である．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
    pnode/.style={draw, circle, fill=gray!40, minimum size=0.5cm, inner sep=0pt},
  ]
    % 物理ノード（平行四辺形: 各辺3ノード，角を共有で8ノード + 中央1ノード）
    % 上辺: a - b - c
    \node[pnode] (a) at (0,1.5) {};
    \node[pnode] (b) at (2,1.5) {};
    \node[pnode] (c) at (4,1.5) {};
    % 右辺: c - d - e (cは共有)
    \node[pnode] (d) at (4.8,0) {};
    \node[pnode] (e) at (5.6,-1.5) {};
    % 下辺: e - f - g (eは共有)
    \node[pnode] (f) at (3.6,-1.5) {};
    \node[pnode] (g) at (1.6,-1.5) {};
    % 左辺: g - h - a (g,aは共有)
    \node[pnode] (h) at (0.8,0) {};
    % 中央
    \node[pnode] (m) at (2.8,0) {};

    % 物理リンク（灰色実線で平行四辺形の辺を構成）
    \draw[thick, gray!60] (a) -- (b) -- (c);
    \draw[thick, gray!60] (c) -- (d) -- (e);
    \draw[thick, gray!60] (e) -- (f) -- (g);
    \draw[thick, gray!60] (g) -- (h) -- (a);
    % 中央への物理リンク
    \draw[thick, gray!60] (b) -- (m);
    \draw[thick, gray!60] (d) -- (m);
    \draw[thick, gray!60] (f) -- (m);
    \draw[thick, gray!60] (h) -- (m);

    % スライス1（青）: a-b-m-d-e（上辺から中央経由で右辺へ）
    \draw[very thick, blue!70, dashed, bend left=12] (a) to (b);
    \draw[very thick, blue!70, dashed, bend left=10] (b) to (m);
    \draw[very thick, blue!70, dashed, bend left=10] (m) to (d);
    \draw[very thick, blue!70, dashed, bend left=12] (d) to (e);

    % スライス2（緑）: a-h-m-f-e（左辺から中央経由で右下へ）
    \draw[very thick, green!60!black, dotted, bend right=12] (a) to (h);
    \draw[very thick, green!60!black, dotted, bend right=10] (h) to (m);
    \draw[very thick, green!60!black, dotted, bend right=10] (m) to (f);
    \draw[very thick, green!60!black, dotted, bend right=12] (f) to (e);

    % スライス3（オレンジ）: a-h-g-f-m-d-e（下辺から中央経由で右辺へ）
    \draw[very thick, orange!80!black, dashdotted, bend left=12] (a) to (h);
    \draw[very thick, orange!80!black, dashdotted, bend left=12] (h) to (g);
    \draw[very thick, orange!80!black, dashdotted, bend left=12] (g) to (f);
    \draw[very thick, orange!80!black, dashdotted, bend right=10] (f) to (m);
    \draw[very thick, orange!80!black, dashdotted, bend right=10] (m) to (d);
    \draw[very thick, orange!80!black, dashdotted, bend right=12] (d) to (e);

    % 凡例
    \draw[thick, gray!60] (7,1.2) -- (7.6,1.2);
    \node[anchor=west, font=\scriptsize] at (7.7,1.2) {物理リンク};
    \draw[very thick, blue!70, dashed] (7,0.6) -- (7.6,0.6);
    \node[anchor=west, font=\scriptsize] at (7.7,0.6) {S-NSSAI = 1};
    \draw[very thick, green!60!black, dotted] (7,0) -- (7.6,0);
    \node[anchor=west, font=\scriptsize] at (7.7,0) {S-NSSAI = 2};
    \draw[very thick, orange!80!black, dashdotted] (7,-0.6) -- (7.6,-0.6);
    \node[anchor=west, font=\scriptsize] at (7.7,-0.6) {S-NSSAI = 3};
  \end{tikzpicture}
  \caption{ネットワークスライシングの概念}
  \label{fig:network_slicing_concept}
\end{figure}

5Gでは，UEが希望するスライス情報（S-NSSAI）をRAN経由でCNに伝え，スライス選択機能により適切なスライスへルーティングされる．しかし，レガシーeNBはS-NSSAIフィールドを持たないS1-APを使用するため，この情報を保持・転送できない．単純なプロトコル変換だけではスライス選択機能を提供できず，デフォルトスライスへのフォールバックやゲートウェイ側でのスライス選択代行など，アーキテクチャ上の工夫が必要となる．

\subsubsection{NAS: セキュリティと鍵管理の相違}
UEとCNが直接通信するNAS（Non-Access Stratum）層においても，4G NAS\cite{3GPP_TS24301}と5G NAS\cite{3GPP_TS24501}はプロトコル仕様が異なる．前述のC-Planeの手順相違（Attach vs Registration/PDU Session Establishment）はNASメッセージ体系にも反映されており，NASレベルでも手順の変換が必要となる．

特にセキュリティアーキテクチャの変更は大きく，5Gでは鍵導出階層が刷新されている．4Gの鍵階層（K → CK/IK → KASME → KeNB）\cite{3GPP_TS33401}と5Gの鍵階層（K → CK'/IK' → KAUSF → KSEAF → KAMF → KgNB）\cite{3GPP_TS33501}は構造が異なり，相互に変換できない．4G UEは5Gの認証手順（5G AKA: Authentication and Key Agreement）を実行できないため，eNBを無改修で5GCに接続する場合，ゲートウェイ側で4G互換の認証ベクトルを生成するか，UEに代わって5GC向けの認証を代行する仕組みが必要となる．

\subsubsection{GTP-U: QoSモデルの変革}
ユーザデータ転送（GTP-U: GPRS Tunnelling Protocol - User Plane）\cite{3GPP_TS29281}自体は互換性が高いが，QoS（Quality of Service）モデルが大きく異なる．4Gは「ベアラ（Bearer: QoSを保証するデータ転送路の論理単位）」単位でQoSを管理するのに対し，5Gは「QoSフロー（QoS Flow: 同一のQoS処理を受けるパケット群を識別する論理単位）」単位で管理する．

両者の本質的な違いは粒度にある．4Gでは1つのベアラが1つのGTP-Uトンネルに対応し，ベアラごとに1つのQoSクラス（QCI: QoS Class Identifier）が割り当てられる．QCIは9段階の数値（1〜9）で定義され，標準化された遅延・信頼性・優先度の特性を持つ．したがって，異なるQoS要件を持つトラフィックには別々のベアラを確立する必要があり，管理が粗粒度となる．一方，5Gでは1つのPDU Session（1つのGTP-Uトンネル）の中に複数のQoSフローを多重化できる．各フローはQFI（QoS Flow Identifier）で識別され，同時に5QI（5G QoS Identifier）が割り当てられる．5QIはQCIと同様に標準化されたQoS特性を表す指標であり，より多くの段階が定義されている．同一トンネル内で異なるQoS処理を適用できるため，細粒度な制御が可能となる．
図\ref{fig:qos_model_comparison}に，4Gと5GのQoSモデルの構造的な違いを示す．

補足として，無線ベアラ（DRB）との対応を明示する．4Gでは各EPSベアラが1つのDRBと1つのGTP-Uトンネルに1対1で結び付くのに対し，5Gでは1つのPDUセッション（1つのGTP-Uトンネル）内に複数のQoSフローが存在し，フローとDRBの対応は多対一または一対一で構成され得る．この構造差が，移行時のQoS整合（QCI→5QI）やトラフィック分類の設計に直接影響する．

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[scale=0.8, every node/.style={font=\small}]
    % === 4G側（左） ===
    \node[font=\small\bfseries] at (3.5,4) {4G: ベアラ単位のQoS管理};
    % DRB annotation (4G)（タイトルより上に配置）
    \node[font=\scriptsize, align=center] at (3.5,3.2) {DRB（無線ベアラ）\\1 Bearer = 1 DRB = 1 Tunnel};

    % UE (4G)
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.8cm, minimum height=1.2cm] (ue4g) at (0,1.5) {UE};

    % eNB（間隔拡大）
    \node[draw=gray!50, rounded corners, fill=GoogleBlue!50, text=gray!60, minimum width=1.8cm, minimum height=1.2cm] (enb) at (3.5,1.5) {eNB};

    % S-GW（間隔拡大）
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.8cm, minimum height=1.2cm] (sgw) at (7.0,1.5) {S-GW};

    % Bearers (ノードの東西アンカーに揃えて描画／短縮表記)
    \draw[-, very thick, GoogleRed] ($(ue4g.east)+(0,0.6)$) -- ($(ue4g.east)!0.40!(sgw.west)+(0,0.6)$);
    \node[font=\tiny] at ($(ue4g.east)!0.5!(sgw.west)+(0,0.6)$) {QCI=1};
    \draw[-, very thick, GoogleRed] ($(ue4g.east)!0.60!(sgw.west)+(0,0.6)$) -- ($(sgw.west)+(0,0.6)$);

    \draw[-, very thick, GoogleRed] ($(ue4g.east)+(0,0.0)$) -- ($(ue4g.east)!0.40!(sgw.west)+(0,0.0)$);
    \node[font=\tiny] at ($(ue4g.east)!0.5!(sgw.west)+(0,0.0)$) {QCI=5};
    \draw[-, very thick, GoogleRed] ($(ue4g.east)!0.60!(sgw.west)+(0,0.0)$) -- ($(sgw.west)+(0,0.0)$);

    \draw[-, very thick, GoogleRed] ($(ue4g.east)+(0,-0.6)$) -- ($(ue4g.east)!0.40!(sgw.west)+(0,-0.6)$);
    \node[font=\tiny] at ($(ue4g.east)!0.5!(sgw.west)+(0,-0.6)$) {QCI=9};
    \draw[-, very thick, GoogleRed] ($(ue4g.east)!0.60!(sgw.west)+(0,-0.6)$) -- ($(sgw.west)+(0,-0.6)$);

    % 説明
    \node[font=\scriptsize, align=center] at (3.5,-0.5) {1 Bearer = 1 GTP-Uトンネル\\各トンネルに1つのQoSクラス（QCI）};

    % === 5G側（右） ===
    \node[font=\small\bfseries] at (14.5,4) {5G: QoSフロー単位のQoS管理};

    % UE (5G)（左右図の間隔拡大）
    \node[draw, rounded corners, fill=GoogleLightGray, minimum width=1.8cm, minimum height=1.2cm] (ue5g) at (11.0,1.5) {UE};

    % gNB（間隔拡大）
    \node[draw=gray!50, rounded corners, fill=GoogleBlue!50, text=gray!60, minimum width=1.8cm, minimum height=1.2cm] (gnb) at (14.5,1.5) {gNB};

    % UPF（間隔拡大）
    \node[draw, rounded corners, fill=GoogleGreen!50, minimum width=1.8cm, minimum height=1.2cm] (upf) at (18.0,1.5) {UPF};

    % DRB annotation (5G)（タイトルより上に配置）
    \node[font=\scriptsize, align=center] at (14.5,3.2) {DRB（無線ベアラ）\\フロー↔DRBの対応は柔軟};

    % PDU Session枠（1つのトンネル）: UE.east〜UPF.west を矩形で囲う
    \draw[very thick, GoogleRed, rounded corners] ($(ue5g.east)+(0,0.80)$) -- ($(upf.west)+(0,0.80)$) -- ($(upf.west)+(0,-0.80)$) -- ($(ue5g.east)+(0,-0.80)$) -- cycle;
    \node[font=\tiny, GoogleRed, anchor=north west] at ($(ue5g.east)+(0.1,1.25)$) {PDU Session (1 GTP-Uトンネル)};

    % QoS Flows (ノードの東西アンカーに揃えて描画／短縮表記)
    \draw[-, thick, blue!70] ($(ue5g.east)+(0,0.6)$) -- ($(ue5g.east)!0.26!(upf.west)+(0,0.6)$);
    \node[font=\tiny, text=blue!70] at ($(ue5g.east)!0.5!(upf.west)+(0,0.6)$) {QFI=1 / 5QI=1};
    \draw[-, thick, blue!70] ($(ue5g.east)!0.74!(upf.west)+(0,0.6)$) -- ($(upf.west)+(0,0.6)$);

    \draw[-, thick, green!60!black] ($(ue5g.east)+(0,0.0)$) -- ($(ue5g.east)!0.26!(upf.west)+(0,0.0)$);
    \node[font=\tiny, text=green!60!black] at ($(ue5g.east)!0.5!(upf.west)+(0,0.0)$) {QFI=2 / 5QI=5};
    \draw[-, thick, green!60!black] ($(ue5g.east)!0.74!(upf.west)+(0,0.0)$) -- ($(upf.west)+(0,0.0)$);

    \draw[-, thick, orange!70!black] ($(ue5g.east)+(0,-0.6)$) -- ($(ue5g.east)!0.26!(upf.west)+(0,-0.6)$);
    \node[font=\tiny, text=orange!70!black] at ($(ue5g.east)!0.5!(upf.west)+(0,-0.6)$) {QFI=3 / 5QI=9};
    \draw[-, thick, orange!70!black] ($(ue5g.east)!0.74!(upf.west)+(0,-0.6)$) -- ($(upf.west)+(0,-0.6)$);

    % 説明
    \node[font=\scriptsize, align=center] at (14.5,-0.5) {1 PDU Session = 1 GTP-Uトンネル\\トンネル内に複数のQoSフロー（QFI）};
  \end{tikzpicture}
  \caption{4Gと5GのQoSモデル比較（DRB対応の違いを併記）}
  \label{fig:qos_model_comparison}
\end{figure}

eNBを無改修で5GCに接続する場合，5GのQoSフローID（QFI）と4GのベアラID（EBI）を適切にマッピングし，GTP-Uパケットヘッダ（拡張ヘッダ等）を変換する仕組みが必要となる．

さらに，5GCのN3インターフェースではQoS情報を運ぶ\textbf{GTP-U拡張ヘッダ（PDU Session Container）}の付与が必須となる．標準仕様のLTE eNBは未知の拡張ヘッダをドロップまたは無視する実装が多く，そのままではダウンリンクパケットが適切に処理されないリスクがある．拡張ヘッダを終端し，4G互換のGTP-Uに再封入する処理が求められる．

\subsubsection{RRCステート管理の不整合}
RRCは無線区間（UE--eNB間）のプロトコルであるが，RRC状態はコアネットワーク層の接続管理状態（4GのECM-IDLE/CONNECTED，5GのCM-IDLE/CONNECTED）と連動している．5Gでは省電力と低遅延復帰を両立する\textbf{RRC Inactive}状態が導入されており，この状態ではRRC接続は解放されているがCM層ではCM-CONNECTEDとして扱われる．5GCはこの3状態モデルを前提にページングや状態遷移を制御するが，レガシーLTE eNBはIdle/Connectedの2状態モデルを前提としており，RRC Inactiveに相当する動作を実行できない．このステート管理の非対称性は，CN層の状態マッピングを行う変換ゲートウェイにおいて考慮すべき設計課題となり得る．

\section{本研究の目的}
\label{sec:research_objective}

前節で明らかにした技術的障壁を踏まえ，本研究の目的を以下のように設定する．

\subsection{研究目的}

本研究の目的は，\textbf{既存の4G RAN（eNB）を改修せずに5GCへ収容し，CN先行移行を可能にする「疎結合アーキテクチャ」を提案・実証すること}である．

ここで「疎結合」とは，RANとCNのライフサイクルを分離し，一方の世代更新が他方に依存しない構造を指す．現行の3GPP標準が前提とする「密結合モデル」では，RANとCNは同一世代で同期的に更新されることが想定されている．これに対し，本研究が提案する疎結合アーキテクチャでは，CNを先行して5GCへ移行しつつ，RANは既存の4G設備を継続利用することが可能となる．

\subsection{達成目標}

上記の目的を達成するため，本研究では以下の3点を具体的な達成目標とする．

\begin{enumerate}
    \item \textbf{世代不変性に基づくプロトコルマッピングの体系化}:
    前節で整理したプロトコル障壁（S1AP↔NGAP，NAS，GTP-U）に対し，4G/5Gの各プロトコル仕様を比較分析する．AAA，移動管理，ユーザデータ転送といった世代不変な機能要件が，各世代のプロトコル上でどのようなパラメータとして実装されているかを整理し，変換ルールとして体系化する．

    \item \textbf{疎結合アーキテクチャの設計と実証}:
    体系化したプロトコルマッピングに基づき，S1インターフェースとNGインターフェースを相互変換するゲートウェイを設計・実装する．特に，4Gと5Gの接続シーケンスの差異（Attach手順とRegistration/PDU Session Establishment手順の違い）を吸収するシーケンス制御手法を導入し，レガシーRANから5GCへの透過的な接続が可能であることを実証する．

    \item \textbf{5GC固有機能への拡張性の明確化}:
    提案アーキテクチャにおいて，ネットワークスライシング等の5GC固有機能を将来的にどのように適用可能か，その設計指針と課題を明らかにする．本研究のPoCでは基本機能の実証に注力するが，5GC固有機能への対応はアーキテクチャの実用性を左右する重要な要素であるため，拡張に向けた考察を行う．
\end{enumerate}

\subsection{研究の範囲}

本研究は，提案アーキテクチャの実現可能性を示す「概念実証（Proof of Concept: PoC）」を主たる範囲とする．

\begin{itemize}
    \item \textbf{実装・検証対象}:
    \begin{itemize}
        \item 接続確立に必要なC-Plane（S1AP↔NGAP）およびU-Plane（S1-U↔N3）のプロトコル変換
        \item 4G/5G間の接続シーケンス差異（ベアラ確立タイミング等）の吸収
        \item 実機環境（sXGP基地局 + Open5GS）における基本動作（位置登録，データ通信）の検証
    \end{itemize}
    \item \textbf{議論・考察対象}（実装対象外だが設計指針を示す）:
    \begin{itemize}
        \item ネットワークスライシング等の5GC固有機能の適用アーキテクチャ
        \item ハンドオーバー等の移動管理機能への拡張性
        \item 大規模環境でのスケーラビリティ
    \end{itemize}
\end{itemize}

\section{本章のまとめ}

本章では，4G EPSと5GSのアーキテクチャを比較し，異世代相互接続の技術的障壁を整理した．主要な知見を以下に要約する．

\begin{itemize}
    \item \textbf{アーキテクチャの刷新}: 5GCはNFV/SBAによりクラウドネイティブ化が進み，CN側の進化速度が加速している．一方，RANは物理的な電波設備を伴うため，更新サイクルが長い（表\ref{tab:4g5g_architecture_comparison}参照）．この非対称性が，「CN先行移行」への需要を生んでいる．
    \item \textbf{移行シナリオの限界}: 3GPP標準のSA/NSA等の移行形態は，いずれもRANの更新（SA）または改修（NSA，DAPS）を前提としており，既存eNBを無改修のまま5GCへ接続する手段が規定されていない．
    \item \textbf{プロトコル障壁}: eNBを無改修で5GCに接続するには，C-Plane（S1AP↔NGAP：メッセージ体系・IE構造の差異），NAS（EPS-AKA↔5G-AKA：鍵導出階層の差異），U-Plane（GTP-U：QoSモデル・拡張ヘッダの差異）の各層でプロトコル変換が必要となる．
    \item \textbf{研究目的の設定}: これらの障壁を克服するため，eNBと5GCの間にプロトコル変換ゲートウェイを介在させ，既存4G RANを無改修で5GCに収容する「疎結合アーキテクチャ」の提案・実証を本研究の目的として設定した．
\end{itemize}

