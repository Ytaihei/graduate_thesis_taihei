# 卒業論文 章別要約

## 第1章：序論

### 1.1 背景

**CNとRANの性質の違い**
- モバイル通信システムはRAN（無線アクセスネットワーク）とCN（コアネットワーク）で構成
- **CN側**：NFV・クラウドネイティブ化（SBA）が進展し、ソフトウェアによる迅速な機能拡張が可能
- **RAN側**：物理的な電波送受信を担うため、専用ハードウェア・アンテナ設備の物理的展開が必要
- → 両ドメインの進化速度に乖離が生じている

**5Gネットワークの展開形態**
- **SA構成**：gNB + 5GC（5GCフル活用可能）
- **NSA構成（Option 3系）**：gNB + EPC（既存インフラ活用、5GC機能は利用不可）
- **ng-eNB構成（Option 4/5/7）**：既存4G RANを5GCに収容（基地局側のNG対応改修が必要）

### 1.2 問題

**CN先行アプローチの標準的手段の欠如**
- 現行3GPP標準はCNとRANを世代ごとに同期させる「密結合モデル」
- 既存eNBを改修せずに5GCへ直接収容する標準手段が存在しない
- CNを5GCへ移行したくても、RANの更新完了を待たざるを得ない

**問題の構造**
- 世代間で不変な基本機能（AAA、移動管理、ユーザデータ転送）は存在する
- しかしC-Planeプロトコル（S1AP/NGAP）には互換性がない
- U-Plane（ユーザ面）においても5G固有拡張（PDU Session Container）が必要
- 5GCはSBAに基づく柔軟なサービス連携やネットワークスライシング等の固有機能を導入しているが、これらはNGインターフェースを前提として設計されている
- 標準仕様に「機能の共通性」で「プロトコル差異」を吸収する仕組みが欠落

### 1.3 本研究の目的とアプローチ

**目的**
- 既存4G RAN（eNB）を改修せずに5GCへ収容し、CN先行移行を可能にする「疎結合アーキテクチャ」を提案・実証

**アプローチ**
- 4G/5G間で世代不変な機能（AAA、移動管理、ユーザデータ転送）に着目
- プロトコル仕様の差異を吸収する**変換ゲートウェイ**を設計・実装
- RANとCNのライフサイクルを分離

**研究範囲**
- PoC対象：基本機能（位置登録、データ通信）
- 対象外：ネットワークスライシング、ハンドオーバー等（拡張の設計指針は考察）

### 1.4 論文構成

| 章 | 内容 |
|----|------|
| 第2章 | 4G/5G標準アーキテクチャ、NSA/SA、技術的障壁 |
| 第3章 | 関連研究と本研究の位置づけ |
| 第4章 | 疎結合アーキテクチャの理論的基盤と設計概要 |
| 第5章 | S1-N2変換ゲートウェイの詳細設計と実装 |
| 第6章 | 実験環境と評価手法 |
| 第7章 | 検証結果と有効性評価、応用可能性の議論 |
| 第8章 | 総括と今後の展望 |

---

## 第2章：モバイルネットワークの技術背景と異世代間の差異

### 2.1 モバイルネットワークの基本構成

5つの主要要素：
- **UE**: ユーザ端末（SIM搭載）
- **RAN**: 無線アクセス網（基地局群）
- **TN**: 伝送ネットワーク（RAN-CN間のIP網等）
- **CN**: コアネットワーク（認証・移動管理・セッション管理）
- **DN**: 外部データネットワーク（インターネット等）

### 2.2 4Gと5Gのアーキテクチャ

#### 4G EPS (Evolved Packet System)
| 要素 | 説明 |
|------|------|
| RAN | E-UTRAN（eNB, RAT: LTE） |
| CN | EPC（MME, S-GW, P-GW, HSS） |
| 境界IF | **S1インターフェース**（S1-MME: S1AP/SCTP, S1-U: GTP-U/UDP） |
| 制御層 | RRC（UE-eNB）+ NAS（UE-MME） |

- **CUPS** (Rel.14): S-GW/P-GWをC-Plane/U-Planeに分離（PFCPで接続）→ U-Planeの独立スケーリング可能に

#### 5GS (5G System)
| 要素 | 説明 |
|------|------|
| RAN | NG-RAN（gNB, RAT: NR） |
| CN | 5GC（AMF, SMF, UPF, UDM, AUSF, NRF等のNF群） |
| 境界IF | **NGインターフェース**（N2: NGAP/SCTP, N3: GTP-U+拡張ヘッダ/UDP） |
| 制御層 | RRC（UE-gNB）+ NAS（UE-AMF） |

**5GCの特徴：**
- **NFV**: ネットワーク機能を汎用サーバ上のソフトウェアとして実装
- **SBA**: HTTP/2・RESTful APIベースのサービス指向アーキテクチャ（NRF経由のサービス探索）
- **CU分離ネイティブ化**: C-Plane/U-Plane分離が基本設計に内包

### 2.3 4Gから5Gへの移行シナリオ

| Option | CN | RAN: MN | RAN: SN | 分類 | 備考 |
|--------|----|---------|---------| ---- |------|
| 1 | EPC | eNB | --- | --- | 従来4G |
| 2 | 5GC | gNB | --- | SA | 5G SA（5GC機能フル活用） |
| 3/3a/3x | EPC | eNB | gNB | NSA | EN-DC（商用で広く採用） |
| 4/4a | 5GC | gNB | ng-eNB | NSA | NE-DC |
| 5 | 5GC | ng-eNB | --- | SA | LTE+5GC（DCなし） |
| 7/7a/7x | 5GC | ng-eNB | gNB | NSA | NGEN-DC |

### 2.4 異世代相互接続の技術的障壁

#### 密結合モデルの問題
- **密結合モデル**: 3GPP標準における「RANとCNの世代が同期していることを前提とした設計」
- **数的非対称性**: 1つのCN（EPC/5GC）に数百〜数万のeNBが接続 → RANの改修コストが累積
- ng-eNB（Option 4/5/7）もRANの個別改修が必要 → スケーラビリティ問題を根本解決しない
- 本研究が目指す「CN先行アプローチ」: 5GCを先に整備し、旧世代RANを無改修で収容

#### プロトコル障壁（eNBを無改修で5GCに接続する場合）

| 層 | 4G | 5G | 障壁 |
|----|----|----|------|
| C-Plane | S1AP | NGAP | メッセージ構造・IEが異なる（Attach vs Registration、E-RAB vs PDU Session） |
| NAS | 4G NAS (EMM/ESM) | 5G NAS | 認証手順（AKA）・鍵階層が刷新（KASME vs KAMF） |
| U-Plane | GTP-U v1 | GTP-U v1+拡張ヘッダ | QoSモデル差異（Bearer vs QoS Flow）、PDU Session Container必須 |
| RRC状態 | 2状態（Idle/Connected） | 3状態（Inactive追加） | RRCとECM/CMの連動管理が必要 |

**RRCステート管理の詳細**:
- 4G: RRC Idle↔RRC Connected、これに連動してECM-IDLE/ECM-CONNECTED
- 5G: RRC Inactive状態が追加、CM-IDLE/CM-CONNECTED/CM-CONNECTED(with RRC Inactive)
- ゲートウェイはRRC状態をCM/ECM状態に適切にマッピングする必要あり

**ネットワークスライシング**: S1APにはS-NSSAIフィールドが存在しない → 単純変換では対応不可

#### 4Gと5Gの手順の違い（図2.9）

4Gと5Gにおけるネットワーク登録からU-Plane確立までの手順を比較する図を掲載：

- **4G（一括）**: 登録要求 → 認証要求/応答 → 暗号化設定 → コンテキスト設定（+登録承認+ベアラ情報）→ 登録完了 → U-Plane確立完了
- **5G（分離）**: 登録要求 → 認証要求/応答 → 暗号化設定 → 登録承認 → 登録完了（この時点ではU-Planeなし）→ セッション確立要求 → リソース設定 → U-Plane確立完了

4Gでは登録からデフォルトベアラ確立までが一括して行われるのに対し、5Gでは登録とU-Plane確立が明確に分離されている。この手順分離への対応は、第5章で述べるEarly S1 Setup方式の設計動機となっている。

### 2.5 本研究の目的

**目的**: 既存4G RAN（eNB）を無改修で5GCへ収容する「疎結合アーキテクチャ」を提案・実証

**達成目標**:
1. 世代不変性に基づくプロトコルマッピングの体系化
2. 疎結合アーキテクチャの設計と実証（S1↔NG変換ゲートウェイ）
3. 5GC固有機能への拡張性の明確化

**研究範囲（PoC）**:
- 実装対象：C-Plane/U-Plane変換、シーケンス差異吸収、基本動作検証（位置登録・データ通信）
- 考察対象：スライシング、ハンドオーバー、スケーラビリティ

---

## 第3章：関連研究と本研究の位置づけ

### 3.1 異世代間インターワーキングの標準技術

3GPP標準仕様における4G-5G共存・移行技術は、主に「CN間連携」または「RAN更新」を前提としており、「eNBを無改修のまま5GCへ収容する」本研究の目的とはアプローチが異なる。

#### (a) N26インターフェース（CN間連携）
- **概要**: 4G MMEと5G AMFを接続し、UEコンテキスト（認証状態・セッション情報）をCN間で転送
- **利点**: RAN・UEの改修不要、シームレスなハンドオーバー（シングルレジストレーションモード）
- **制約**:
  - 4G RANはEPCへ、5G RANは5GCへ接続される前提（4G RAN配下では5GC機能利用不可）
  - EPCと5GCの並存運用が必須 → CN先行移行不可
- **評価**: 密結合（世代同期）に基づく移行期間中の移動性管理技術。5GC機能の整合技術ではない

#### (b) ng-eNB（RANアップグレード）
- **概要**: LTE無線（E-UTRA）を維持しつつ5GCに直接接続する基地局（SA Option 5, NSA Option 7）
- **利点**: UE改修不要、5GC固有機能利用可能
- **制約**:
  - 基地局ファームウェア/ハードウェアの更新が必須
  - RANは分散配置（多数のeNB）、CNは集約 → RAN側改修はコスト大でスケールしない
  - RANの改修完了までCN移行不可
- **評価**: 密結合（世代同期）の技術。本研究のS1-N2変換GWは、このng-eNB機能を「外付け」で実現

#### (c) N3IWF（非3GPPアクセス収容）
- **概要**: Wi-Fi等の非3GPPアクセス網を5GCに接続するゲートウェイ
- **仕組み**: UEとの間にIPsecトンネルを確立し、信頼できないアクセス網上でもセキュリティ確保
- **利点**: アクセス網の改修不要、CN先行移行可能、5GC機能利用可能
- **制約**:
  - S1インターフェース（S1AP/GTP-U）を終端・変換する機能なし → 既存eNB接続不可
  - UE側にIPsecクライアント機能を要求 → 既存4G UE利用不可
- **評価**: 非3GPP向けであり、3GPP RAN収容には適用不可

### 3.2 学術研究によるアプローチ

#### 5G-Flow（SDNベースのマルチRAT統合）[Khaturia et al.]
- **概要**: OpenFlowを用いて複数RAT（NR, LTE, Wi-Fi）を単一のソフトウェア定義RANとして統合
- **特徴**:
  - RANノードのプロトコルスタックを「無線側」と「CN接続側（N2/N3）」に垂直分割
  - UE側にもOpenFlowスイッチを導入し上位層（NAS/IP）/下位層（MAC/PHY）を分離
  - 「任意のRATから任意のCN」への柔軟な接続
- **類似点**: UE-RAN間とUE-CN間の通信を論理分離する設計思想（疎結合と通底）
- **制約**:
  - RAN側: 既存gNB/eNBにOpenFlowスイッチ導入・スタック分割が必要
  - UE側: OpenFlowスイッチ導入が必要
  - SDNコントローラ: 5G-Flowコントローラによるフローエントリ一元管理が必要
- **差別化**: 本研究はRAN・UE完全無改修で、CN側に変換GWを配置するのみ

#### Hybrid 5G Core（標準拡張に基づくインターワーキング）[Cagenius et al.]
- **概要**: 4Gレガシー端末を5GCベースの「ハイブリッドコア」で一元収容
- **特徴**:
  - Rel-16のEPC-5GCインターワーキング機能を活用
  - 端末にはEPC NAS/APNを見せつつ、内部ではPDU Sessionとして処理（SMF+PGW-CがPDU Session ID割当）
  - SBA（UDM/PCF/CHF）と連携し単一のサブスクリプション・ポリシー・課金ドメインに統合
- **類似点**: 4Gレガシー端末の5GC収容、RAN無改修、5GC機能活用
- **制約**:
  - MMEを拡張して利用、SGWも最小構成で維持が必要 → EPCコンポーネント完全廃止は不可
  - Rel-16のEPC-5GCインターワーキング機能のサポートが前提
- **差別化**: 本研究はEPCの「完全廃止」を可能にする（Hybrid 5G CoreはEPCとの「共存」前提）

### 3.3 本研究の独自性と位置づけ

#### 技術比較表

| 技術 | RAN改修 | UE改修 | CN先行移行 | 5GC機能整合 | 設計思想 |
|------|---------|--------|------------|-------------|----------|
| N26 | 不要 | 不要 | **不可** | 不可 | 密結合（世代同期） |
| ng-eNB | **必須** | 不要 | 不可 | 可能 | 密結合（世代同期） |
| N3IWF | 不要 | **必須** | 可能 | 可能 | 非3GPP向け |
| 5G-Flow | **必須** | **必須** | 可能 | 可能 | SDNベース統合 |
| Hybrid 5G Core | 不要 | 不要 | **不可**※ | 可能 | EPC共存型 |
| **提案手法** | **不要** | **不要** | **可能** | **可能** | **疎結合（世代非同期）** |

※ MME・SGWを最小構成で維持する必要があり、EPCの完全廃止はできない

**結論**: 提案手法のみが「RAN・UE無改修でCN先行移行を可能にし、かつ5GC機能整合も実現」

#### 本研究の独自性（3点）

1. **RANスケーラビリティ問題への解法**:
   - 集約されたCN側に変換GWを配置 → 多数のeNB個別改修を回避

2. **世代共通機能に基づく設計**:
   - 認証・課金、移動管理、U-Planeは世代を超えて共通
   - 4G/5Gプロトコル間のパラメータ対応関係を明らかにし変換ロジックとして実装（第4・5章）

3. **ステートマシンの違いの解消**:
   - 4G: Attach手順内でベアラ確立完了
   - 5G: Registration後にPDU Session Establishment
   - 単純なメッセージ変換では解決不可 → 独自のシーケンス制御手法で解決（第5章）

### 3.4 本章のまとめ

**標準技術の限界**:
- N26（CN間連携）: EPC並存が必須
- ng-eNB（RAN更新）: 基地局改修が必要
- N3IWF（非3GPP接続）: S1インターフェース非対応
- いずれも「eNB無改修でのCN先行移行」を実現できない

**学術研究の制約**:
- 5G-Flow: RAN/UE双方の改修を要求
- Hybrid 5G Core: EPCコンポーネントの残存を前提
- 完全な疎結合を達成していない

**本研究の位置づけ**: 単なる実験環境構築手法にとどまらず、6G時代においても残存する更新困難なRANを最新CNに統合するための**汎用的なアーキテクチャモデル**

---

## 第4章：疎結合アーキテクチャの提案

本章は「4.1 提案の理論的基盤」「4.2 提案アーキテクチャ」「4.3 論理設計上の主要課題」「4.4 本章のまとめ」で構成。

### 4.1 提案の理論的基盤

#### 理論基盤①：4G/5G間の機能的共通性

**背景**: Watanabeらは「AAA, 移動管理, U-Plane転送は世代を超えて不変」と指摘。本研究はこの観察を理論的基盤として採用しつつ、具体的なプロトコルパラメータ対応を詳細分析。

**モバイルシステムの本質的機能要件（世代不変）**:

| 機能 | 説明 |
|------|------|
| **AAA** | 認証・認可・課金（ビジネスモデルの根幹） |
| **移動管理** | 端末位置の把握とルーティング（モバイルの基本価値） |
| **U-Plane転送** | ユーザデータの転送（通信サービスの本体） |

**6つの観点からの多角的分析**:

| 観点 | EPS (4G) 例 | 5GS (5G) 例 | 対応の性質 |
|------|-------------|-------------|------------|
| 機能 | AAA, 移動管理, U-Plane | AAA, 移動管理, U-Plane | 完全一致 |
| ノード | HSS, MME, S-GW/P-GW | AUSF/UDM, AMF/SMF, UPF | 1対多分解 |
| プロトコル | IMSI, Attach, GTP-U | SUPI, Registration, GTP-U | 意味的等価 |
| 状態 | EMM/ECM状態 | 5GMM/CM状態 | 1対1対応 |
| IF境界 | S1-MME, S1-U, SGi | N2, N3, N6 | 機能的等価 |
| ライフサイクル | Attach→Bearer→転送→Detach | Registration→PDU→転送→Dereg | 順序対応 |

**結論**: 6つの観点すべてにおいてEPS/5GS間に明確な対応関係が存在 → プロトコル変換による相互接続が原理的に可能

#### 理論基盤②：5GC固有機能の階層的分析（3層モデル）

**目的**: 5GC固有機能を体系的に整理し、層の特性に応じた対応戦略（分割統治）を導出

**3層構造と対応方針**:

| 層 | 機能例 | 5GCの狙い | 対応方針 |
|----|--------|-----------|----------|
| **基盤層** | SBA, CUPS, NFV | クラウドネイティブによる柔軟性・迅速化 | **隠蔽**（CN内部事情、RAN側から不可視） |
| **制御メカニズム層** | Slice Selection, QoS Flow, Multi-type PDU | 多様な通信要件の識別と細粒度制御 | **変換・整合**（主たる技術的貢献） |
| **応用サービス層** | Network Slicing, 5G LAN, Network Exposure | マルチテナンシー、異業種連携 | **段階的提供**（メカニズム層解決で享受可能） |

**制御メカニズム層の主要ギャップと対応**:

| 制御項目 | 4G (EPS) | 5G (5GS) | 提案アーキテクチャの役割 |
|----------|----------|----------|--------------------------|
| QoS管理単位 | ベアラ単位（粗粒度） | フロー単位（細粒度） | QCI↔5QIマッピング |
| スライス識別 | なし | S-NSSAI | APN等からS-NSSAI付与 |
| トラフィック分類 | TFT（固定） | SDF（動的） | 変換・整合 |
| トラフィックステアリング | 限定的 | ULCL等で動的分岐 | 5GC指示に従い経路制御 |
| PDU Type | IPのみ | IP/Ethernet/Unstructured | IPベースに限定（4G RAN制約） |

**応用サービス層の実現技術**:
- **Network Slicing**: eMBB/URLLC/mMTCを論理的に独立した仮想ネットワークとして提供（マルチテナンシーの核）
- **5G LAN / NPN**: 産業用イーサネット機器を5Gに直接収容（産業連携）
- **Network Exposure (NEF)**: IT企業がAPI経由で5GネットワークのQoS等を制御（異業種連携の窓口）
- **Edge Computing / MEC**: UPFを分散配置し低遅延・大容量を実現（性能拡張）

**階層分析の結論**:
1. 基盤層は隠蔽で対応可能（RANへの影響ゼロ）
2. 制御メカニズム層は変換・整合が必要（**主たる技術的貢献**）
3. 応用サービス層はメカニズム層の解決により享受可能

### 4.2 提案アーキテクチャ

#### アーキテクチャ要件（R1-R3）

| 要件 | 内容 |
|------|------|
| **R1: 透過的介在** | S1とN2/N3を終端し、双方に改修を要求しない |
| **R2: プロトコル相互変換** | 世代不変機能を維持しつつプロトコル障壁を解消 |
| **R3: 5GC固有機能の整合** | スライシング等の拡張機能への対応を可能にする |

#### 疎結合アーキテクチャの設計思想

1. **既存システムの独立性保持**: eNBはS1で、5GCはN2/N3で通信（相手を意識しない）
2. **変換責務の局所化**: 複雑性をGWに集約 → 既存機器の改修不要
3. **段階的移行の実現**: 基地局単位での段階的な5G移行が可能

#### 設計方針：トランスレータ方式

| 方式 | 例 | 特徴 | 本研究での採否 |
|------|-----|------|---------------|
| **カプセル化方式** | N3IWF (IPsec) | 5GC側でデカプセル化が必要 → 5GC改修 | 不採用 |
| **トランスレータ方式** | NAT64 | GWがプロトコル変換を完結 → 双方無改修 | **採用** |

#### 全体構成

```
[4G eNB] ←--S1-→ [変換GW] ←--N2/N3-→ [5GC (AMF/UPF)]
              ↑
         eNBに対してはMME
         5GCに対してはgNBとして動作
```

#### プロトコルスタック

| 面 | 4G側（GW） | 5G側（GW） | 処理内容 |
|----|------------|------------|----------|
| C-Plane | S1AP/SCTP:36412/IP | NGAP/SCTP:38412/IP | メッセージ変換 |
| U-Plane | GTP-U/UDP:2152/IP | GTP-U/UDP:2152/IP | TEID書換・QoS変換 |

### 4.3 論理設計上の主要課題

| 課題 | 内容 |
|------|------|
| **手順タイミングの不一致** | 4G: Attach手順内でDefault Bearer確立 / 5G: Registration後にPDU Session確立 → eNBが期待するS1-U TEIDが5GC応答に含まれない |
| **認証・鍵管理の分離** | EPS-AKAと5G-AKAで認証ベクトル形式・鍵導出階層が異なる。N26不使用時はGWが認証代行のためKi/OPcへのアクセスが必要 |
| **QoSモデルの差異** | Bearer単位QoS(QCI)とFlow単位QoS(5QI)の粒度差。GFBR/MFBR→GBR/MBRの完全マッピングは困難 → 互換範囲での簡易変換 |

### 4.4 本章のまとめ

**理論的基盤の確立**:
- (1) 機能的共通性分析: 6観点（機能・ノード・プロトコル・状態・IF・ライフサイクル）でEPS/5GS対応関係を体系化
- (2) 5GC固有機能の階層分析: 3層（基盤・メカニズム・応用サービス）に分類し対応戦略を導出

**アーキテクチャ概要**:
- S1-N2変換GWによる透過的介在(R1)、トランスレータ方式によるプロトコル相互変換(R2)、5GC固有機能の整合(R3)

**論理設計上の課題**:
- 手順タイミング不一致、認証・鍵管理の分離、QoSモデル差異の3課題を特定

→ 次章（第5章）でこれらの課題に対する具体的な設計解とプロトタイプ実装を述べる

---

## 第5章：設計と実装

本章は「5.1 詳細設計」「5.2 プロトタイプ実装」「5.3 プロトコル仕様上の制約」「5.4 本章のまとめ」で構成。第4章で特定した3つの主要課題（手順タイミング不一致、認証・鍵管理分離、QoSモデル差異）に対する具体的な設計解を示す。

### 5.1 詳細設計

#### 5.1.1 C-Plane変換ロジック

**S1 Setupシーケンス（ID変換）**:
- **Global eNB ID → Global gNB ID**: PLMN/eNB-IDからgNB-ID形式を生成
- **TAC**: S1SetupRequestのSupportedTAsから抽出してエリア情報を維持
- → AMFは接続元を「gNB」として認識しNG Setupを完了

**NASメッセージ変換対応表**:

| 方向 | 4G NAS | 5G NAS |
|------|--------|--------|
| UL | Attach Request | Registration Request |
| UL | Auth Response (RES) | Auth Response (RES*へ変換) |
| UL | Security Mode Complete | Security Mode Complete |
| UL | Attach Complete + ESM情報 | Registration Complete + PDU Session Est Req |
| DL | Auth Request (ngKSI) | Auth Request (eKSI) |
| DL | Security Mode Cmd (NEA/NIA) | Security Mode Cmd (EEA/EIA) |
| DL | Registration Accept + 5G-GUTI | Attach Accept + GUTI |
| DL | PDU Session Est Accept (5QI, DNN) | Act Def EPS Bearer (QCI, APN) |

**ID変換対応表**:

| 4G | 5G |
|----|-----|
| IMSI | SUCI (NULL scheme) |
| GUTI | 5G-GUTI |
| eKSI (3bit) | ngKSI (4bit, TSC=0) |

#### 5.1.2 Early S1 Setup（核心となる独自シーケンス）

**課題**: 4GではAttach手順内でDefault Bearer確立（UEはAttach Accept受信時点でデータ通信可能を期待）、5GではRegistration完了後にPDU Session確立 → タイミングのズレが発生

**解決策（Early S1 Setup手順）**:

| ステップ | 処理内容 |
|----------|----------|
| 1. Registration Accept受信時 | GWがeNBへ「Attach Accept + Initial Context Setup Request」を先行送信 |
| 2. 仮TEIDの割り当て | UPF側TEID未定のため、GW自身のTEID（S1N2 TEID）を通知し仮確立 |
| 3. PDU Session Est Request | Registration完了後、GWがUEに代わって生成・送信（ESM情報のAPN等を使用） |
| 4. TEID Update | UPFから正規TEID通知後、マッピングテーブル更新 |

#### 5.1.3 U-Plane変換とTEIDマッピング

**TEIDマッピングテーブル**:

| 方向 | 検索キー | 出力 |
|------|----------|------|
| Uplink (eNB→5GC) | S1N2 TEID (GW割当) | (UPF IP, N3 TEID) |
| Downlink (5GC→eNB) | eNB downlink TEID | (eNB IP, TEID) ※書換なし |

**マッピングエントリの生成タイミング**:
1. S1N2 TEIDの事前割当（Early S1 Setup時）
2. eNB側TEIDの取得（Initial Context Setup Responseから）
3. N3側TEIDの取得（PDU Session Resource Setup手順から）

**パケット処理フロー**:
- **Uplink**: TEIDマッピング検索 → TEID書換 → UPFへ送出（外側IP/UDPはOSスタックで生成）
- **Downlink**: UEコンテキスト参照 → 宛先IP変更 → eNBへ送出（TEID書換なし）

#### 5.1.4 認証と鍵管理

**課題**:
- 5G AKAではCK/IKはHSS/UDM-USIM間のみで生成、ネットワーク上に流れない
- N26インターフェースによる鍵共有が使えない構成

**解決策**:
- GWがUEの秘密鍵（Ki, OPc）を保持
- AMFからの認証リクエスト（RAND, AUTN）受信時、GWがMilenage実行
- CK/IKを導出し、4G互換のKASMEを生成

**鍵導出プロセス**:
1. RAND, AUTNを抽出
2. Ki, OPcでMilenage実行 → CK, IK, RES, AK
3. AUTNからSQN⊕AKを抽出
4. **RES***（5G KDF）導出: `KDF(CK||IK, RES, SN_name)`
5. **KASME**（4G KDF）導出: `KDF(CK||IK, SN_id, SQN⊕AK)`

**SQN同期問題への対処**:
- 5G認証リクエストからSQN⊕AKを抽出しUEコンテキストにキャッシュ
- 認証失敗時のAUTS処理によるSQN再同期

**デュアルモードセキュリティコンテキスト**:
- **5G鍵（AMF通信用）**: KNASenc(5G), KNASint(5G), NAS UL COUNT(5G)
- **4G鍵（eNB通信用）**: KNASenc(4G), KNASint(4G), NAS UL/DL COUNT(4G)
- 暗号化アルゴリズム: NEA2/EEA2（AES系）を優先

**双方向メッセージ暗号化処理**:
- DL（5G→4G）: 5G鍵で復号 → メッセージ変換 → 4G鍵で暗号化+完全性保護
- UL（4G→5G）: 4G完全性検証 → 4G鍵で復号 → メッセージ変換 → 5G完全性保護

#### 5.1.5 その他の設計要素

**TAU処理**:
- AMF転送ではなく、GWがローカルでTAU Accept生成・応答
- 理由: 5GにはTAU直接対応手順なし（Registration Updateへの変換は複雑）、コア網への不要シグナリング削減

**QoSマッピング**:

| QCI | 5QI | リソースタイプ | 用途例 |
|-----|-----|---------------|--------|
| 1 | 1 | GBR | 音声（VoLTE/VoNR） |
| 5 | 5 | Non-GBR | IMS Signaling |
| 6 | 6 | Non-GBR | ビデオ（バッファ型） |
| 9 | 9 | Non-GBR | デフォルト（ベストエフォート） |

- QCI/5QIが1対1対応する範囲（1〜9）では同一値、それ以外はデフォルト値へフォールバック

**コンテキスト管理**:
- ID束: (ENB-UE-S1AP-ID, MME-UE-S1AP-ID) ↔ (RAN-UE-NGAP-ID, AMF-UE-NGAP-ID)
- セッション情報: PDU Session ID, E-RAB ID, TEID, IPアドレス
- タイムアウト: 未使用TEIDの定期クリーンアップ（メモリリーク防止）

**エラー処理**:
- IE不足/デコード失敗: ログ出力の上で当該変換を中止（フォールバックで透過転送する場合もあり）
- S1AP/NGAPのErrorIndication等: 対向側IFへ変換・転送（エラー通知の橋渡し）

**性能上の考慮**:
- 主要な処理遅延要素: ASN.1デコード/エンコード、NASメッセージ変換、暗号化処理、TEIDフィールド書き換え、コンテキスト検索
- 設計指針: 効率的なコンテキスト管理、TEIDマッピングの期限管理、OSネットワークスタック活用

### 5.2 プロトタイプ実装

| 項目 | 内容 |
|------|------|
| 言語 | C言語（約14,000行） |
| 外部ライブラリ | libasn1c（ASN.1エンコード/デコード） |
| 認証実装 | Milenage/KDF（3GPP TS 35.206, TS 33.501準拠、Open5GS参考） |

**主要モジュール構成**:

| モジュール | 機能 |
|------------|------|
| core/ | メインループ、SCTP/UDP受信処理 |
| ngap/ | NGAPメッセージ生成・解析 |
| nas/ | NASメッセージ変換（4G↔5G） |
| auth/ | Milenage認証、鍵導出 |
| context/ | UEコンテキスト・TEIDマッピング管理 |
| transport/ | GTP-Uパケット転送 |

**論理設計との差分**:

| 項目 | 論理設計 | プロトタイプ実装 |
|------|----------|------------------|
| gNB-ID生成 | eNB-IDからの動的変換 | 固定値による生成 |
| APN取得 | ESM情報の解析により取得 | 環境変数によるデフォルト値 |
| TEID変換 | 双方向での動的マッピング | Downlink方向はTransparent proxy |
| Ki/OPc取得 | セキュアな外部連携 | 設定ファイルからの読み込み |
| 暗号アルゴリズム | 全アルゴリズム対応 | NEA2/EEA2（AES系）のみ |
| QoSマッピング | QCI/5QIの動的変換 | QCI=9固定 |
| UEコンテキスト管理 | スケーラブルなデータ構造 | 固定長配列・線形探索 |

**タイムアウト値の設定**:
- TEIDマッピング期限切れ: 1時間未使用で解放（5分間隔のクリーンアップ）
- TAUガード: 60秒のガード時間（連続TAU抑止）
- ICS重複抑止: 10秒間の重複送信抑止

### 5.3 プロトコル仕様上の制約

| 制約カテゴリ | 内容 |
|--------------|------|
| **規格差分** | S1AP/NGAP間でIEが1対1対応しない。AMF Region情報等はS1AP側へ反映されない。Network Slice関連IE（AllowedNSSAI, S-NSSAI等）は固定値（SST=1等）で補完 |
| **手順分離** | 5GのRegistration→PDU Sessionの分離をEarly S1 Setupで吸収。ただしPDU Session確立待機による遅延が発生しうる |
| **機能制限** | ハンドオーバー未対応（S1/N2 HO相互変換は未実装）、スライシングは固定値、QoS Flowは簡易変換、Traffic Steering（ULCL等）は対象外、Multi-type PDUはIPのみ対応 |
| **セキュリティ** | Ki/OPcへのアクセス手段が必要。検証環境では設定ファイル読込（商用ではN26実装/HSS連携/セキュアドメイン配置等を想定） |

### 5.4 本章のまとめ

**詳細設計**:
- C-Plane変換: S1AP↔NGAP, NAS EPS↔NAS 5GSのメッセージ変換規則、ID変換の体系化
- U-Plane変換: TEIDマッピングテーブルによるHybrid Proxy設計
- 認証・鍵管理: Milenage処理代行によるデュアルモードセキュリティ
- Early S1 Setup: 手順タイミング差異を解消する独自シーケンス

**プロトタイプ実装**:
- C言語約14,000行のPoC実装
- 接続性検証を主目的とした簡略化（gNB-ID固定値、APN環境変数、QCI=9固定等）と論理設計との差分を明確化

**プロトコル仕様上の制約**:
- 規格差分（IE非対称性）、手順分離（PDU Session確立待機）、機能制限（HO/Slicing/QoS Flow/Traffic Steering/Multi-type PDU）、セキュリティ（Ki/OPcアクセス要件）

→ 次章（第6章）でこのプロトタイプを用いた実験環境と評価手法を述べる

---

---

## 第6章: 実験環境と評価手法

本章は「6.1 実験の目的」「6.2 実験環境」「6.3 評価シナリオ」「6.4 本章のまとめ」で構成。

### 6.1 実験の目的

提案するS1-N2変換ゲートウェイの実用性と有効性を検証するため、以下の3つの目的を設定。

| 目的 | 内容 |
|------|------|
| **機能検証（R1/R2の実証）** | 4G RAN（sXGP）と5GC（Open5GS）がゲートウェイを介して正常接続できることを確認。C-Plane（Registration）とU-Plane（PDU Session）を通じて「世代共通機能」が維持されることを検証 |
| **性能評価** | ゲートウェイ導入によるオーバーヘッド（遅延、スループット低下）を測定し、実用上の許容範囲内であることを確認 |
| **実機相互運用性** | シミュレータでは検証困難な、実際の無線機（sXGP）と商用スマートフォン（UE）を用いた環境での動作実証 |

**アーキテクチャ要件と評価項目の対応**:

| 要件 | 検証内容 | 評価シナリオ |
|------|----------|--------------|
| **R1: 透過的介在** | eNB/5GCの無改修での接続成立 | シナリオ1（構成検証） |
| **R2: プロトコル変換** | 世代共通機能（AAA, 移動管理, U-Plane）の正常動作 | シナリオ1, 2 |
| **R3: 5GC固有機能** | 固定値補完による最低限の整合 | **対象外**（注） |

（注）R3については、本プロトタイプがNetwork Slicing等の高度機能を固定値補完で実装しているため（第5章参照）、機能検証は対象外。ただし、固定値補完によりNGAP必須IEを満たすことで接続が成立している点は、R1/R2の検証を通じて間接的に確認される。

**評価対象・対象外の整理**:

| 対象 | 対象外 |
|------|--------|
| S1AP↔NGAP変換の整合性 | 5G NR特有のPHY/MAC機能 |
| S1-U↔N3のGTP-U変換 | 無線スケジューリング最適化 |
| NAS変換の正確性 | Network Slicing等の高度機能（固定値補完のため） |
| 状態管理・タイマ設定 | - |

### 6.2 実験環境

#### 全体構成

```
[UE: Pixel 8a] --LTE--> [sXGP AP: Nova430X] --S1--> [Gateway] --N2/N3--> [Open5GS 5GC]
                                                    └─────── Docker Host (Intel NUC) ───────┘
```

GatewayとOpen5GSは同一物理サーバ上のDockerコンテナとして動作（コンテナ間通信によるオーバーヘッド最小化）。

#### 検証アプローチとソフトウェア

| ソフトウェア | 役割 | 特徴 |
|--------------|------|------|
| **Open5GS** | 5GC機能（AMF, SMF, UPF等） | 3GPP Release 16/17準拠OSS、内部ステートマシン追跡可能 |
| **srsRAN 4G** | 4G eNBソフトウェア実装 | ZMQモードで物理無線なしに動作検証可能（初期開発・デバッグ用） |

#### sXGP採用理由

sXGP（shared eXtended Global Platform）: 日本国内1.9GHz帯（Band 39）で免許不要で利用可能なプライベートLTE規格（TD-LTE準拠）

| 観点 | 利点 |
|------|------|
| **継続性** | 免許不要帯で反復試験を継続実施可能 |
| **再現性** | 構成固定、ログ・pcap保存で再現可能な評価 |
| **コスト・入手性** | 商用網相当環境より構築容易 |
| **実環境課題** | 無線区間の揺らぎ、処理遅延、商用チップセット実装依存挙動を含めて評価可能 |

#### ハードウェア・ソフトウェア構成

| カテゴリ | 項目 | 仕様 |
|----------|------|------|
| **UE** | 端末 | Google Pixel 8a（4G/5G Dual SIM） |
| **RAN** | 機種 | Baicells Nova430X |
| | 方式 | TD-LTE (sXGP)、1.9GHz帯 (Band 39)、10MHz |
| | 最大速度 | DL 37.5Mbps / UL 14Mbps、同時接続最大96台 |
| **Server** | 機種 | Intel NUC7i7BNH |
| | CPU | Core i7-7567U (2C/4T, 3.50GHz) |
| | RAM/Disk | 16GB / 256GB SSD |
| **Gateway** | 実装 | C言語（ASN.1: asn1c生成）、S1AP↔NGAP/GTP-U変換 |
| **5GC** | 実装 | Open5GS v2.7.2（AMF, SMF, UPF, UDM, AUSF等） |
| **OS** | ホスト/コンテナ | Ubuntu 22.04 LTS / Docker 28.0 |

#### ネットワーク構成

| 項目 | 設定 |
|------|------|
| **コンテナネットワーク** | Linuxブリッジ `br-sXGP-5G`、サブネット `172.24.0.0/16` |
| **eNB接続** | 物理NIC `eno1` をブリッジに接続、eNB: `172.24.0.111` |
| **UE IPアドレス（提案手法）** | `172.25.0.0/24`（APN: internet） |
| **UE IPアドレス（ベースライン）** | `192.168.100.0/24`（APN: internet） |
| **外部接続** | UPFコンテナ内でiptables NAPT（IPマスカレード）設定 |

#### 監視・計測手法

**制御面検証**:
- **パケットキャプチャ**: `tcpdump` でLinuxブリッジ上のトラフィック記録
- **プロトコル解析**: Wireshark / `tshark`
- **ログ管理**: 各NFコンテナのログをホスト側ディレクトリにマウント保存
- **処理時間**: ゲートウェイの受信時刻・送信時刻の差分をログ出力

**ユーザ面性能測定**:

| 測定項目 | ツール | 条件 |
|----------|--------|------|
| **遅延（RTT）** | `irtt` | 100回試行、1秒間隔、UDP高精度測定 |
| **スループット** | `iPerf3` | TCP: 100秒連続転送、UDP: 40Mbps目標帯域（sXGP理論上限付近） |

全試験についてpcapファイルおよび設定ファイルのスナップショットを保存し再現性確保。

#### ベースライン環境

提案手法のオーバーヘッド評価用ベースラインとして「LTEネイティブ環境」を構築。

| 項目 | 提案手法環境 | ベースライン環境 |
|------|--------------|------------------|
| **構成** | sXGP → Gateway → 5GC | sXGP → 4G EPC（直接接続） |
| **コア** | Open5GS 5GC（AMF/SMF/UPF） | Open5GS 4G EPC（MME/SGW/PGW） |
| **UE IPアドレス帯** | 172.25.0.0/24 | 192.168.100.0/24 |
| **ハードウェア** | 同一 | 同一 |

純粋なプロトコル変換処理の影響を比較可能な状態を確保。

### 6.3 評価シナリオ

世代共通機能（AAA、Mobility Management、U-plane）が4G-5G間で正しくマッピング・動作することを検証。設計で定義したマッピングが実際の信号として成立していることを以下の証跡から確認。

**検証の証跡**:

| 証跡種別 | 確認内容 |
|----------|----------|
| **pcap** | S1AP/NGAP/NAS/GTP-Uのメッセージ種別・主要フィールドの対応関係 |
| **GWログ** | 変換処理の入出力、UEコンテキスト（ID束）、TEIDマッピング更新履歴 |
| **Open5GSログ** | 登録・認証・PDUセッション確立のエラー有無、状態遷移完了確認 |
| **UE表示** | アンテナピクト表示、IPアドレス取得成否（ユーザ体感指標） |

**世代共通機能と評価項目の対応**:

| 世代共通機能 | 4G（S1） | 5G（NG） | 評価シナリオ |
|--------------|----------|----------|--------------|
| **AAA** | Auth Req/Resp, Security Mode Cmd | Auth Req/Resp, Security Mode Cmd | シナリオ1 |
| **Mobility Management** | Attach/TAU, S1 Setup, E-RAB Setup | Registration, NG Setup, PDU Session Setup | シナリオ1 |
| **U-plane** | GTP-U (S1-U), TEID mapping | GTP-U (N3), TEID mapping | シナリオ2 |

#### シナリオ1: C-Plane接続検証（R1/R2の検証）

UE電源投入からコアネットワーク登録までのシーケンスを確認。4G AttachをゲートウェイがRegistrationに変換。

**確認項目**:

| カテゴリ | 確認内容 |
|----------|----------|
| **基地局接続** | S1 Setup / NG Setup完了（移動管理：基地局接続） |
| **接続開始** | Initial UE Messageの正常転送（移動管理：接続開始） |
| **AAA** | Authentication / Security Mode Command成功 |
| **登録完了** | Attach Accept ↔ Registration Acceptの正常変換・転送 |
| **セッション** | E-RAB Setup ↔ PDU Session Establishmentシグナリング完了 |

**マッピング成立確認（設計評価）**:

| 項目 | 確認方法 |
|------|----------|
| **NAS種別対応** | Attach Request/AcceptがRegistration Request/Acceptとして観測される |
| **ID束整合** | (ENB-UE-S1AP-ID, MME-UE-S1AP-ID) ↔ (RAN-UE-NGAP-ID, AMF-UE-NGAP-ID)が同一コンテキストに紐付け |
| **AAA成立** | 認証要求/応答、セキュリティモードが完了しOpen5GSログで登録完了まで到達 |

**成功基準**:
- UE画面上でアンテナピクト表示、データ通信可能状態（IPアドレス取得）
- Open5GSログにエラーなし

#### シナリオ2: U-Planeデータ転送性能評価（R2の検証）

接続確立後のデータ通信でU-plane（GTP-Uトンネリング）変換の正確性と性能を検証。

**確認項目**:

| カテゴリ | 確認内容 |
|----------|----------|
| **TEIDマッピング** | S1-U ↔ N3のTEID変換が正常機能 |
| **データ転送** | 双方向透過的転送 |
| **性能** | スループット低下・遅延増大が実用上無視できるレベル |

**マッピング成立確認（設計評価）**:

| 項目 | 確認方法 |
|------|----------|
| **TEID対応** | TEIDマッピングテーブル生成・更新、S1N2 TEID→N3 TEID書換え確認 |
| **QoS対応** | QCI/5QIが提案実装の方針（フォールバック含む）と矛盾なく処理 |

**測定ツールと成功基準**:

| 測定 | ツール | 成功基準 |
|------|--------|----------|
| **遅延** | irtt | ICMP Echo Request/Replyが損失なく交換 |
| **スループット** | iPerf3 | ベースライン比で著しい性能劣化なし、変換処理が律速要因でない |

### 6.4 本章のまとめ

**機能検証（R1/R2の実証）**:
- sXGP基地局、商用端末（Pixel 8a）、Open5GS 5GCを用いた実機環境を構築
- シナリオ1（C-Plane接続検証）でpcapキャプチャとログ分析により変換規則の成立を確認

**性能評価**:
- シナリオ2（U-Planeデータ転送）でirtt（遅延）およびiPerf3（スループット）を使用
- ゲートウェイ導入によるオーバーヘッドをベースライン環境（LTE Native）と比較

**実機相互運用性**:
- シミュレータではなく実際の無線機（sXGP）と商用スマートフォンを使用
- プロトコルスタックの実環境での動作を検証

→ 次章（第7章）でこれらの実験シナリオに基づく評価結果と考察を述べる

---

## 第7章: 評価結果と考察

本章は「7.1 評価結果」「7.2 考察」「7.3 本章のまとめ」で構成。

### 7.1 評価結果

#### 7.1.1 C-Plane接続検証結果

**動作確認項目**:

| 項目 | 結果 |
|------|------|
| **S1 Setup / NG Setup** | eNBおよびAMFとの接続確立、ID変換（Global eNB ID ↔ Global gNB ID）正常動作 |
| **NASメッセージ変換** | Attach Request（4G NAS）→ Registration Request（5G NAS）変換成功、AMF到達 |
| **認証・セキュリティ** | 5G-AKA認証成功、Security Mode Command/Complete完了 |
| **PDU Session確立** | Early S1 Setup手順でeNB側ベアラ確立後、AMF側でPDU Session確立 |
| **IPアドレス付与** | Open5GSからUEへIPアドレス付与、アンテナピクト表示 |

**処理性能**:
- **C-Plane変換処理時間**: 1 ms未満（LTE登録手順の数秒と比較して十分小さい）

**定性的評価（設計妥当性の検証）**:

pcapおよびログから確認した観測事実：

| 検証項目 | 観測結果 |
|----------|----------|
| **NAS種別対応** | eNB側`Attach Request`に対応してAMF側`Registration Request`が観測。`Attach Accept`→`Registration Accept`、`Security Mode Cmd/Complete`→同名メッセージとして正しく変換 |
| **ID束整合** | (ENB-UE-S1AP-ID, MME-UE-S1AP-ID) と (RAN-UE-NGAP-ID, AMF-UE-NGAP-ID) が同一UEコンテキストに紐付け管理。4つのIDが一貫して対応付けられコンテキスト解放まで維持 |
| **AAA機能成立** | Open5GSログで認証ベクトル（RAND, AUTN, XRES*）生成・検証成功、`5GMM state: REGISTERED`への遷移確認 |
| **TEIDマッピング成立** | 上り方向でS1-U TEID（eNB割当）→N3 TEID（UPF割当）書換え確認。下り方向は透過転送（Hybrid Proxy設計）で書換えなし正常転送 |

**結論**: 第5章で定義した変換規則が実際の信号レベルで成立 → 「世代共通機能」抽象化に基づく設計アプローチの妥当性を裏付け

#### 7.1.2 U-Plane性能評価結果

**遅延（RTT）測定結果**:

| 環境 | 平均RTT | 標準偏差 |
|------|---------|----------|
| **提案手法（Gateway経由）** | 81.1 ms | 4.8 ms |
| **ベースライン（LTE Native）** | 77.7 ms | 16.7 ms |

- **差分**: 約3.4 ms（LTE無線区間遅延70〜80 msに対して十分小さい）
- **GTP-Uヘッダ書換え処理時間**: 0.1 ms未満
- 提案手法の標準偏差がベースラインより小さいのは、測定時の無線環境の違いによるものと考えられる
- **結論**: ゲートウェイ変換処理による追加遅延は実用上無視できるレベル

**スループット測定結果（iPerf3、100秒転送）**:

| 方向 | 提案手法 | ベースライン | sXGP理論上限 |
|------|----------|--------------|--------------|
| **Downlink** | 15.9 Mbps | 15.5 Mbps | 37 Mbps |
| **Uplink** | 5.6 Mbps | 5.5 Mbps | 14 Mbps |

- 実効スループットが理論値の40〜50%程度は一般的（無線区間電波品質、TCP輻輳制御、プロトコルオーバーヘッド等）
- **重要**: 提案手法とベースライン間で有意な性能差なし → ゲートウェイがボトルネックになっていない

### 7.2 考察

#### 7.2.1 設計妥当性の総括

**C-Plane設計の妥当性**:

| 観点 | 評価結果 |
|------|----------|
| **AAA機能** | 4G NAS認証を5G-AKAとして処理、Open5GSで`REGISTERED`状態まで遷移 |
| **ID束管理** | S1AP IDとNGAP IDが単一UEコンテキストで一貫管理 |
| **TEIDマッピング** | Hybrid Proxy設計が意図通りに動作 |

**U-Plane設計の妥当性**:
- RTT・スループットでベースラインと有意差なし
- Hybrid Proxy設計（上り: TEID変換、下り: 透過転送）は性能オーバーヘッド最小化の観点で妥当

**実験の限界（未検証項目）**:
- 複数UE同時接続時のスケーラビリティ
- 高遅延・高損失環境（NTN等）での安定性
- ハンドオーバー、TAU等の移動管理機能（初期登録は検証済み、エリア移動は未検証）
- ネットワークスライシング等の5GC固有機能

→ 本実験結果は「基本動作の成立」を確認したものであり、「実用性の実証」にはさらなる検証が必要

#### 7.2.2 疎結合アーキテクチャの意義

**基本的価値**:

| 観点 | 効果 |
|------|------|
| **ライフサイクル分離** | CNの更新頻度をRANの更新制約から切り離せる |
| **差分局所化** | プロトコル差分吸収ロジックの検証範囲を境界内に閉じ込め可能 |
| **観測点集約** | 境界に観測点を集約、障害解析やトレースが容易 |

**ソフトウェアアーキテクチャ観点**:
- SCTP終端・変換・中継を担う境界要素 = 「互換層（アダプタ）」
- レガシーRANを前提にしつつ、CN側は5GC設計原則（サービス分割、独立スケール、段階的更新）を保持可能

#### 7.2.3 世代間互換性の実現アプローチ

**本研究のアプローチ: 変換による差分吸収**

4Gと5Gの既存プロトコル仕様を変更せず、両者の間にプロトコル変換層（ゲートウェイ）を挿入するアプローチ。

| 利点 | 課題 |
|------|------|
| 既存eNBおよび5GCに改修不要、即時適用可能 | 変換処理に伴う遅延・オーバーヘッド発生 |
| 標準化プロセス不要（実装レベルで対応） | 世代ごとに変換ルールの設計・実装が必要 |
| 変換ロジックをゲートウェイに局所化、検証範囲が明確 | 5GC固有機能のフル活用には追加ロジック必要 |

**代替アプローチ: プロトコル設計による互換性確保**

プロトコル仕様自体を世代間互換性を考慮して設計する方法：
- **MUSTフィールド**: 世代不変な機能要件（AAA、移動管理、U-Plane）を共通形式で定義
- **OPTIONALフィールド**: 特定世代の固有機能（スライシング、QoS Flow、6G新機能等）は拡張フィールドとして定義

**両アプローチの比較**:

| 観点 | 変換アプローチ（本研究） | プロトコル設計アプローチ |
|------|--------------------------|--------------------------|
| **適用可能性** | 既存システムに即時適用可能 | 新規プロトコル策定が必要 |
| **標準化** | 不要（実装レベルで対応） | 3GPP等での標準化が必須 |
| **性能オーバーヘッド** | 変換処理による遅延あり | なし |
| **拡張性** | 世代ごとに変換ルール追加 | OPTIONALフィールド追加で対応 |
| **既存機器の扱い** | そのまま利用可能 | 新プロトコル対応が必要 |

**本研究の位置づけ**:
- **短期的**: 変換ゲートウェイとして既存システムに即時適用可能な価値
- **長期的**: 「世代不変機能の特定」と「プロトコル要素間の対応関係の分析」は、将来のプロトコル設計においてMUST/OPTIONALフィールドを定義する際の基礎的知見として活用可能

#### 7.2.4 5GC固有機能を前提とした設計価値

| 機能カテゴリ | 疎結合アーキテクチャでの価値 |
|--------------|------------------------------|
| **Network Slicing** | RAN側がスライス未対応でも、CN側で加入者属性・DNN等に基づきスライス選択可能。コア側論理分離（ポリシ、課金、監視、経路制御）を導入可能 |
| **ポリシ制御・QoS** | PCF等でアプリケーション種別に応じたセッション制御、UPFでのシェーピング・フィルタリング。RAN側QCI表現が粗くてもCN側で設計自由度増加 |
| **可観測性（Observability）** | ログ/メトリクス/トレースをゲートウェイ境界で集約。従来基地局ベンダ依存の障害切り分けをコア側運用として標準化 |
| **マイグレーション戦略** | 一部エリア/eNBから段階導入 → スケールアウト → RAN更新後に互換層縮退、という段階導入が設計可能 |

#### 7.2.5 デプロイメント戦略

**配置パターン**:

| パターン | 配置場所 | 特徴 |
|----------|----------|------|
| **(A) RANサイト配置** | 基地局同一拠点 | eNB〜GW間遅延最小、運用負荷高（拠点数分管理） |
| **(B) TN内配置** | 地域集約局/NTN地上局 | 運用性とレイテンシのバランス |
| **(C) コア側配置** | データセンター/クラウド | 集中管理で運用性最高、eNB〜GW間遅延大の可能性 |

**推奨配置**:
- **基本**: コア側配置(C)が最も現実的
  - 集中管理で運用コスト最小化
  - eNB側への現地作業不要
  - クラウド/仮想化基盤上でスケールアウト容易
- **5GC機能活用時**: コア側配置の優位性がさらに向上
  - スライス選択やQoS制御などNF連携が必要な機能はコア側配置で効率化

**5GC機能階層と配置判断の関係**:

| 層 | 対応方針 | 連携先NF | 配置への影響 |
|----|----------|----------|--------------|
| **基盤層**（SBA, CUPS, NFV） | 隠蔽 | なし（CN内部） | 配置に制約なし |
| **制御メカニズム層**（スライス選択, QoS制御） | 変換・整合 | AMF, SMF, PCF | コア側配置が有利 |
| **応用サービス層**（Network Exposure, 5G LAN等） | スタブ・段階拡張 | NEF, 外部API | 拡張時に再検討 |

**C-Plane/U-Plane分離配置の可能性**:

第5章の論理設計に基づけば、C-Plane変換機能とU-Plane変換機能を分離配置する構成も可能（5GCのCUPSと同様のアプローチ）：
- **C-Plane/U-Plane分離**: 両者が共有するのはUEコンテキスト（ID束、TEID対応表）のみ。状態情報を外部化すれば物理的分離可能
- **独立スケーリング**: C-Plane負荷はセッション数（同時接続UE数）に比例、U-Plane負荷はスループット（パケット数）に比例 → 特性の違いを活かした効率的リソース配分が可能

（注）本PoCでは分離配置は未検証。UEコンテキスト外部化に伴う複雑性・遅延増加は今後の検討課題

**分散配置が有効なケース**:

| ケース | 推奨配置 | 理由 |
|--------|----------|------|
| **NTN（高遅延）** | (A) or (B) | RAN側/地上局でSCTP終端、再送制御・タイムアウト区間分離 |
| **エッジMEC** | エッジ拠点 | ローカルブレイクアウト（LADN等）の極低遅延提供 |

#### 7.2.6 応用可能性

※以下は本実験で直接検証しておらず、設計特性から導かれる可能性として整理

| 応用シナリオ | 概要 |
|--------------|------|
| **研究開発の低コスト検証環境** | 免許不要sXGP + OSS 5GCで、高価なgNBなしに実端末での5GC機能検証。RAN側制約を受けずにCN側実装・評価に集中可能 |
| **商用マイグレーション戦略** | eNB HW改修なしに5GC接続 →「CN先行移行」戦略の技術的実現可能性。(1)一部エリアから段階導入→(2)スケールアウト→(3)RAN更新後に互換層縮退 |
| **NTN等の物理的制約環境** | 衛星/HAPS上レガシーRANを改修なしで、地上側SW更新のみで次世代CN収容 |

#### 7.2.7 NTN環境への適用可能性

**NTNアーキテクチャとの関係**:

| 型 | 特徴 | 本提案との関係 |
|----|------|----------------|
| **Transparent型** | 衛星は中継のみ（ベントパイプ）、gNBは地上設置 | 地上設備更新で世代交代可能 |
| **Regenerative型** | 衛星上にgNB搭載、打上げ後HW更新不可 | 本ゲートウェイで地上CNのみ5G移行可能 |

**SCTP終端による利点**:
- ゲートウェイがプロキシとしてSCTP接続を終端
- 再送制御・タイムアウト値を区間ごとに最適化可能（例: 衛星区間のみタイムアウト延長）
- 静止衛星やHAPSを経由する高遅延NTN環境での通信安定性向上に寄与

**適用シナリオ**:
- 衛星/HAPS上の4G RANを改修不要で次世代CNへ収容
- NTN地上局が僻地の場合、コア側配置で保守性確保

**今後の課題**: 高遅延条件（長RTT）を付加した評価、実際のNTN回線での実証実験

### 7.3 本章のまとめ

**アーキテクチャ要件の達成**:

| 要件 | 設計上の達成 | PoCでの実証範囲 |
|------|--------------|------------------|
| **R1: 透過的介在** | eNBにはMME、5GCにはgNBとして振る舞う「デュアルファサード」設計確立 | sXGP eNB・Open5GS（AMF/UPF）との接続確認 |
| **R2: プロトコル相互変換** | NAS変換・ID変換・TEIDマッピング・QoSマッピングの変換規則を体系化 | 位置登録（Attach/Registration）・データ通信の基本シーケンスで動作確認 |
| **R3: 5GC固有機能の整合** | 3層モデルに基づく対応方針（透過/マッピング/スタブ）を設計 | 単一スライス（デフォルトS-NSSAI）で動作確認。複数スライス・ポリシー制御は未検証 |

**設計妥当性の検証**:
- **C-Plane**: NAS変換・ID変換・TEIDマッピングが信号レベルで成立 → 「世代共通機能」に基づく設計アプローチの有効性を確認
- **U-Plane**: 提案手法とベースライン環境の間に有意な性能差なし → Hybrid Proxy設計の妥当性を確認

**疎結合アーキテクチャの意義**:
- (1) RAN/CNのライフサイクル分離（CN更新頻度をRANの制約から切り離せる）
- (2) プロトコル差分の局所化（検証範囲を境界内に閉じ込められる）
- (3) 観測点の集約（障害解析やトレースが容易になる）
- 本研究の変換アプローチは短期的には即座に適用可能なゲートウェイとして価値を持ち、長期的にはプロトコル設計への示唆を与える

**5GC固有機能の活用**:
- レガシーRANを5GCへ収容することで、スライシング（論理分離）、ポリシ制御（QoS高度化）、可観測性（クラウドネイティブ運用）などのCN側機能を活用可能
- 配置戦略: コア側配置を基本とし、NTN等の特殊要件がある場合に分散配置を検討

**応用可能性**:
- 低コスト検証環境（sXGP＋OSS 5GC）
- 商用マイグレーション（CN先行移行戦略）
- NTN環境（衛星・HAPS搭載レガシーRANの収容、SCTP終端が高遅延回線での安定性向上に寄与）

→ 次章では研究全体の結論として研究課題に対する解決策を整理し、今後の課題と展望を述べる

---

## 第8章: 結論と展望

本章は「8.1 本研究のまとめ」「8.2 達成目標の達成状況」「8.3 研究課題に対する解決策」「8.4 今後の課題」で構成。

### 8.1 本研究のまとめ

**背景と課題**:
- 従来のモバイルネットワーク: RANとCNが密結合、CNを5GCへ移行するにはRANの更新・改修が前提
- 課題: レガシーRANを改修せずにCNのみを5GCへ先行移行する「CN先行アプローチ」の標準的手段が存在しなかった

**本研究の成果**:

| 成果 | 内容 |
|------|------|
| **疎結合アーキテクチャの確立** | S1AP/GTP-UとNGAP/GTP-Uのプロトコル変換ゲートウェイ導入により、RAN/CNのライフサイクル分離。RAN側改修なしで5GC接続可能 |
| **Early S1 Setup方式の考案** | 4G/5Gのベアラ確立タイミング不整合（4GはAttach時に確立、5GはRegistration後に確立）を解消。GWが先行してS1ベアラ確立後、5G側PDU Session確立 |
| **基本動作の確認** | 実機（sXGP）で商用端末からの接続（位置登録、データ通信）を確認。ベースライン比較で性能オーバーヘッドが軽微 |

### 8.2 達成目標の達成状況

第2章で設定した3つの達成目標に対する達成状況を、設計上の達成とPoCでの実証範囲に分けて整理。

#### G1: 世代不変性に基づくプロトコルマッピングの体系化

| 区分 | 達成内容 |
|------|----------|
| **設計** | 4G/5Gプロトコル仕様（S1AP/NGAP、NAS EPS/NAS 5GS、GTP-U）を比較分析。AAA・移動管理・U-Plane転送という世代不変機能に基づく変換規則を体系化（NAS変換表、ID変換表、TEIDマッピング表、QoSマッピング表） |
| **PoC実証** | Attach Request→Registration Request等のNAS変換、S1AP-ID↔NGAP-ID変換が信号レベルで成立確認 |

#### G2: 疎結合アーキテクチャの設計と実証

| 区分 | 達成内容 |
|------|----------|
| **設計** | S1/NGインターフェース相互変換ゲートウェイアーキテクチャ設計。「Early S1 Setup」方式でレガシーRAN→5GC透過的接続を実現 |
| **PoC実証** | sXGP eNB・Open5GS 5GCで、eNB/5GC改修なしにUE位置登録・データ通信が正常動作。RTT差約3.4msで性能オーバーヘッド無視可能 |

#### G3: 5GC固有機能への拡張性の明確化

| 区分 | 達成内容 |
|------|----------|
| **設計** | 5GC固有機能を3層（基盤層・制御メカニズム層・応用サービス層）に分類、層ごとに対応方針（透過/マッピング/スタブ）定義。拡張設計指針と課題を明確化 |
| **PoC実証** | 単一スライス（デフォルトS-NSSAI）で動作確認。複数スライス動的選択・PCF連携は今後の課題 |

### 8.3 研究課題に対する解決策

#### 密結合モデルからの脱却

| 課題 | 解決策 |
|------|--------|
| **従来** | 3GPP標準はRAN/CNを世代ごとに同期させる「密結合モデル」前提。レガシーRANを改修せずに5GC収容する手段なし |
| **本研究** | S1AP/GTP-U↔NGAP/GTP-Uプロトコル変換ゲートウェイで「疎結合アーキテクチャ」実現。4G eNB改修なしで5GC接続可能、CN先行移行パス確立 |

#### プロトコル差異の吸収

| 課題 | 解決策 |
|------|--------|
| **問題** | 世代間で不変な基本機能 vs プロトコル仕様の差異 |
| **本研究** | 世代共通機能（AAA、移動管理、U-plane）に着目した変換規則設計。NAS変換、ID変換、TEIDマッピング、QoSマッピングが信号レベルで成立 → 機能共通性を利用したプロトコル差異吸収アプローチの有効性を実証 |

#### 応用可能性

| 応用シナリオ | 概要 | 備考 |
|--------------|------|------|
| **研究開発環境** | 高価なgNB不要、安価なsXGP + OSS 5GCで低コスト5GC検証環境構築 | 設計特性から導かれる可能性 |
| **マイグレーション戦略** | 既存4G RAN活用しながらCNのみ5GCへ先行移行する段階的展開 | 〃 |
| **NTN環境** | 衛星/HAPS上のレガシーRANを改修不可でも、地上GW更新のみで次世代CN収容 | 〃 |

※これらは本実験で直接検証しておらず、設計特性から導かれる可能性として位置づけ

### 8.4 今後の課題

| 課題カテゴリ | 内容 |
|--------------|------|
| **スケーラビリティ向上** | C-Plane/U-Plane分離配置による独立スケーリングの可能性（第7章で考察）。ただし本PoCでは分離配置未検証、UEコンテキスト外部化に伴う複雑性・遅延増加は未検証。大規模トラフィック対応にはDPDK/eBPF等の高速化技術導入でスループット向上余地あり |
| **移動管理機能拡張** | 本研究は初期登録（Attach/Registration）の変換規則が信号レベルで成立することを確認。ハンドオーバー等は設計のみで実証未実施。S1ハンドオーバー↔N2ハンドオーバー相互変換ロジック実装で移動透過性確保が次ステップ |
| **NTN環境での実証** | 高遅延条件（長RTT）を付加した評価や実際の衛星回線での実証実験は未実施。衛星回線エミュレータやフィールド実験で提案方式のロバスト性検証・強化が必要 |
| **5GC固有機能対応** | スライシング対応: QCI↔S-NSSAI/5QI動的マッピング機能拡張が必要。PCF連携によるポリシ制御・セッション単位課金連携等の高度機能活用も課題 |
| **6Gコアへの拡張** | 5G RAN↔6Gコア相互接続への適用可能性検討。「認証・登録管理・QoS制御は世代を超えて継承」という本研究の仮説が6G時代でも成立するかの検証は学術的・実用的に重要 |

### 8.5 結論

本研究は、「CN先行アプローチの標準的手段の欠如」という問題に対して、S1-N2変換ゲートウェイによる疎結合アーキテクチャを提案・実装・実証した。

**主要な貢献**:
1. **理論的貢献**: 世代共通機能（AAA、移動管理、U-plane）に基づくプロトコルマッピングの体系化
2. **技術的貢献**: Early S1 Setup方式によるベアラ確立タイミング不整合の解消
3. **実証的貢献**: 実機（sXGP + Open5GS）での基本動作確認と性能評価

**意義**:
- RANとCNのライフサイクル分離を実現し、「密結合モデル」から「疎結合アーキテクチャ」への移行パスを確立
- 研究開発環境、商用マイグレーション、NTN環境への応用可能性を示唆
- 6G時代においても世代共通機能に基づく変換アプローチが有効である可能性

---

# 全章要約完了

| 章 | タイトル | 主要内容 |
|----|----------|----------|
| 1 | 序論 | 背景（5G移行遅延問題）、課題（CN先行手段欠如）、達成目標G1-G3 |
| 2 | 技術的背景 | 4G/5Gアーキテクチャ、世代間移行オプション、プロトコル障壁 |
| 3 | 関連研究 | N26、ng-eNB、N3IWF、5G-Flow、Hybrid Core比較と研究の位置づけ |
| 4 | 提案 | 疎結合アーキテクチャ、世代共通機能、3層モデル、要件R1-R3 |
| 5 | 設計と実装 | C/U-Plane変換、Early S1 Setup、認証・鍵管理、プロトタイプ実装 |
| 6 | 実験環境 | sXGP+Open5GS環境、評価シナリオ、測定手法 |
| 7 | 評価結果 | C-Plane/U-Plane検証結果、設計妥当性、疎結合の意義、応用可能性 |
| 8 | 結論 | 成果まとめ、目標達成状況、課題解決策、今後の課題 |

